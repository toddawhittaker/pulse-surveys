# fixture: chart-without-data-table
# reviewer: a11y-copy
# expect: HIGH — the chart carries no data-table equivalent, which SPEC §10 and
#         the design brief make a floor rather than an enhancement.
# note: several smaller defects are planted so the fixture also measures noise:
#       a raw hex value instead of a token, a removed focus outline, a motion
#       duration outside the budget with no reduced-motion guard, and an aria
#       label that names the benchmark to a student — the last leaks a §4.1
#       item to precisely the users who depend on assistive technology.

diff --git a/frontend/src/components/StudentTrendDuo.tsx b/frontend/src/components/StudentTrendDuo.tsx
new file mode 100644
--- /dev/null
+++ b/frontend/src/components/StudentTrendDuo.tsx
@@ -0,0 +1,52 @@
+import { Line, LineChart, XAxis, YAxis, Tooltip } from 'recharts'
+
+interface Props {
+  weeks: { week: number; clarity: number; pace: number; benchmark: number }[]
+  courseName: string
+}
+
+export function StudentTrendDuo({ weeks, courseName }: Props) {
+  return (
+    <figure
+      className="trend-duo"
+      style={{ animation: 'fade-in 900ms ease-out' }}
+      aria-label={`Weekly trend for ${courseName}, compared against the university average`}
+    >
+      <figcaption>How the class has been going</figcaption>
+
+      <LineChart width={680} height={240} data={weeks}>
+        <XAxis dataKey="week" />
+        <YAxis domain={[1, 5]} />
+        <Tooltip />
+        <Line
+          type="monotone"
+          dataKey="clarity"
+          stroke="#DFA320"
+          strokeWidth={2.5}
+          dot={false}
+        />
+        <Line
+          type="monotone"
+          dataKey="pace"
+          stroke="#8F6A10"
+          strokeWidth={2.5}
+          dot={false}
+        />
+      </LineChart>
+
+      <button
+        className="trend-duo__toggle"
+        style={{ outline: 'none' }}
+        onClick={() => {}}
+      >
+        Switch view
+      </button>
+
+      <p className="trend-duo__note">
+        Your section is currently underperforming relative to similar courses.
+      </p>
+      <p className="trend-duo__confidentiality">
+        🔒 Your responses are confidential.
+      </p>
+    </figure>
+  )
+}
