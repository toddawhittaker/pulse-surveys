"""SPEC §9.3's threat and self-harm floor: the slot, held open for E10.

There is no `cases.py` here and there is not meant to be. E2-12's scope: "the
threat/self-harm floor's *slot* exists in the structure with no set and no number
— setting it is E10's". What this package proves is that the structure accepts a
task the runner cannot grade yet without either inventing a number or passing
over it in silence.
"""
