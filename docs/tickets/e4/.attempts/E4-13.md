# E4-13 attempts

## Attempt 1 — the census, the two comment blocks, the closure, 2026-09-06

**Lane note, recorded plainly.** This is a heavy-lane ticket built without a
test-author phase, and there is no red run to confirm at the top of this log.
The whole deliverable is comment blocks and records: nothing executes, so there
is nothing a test could have asserted first. Acceptance criterion 3 is what
takes the place of a red — the code diff touches only comment blocks, so a
reviewer can verify the no-behavior-change claim from the diff shape alone. The
diff is two files, `frontend/src/routes/care/index.tsx` (a docstring) and
`frontend/src/styles.css` (the opening comment), and no line outside a `/* */`
or `/** */` block moved.

**The census, built from the routes rather than from another record.** The
docstring's stale clause was its claim about the sibling landings, so each was
read for what motion would look like — an `animation`, a `transition`, a
`@keyframes`, a JavaScript animation library — rather than for the sentence it
makes:

| Route | What it renders | Motion | Where that is visible |
|---|---|---|---|
| `routes/admin/index.tsx` | `components/LandingView.tsx` | none | `styles.css` 62-87: `.pulse-landing`, `.pulse-landing h1`, `.pulse-landing p`, `.pulse-line` — four rules, no `animation`, no `transition` |
| `routes/care/index.tsx` | same | none | same block; `docs/DESIGN_BRIEF.md` line 141 names it: "Care queue: no motion at all. Stillness is the design" |
| `routes/instructor/index.tsx` | same | none | same block |
| `routes/leadership/index.tsx` | same | none | same block |
| `routes/student/index.tsx` | `StudentWeeklySurvey` (not a landing since E2-10) | nine declarations | `styles.css` 297, 303, 361, 382, 495, 540, 596, 636, 640 — `transition: border-color`, `pulse-beat`, `pulse-reveal` three times, `transition: opacity`, `pulse-draw`, `pulse-terminal-dot` |

The controls, because an absence proved by a search is only as good as the
search (`docs/MISTAKES.md` entry 3): the same grep that reported no motion on
the landings found all nine survey declarations and the reduced-motion kill
switch in `design/tokens.css` 63-68, so it sees motion where motion certainly
is. A second grep for `requestAnimationFrame`, `framer` and `motion` across
`frontend/src` found nothing but prose, and every `className` in the tree is a
literal `pulse-*` string — no Tailwind utility, no computed class — so no rule
reaches a landing that the stylesheet does not spell out.

**What the docstring wrongly claimed.** "none of the five landings has any"
motion. Two things: there are four landing views, not five, since E2-10; and
the route that stopped being a landing is the one surface in the tree that does
have motion. Corrected to name the four, say none of them has any, and say what
the fifth route became. The component's other claims were checked and left:
SPEC §13 does list `routes/care/` as the threat queue, §6.2 does keep the
surface to that queue, and there is no queue yet.

**The stylesheet's opening comment** takes both of the carried entry's
alternatives rather than choosing between them: it is dated in words — E1-04,
2026-08-25, which is commit 4a616df, the commit that wrote both comments — and
marked as that ticket's history. Its sentence is kept rather than rewritten, and
one dated line records where the utility-class question stands: no `className`
in `frontend/src` is a Tailwind utility as of 2026-09-06, checked by listing
every `className` in the tree.

**Gates.** eslint over `frontend/` with the repository's own flat config, run
through the main checkout's `node_modules`: `src/routes/care/index.tsx` linted
with zero findings, proven to have actually been read by asking for JSON output
(the file appears in the results with `errorCount: 0`) and proven live by a
planted `export const x: any = 1` in the same directory, which the same command
refused with `@typescript-eslint/no-explicit-any` before it was deleted. The
run over the whole package reports 28 errors, all of them
`no-unsafe-call`/`no-unsafe-member-access` in `WeekEyebrow.test.tsx`,
`vite.config.ts` and `vitest.config.ts` — the shared install predates E4-16 and
holds neither `vitest` nor `@vitejs/plugin-react`, so those types cannot
resolve. Nothing in that set is this ticket's diff, and CI's own install
answers it. `ruff format --check backend` and `ruff check backend` both exit 0;
no Python changed in any commit here, so mypy was not run.

Worked. No dispute needed — nothing asserts on either comment block, checked by
grepping `tests/` and `scripts/` for the file paths and for the sentences
themselves.

## Attempt 2 — the carried entry's closure, 2026-09-06

Closed in `docs/tickets/e4/carried-from-e3.md`, which carried the item in its
ledger paragraph with no section of its own; it has one now, naming both halves
of the done-when and what closed each. The source entry in
`docs/tickets/e3/carried-from-e2.md` takes the same closure note, in that file's
own convention of recording a closure where the entry lives — and it corrects
one thing while there: its owner line said the next ticket covering
`frontend/src/routes/care/` would be a light one, and `.claude/heavy-lane-paths.md`
makes any path matching `*care*` heavy.

## The wave ledger, on this branch but not this ticket

The wave-3 records commit rides here because this was the branch open when it
came due; its own commit message carries the re-derivation of every figure. In
short: `docs/MISTAKES.md` entry 22 went 16 to 17 and entry 48 went 1 to 2, both
read from the file before they were changed and both bumped for E4-05 (PR #187);
entry 22's detail file took the trim its header note called overdue, keeping the
three most recent instances dated from git; and the E4 README's Merged column
gained the four cells for 01, 05, 14 and 16, each checked against `gh pr view`
and `git log --first-parent`, while row 04's cell — a copy of row 02's merge for
a ticket that has no pull request — was emptied.

## Defects found while verifying, fixed nowhere here

None in code. One record defect, fixed as part of the ledger: the E4 README
credited E4-04 with E4-02's merge.
