# E4-06 — attempts

## 2026-09-06 — the red baseline, confirmed before anything was written

Ran the seven new modules plus the four edited inventories on `1bb5541`:
**24 failed, 73 passed**, matching the test manifest exactly. Every behavioural
red was a `pytest.fail` naming `app.jobs.tasks` as a module with no
`generate_weekly_summaries`; the three grant-side reds were assertions
(`pulse_app` holding nothing on `weekly_summary`). The two control tests in the
scheduling module and the "still ungranted" control were green, as predicted.

Worked.

## 2026-09-06 — the walk, the task and the beat entry

`backend/app/services/reporting.py` — SPEC §13's own module name for §5.1, which
did not exist yet — holds `generate_missing_summaries(session, *, settings,
gateway=None)`. It selects `survey_window` rows whose `closes_at` is before the
effective clock and which carry no `weekly_summary`, then per stream gathers the
week's comments through `question.stream`, calls `summarize_stream`, and adds one
row. One transaction per section-week over both streams; `AIGatewayError` is
caught, rolled back, logged at `WARNING` naming the section and the exception
classes, and the walk continues. Everything else propagates.

`app.jobs.tasks.generate_weekly_summaries(gateway=None)` is the thin wrapper, and
`app.jobs.schedules` gains `"generate-weekly-summaries"` on
`crontab(day_of_week="mon", hour="2", minute="50")`, appended at the end.

Worked: all 24 behavioural reds green.

**One near miss worth recording.** The first edit to `schedules.py` changed
E3-06's existing `post-participation-scores-weekly` entry from minute 20 to
minute 50 instead of appending a new entry. It was caught by re-reading the diff
immediately and reverted in the same minute, but the ticket's own scheduling
tests would *not* have caught it: they read this ticket's entry, and the beat
inventory test in `test_celery_app.py` compares the set of tasks rather than
their minutes. A parallel branch also appends here, so re-read the whole mapping
after any edit to this file.

## 2026-09-06 — the moderation filter needs a grant three tests refuse

The filter SPEC §5.1 requires ("the summaries exclude flagged-held content") reads
`public.moderation_state`, and the walk runs as `pulse_app`, which holds nothing
on that table. Every mechanism for reading it is closed by a test in this
ticket's own set:

- a table grant reds `test_the_summary_writer_is_granted_insert_and_select_and_nothing_wider.py::test_the_three_tables_this_ticket_does_not_write_to_are_still_ungranted`,
  `test_identity_grants.py::test_the_runtime_roles_hold_no_privilege_on_a_base_table_beyond_the_reveals_own`
  and `test_report_schema.py::test_neither_runtime_role_holds_any_privilege_on_a_table_with_no_writer_yet`;
- a view granted to `pulse_app` reds `test_identity_grants.py`'s
  `SANCTIONED_VIEW_COLUMNS` equality;
- a `SECURITY DEFINER` function `pulse_app` may execute reds the same file's
  `SANCTIONED_APPLICATION_EXECUTE` closed set.

All three expected sets live under `tests/`, which is read-only here.

Measured both ways rather than argued:

- with `moderation_state_grants_v001.sql` executed — **94 passed, 3 failed**, the
  three failures being the inventory assertions above, all naming the same grant;
- with the `op.execute` of that file commented out —
  `test_the_summary_job_feeds_no_moderation_held_comment_to_the_model.py`'s two
  tests fail with `psycopg.errors.InsufficientPrivilege: permission denied for
  table moderation_state`, and the three inventory tests pass.

Shipped the grant (narrowest verb, `SELECT` only, every write verb withheld with
its sentence) and raised `docs/disputes/E4-06-01.md`. That is the course
`docs/MISTAKES.md` entry 22 and the E2-04 and E2-06 precedents set, and it is what
those three tests' own docstrings name as the repair: "if one of them is
deliberate it belongs in the ticket that spends it, recorded in
`RUNTIME_BASE_TABLE_PRIVILEGES` … with the sentence it comes from".

Stopped on that item. Nothing else waits on it.
