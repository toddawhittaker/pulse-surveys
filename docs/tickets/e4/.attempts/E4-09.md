# E4-09 attempts

## Attempt 1 — the three stat components, light lane, 2026-09-06

Built in `/home/todd/projects/pulse-worktrees/stat-components` on
`e4/stat-components`, cut from `epic/e4-instructor-monday-report` at 41250d4.
Read first: the ticket, the breakdown's payload sketch and its decision 5,
ADR 0151's conventions, `docs/DESIGN_BRIEF.md`, `design/tokens.css`, the three
prototypes plus `design/Usage Rules.md` and the report screen's stat region,
SPEC §7.6, §5.1, §4.1 items 4 and 5, §3.3, and `docs/MISTAKES.md` whole.

Built (nine new files, no existing file touched):

- `frontend/src/components/instructorReportStatCopy.ts` — every string, one
  keyed mapping in `studentSurvey.ts`'s shape, plus the two format rules
  (`formatStatistic`, one decimal; `formatRate`, whole percent from the 0–1
  fraction). Beside the components rather than under `frontend/src/copy/`
  because the inventory collects that directory recursively and E4-12 owns the
  governance map's growth; the header says so.
- `frontend/src/components/instructorReportStats.css` — component-scoped,
  tokens only, no motion. `frontend/src/styles.css` untouched: three report
  tickets are building in parallel.
- `RatingHistogram.tsx`, `StatPair.tsx`, `ResponseRateBar.tsx` with their
  `.test.tsx` files, and one fixture module
  (`instructorReportStats.fixtures.ts`) in the payload sketch's shape.

Decisions taken and written down in the code:

- Props: `RatingHistogram({stream, distribution})` with five declared bucket
  members; `StatPair({median, mean})`, each `number | null`;
  `ResponseRateBar({response, validity?})` over a `RateFigure`
  (`rate`, `numerator`, `denominator`). The validity figure is optional so a
  student surface (E8) gets no validity word in its DOM at all — §3.3's
  audience rule enforced by the prop's absence rather than by a caller
  remembering.
- The absent treatment: an em dash where the figure would be (the prototypes'
  own treatment in `CarePanel` and `JobRow`) plus the brief's empty-state
  sentence, "No responses yet this week". A rate with no responses behind it
  states its counts and withholds the percent ("0 of 21"); a rate with nothing
  to be a rate of states neither.
- Histogram titles are the stream names §5.1 groups comments by, not the
  quoted survey questions the prototype shows: §3.2 keeps the question wording
  in a versioned table, and `studentSurvey.ts` already refuses to copy it.
- No comparison prop and no comparison string anywhere — E5's, through E4-07's
  guarded member (§4.1 item 7).

Verification: all four npm gates green (test 16/16 in 4 files, typecheck,
lint, build, exit 0 each). Seven mutations applied one at a time and all seven
killed — truncation instead of rounding, an unrounded percent, an absent
statistic rendered as 0.0, "N of M" recomputed from the rate, a percent shown
for a week nobody answered, the validity row always rendered, and empty
buckets dropped. The two Python sweeps that read this diff — the tokens-only
hex sweep and the §4.1 copy-inventory invariant — pass (49 passed), and every
shipped string was checked mechanically against the invariant module's
vocabulary tuples with a canary sentence proving the check was not blind: no
offenders.

Left undone deliberately: no ADR (the contestable decisions were settled in
the work order and the ticket, and the wave shares one ADR counter across
three parallel branches — flagged to the orchestrator instead), and no entry
in `docs/tickets/e4/deferred.md` for the payload sketch's missing
valid-response count, because that file is shared with the two parallel
tickets; reported to the orchestrator for E4-07/E4-11 instead.
