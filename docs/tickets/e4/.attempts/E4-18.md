# E4-18 — implementer attempts

## 1. Confirming the reds (2026-09-07)

Ran the two new modules against the tests-first tree at 394ec60, controls first:

    tests/integration/test_the_instructor_section_list_names_nothing_about_a_section_she_does_not_teach.py
    tests/integration/test_the_instructor_section_list_answers_the_shape_e4_18_declares.py

Result: **10 failed, 1 passed**, which is exactly the manifest's prediction. The
one green is the must-be-green control
(`test_the_seeded_instructor_reads_the_existing_report_for_the_section_she_teaches`),
so the world's teaching grant is real and every absence assertion in the pair is
measuring something. Every red is a FAILED on a status or a list, none an ERROR
in setup — the two things `docs/MISTAKES.md` entries 44 and 47 ask for.

## 2. The route, built to the brief's five settled decisions (2026-09-07) — worked

Four files, one commit, no schema change:

- `app.services.authz`: `_TEACHING_INSTRUCTOR_SECTIONS` (a module-level `text()`
  selecting `DISTINCT section_id` from `public.assignment_scope` on person, the
  cast role and a non-null `section_id`) and `taught_section_ids`, beside
  `teaching_instructor_assigned`, added to `__all__`.
- `app.services.reporting`: `taught_sections(session, *, person_id)` — `None` is
  an empty list, ids become `Section` rows, each entry labelled by the module's
  own `_course_label`, sorted by `(course_label, code, str(section_id))`.
- `app.schemas.report`: `TaughtSection` and `TaughtSections`, frozen like their
  siblings and in `__all__`. No `Field(description=...)` anywhere in them, so
  there is no non-docstring prose for the org-views sweep to read
  (`docs/MISTAKES.md` entry 43).
- `app.api.instructor`: `SECTIONS_PATH`, `read_taught_sections` — sync, behind
  `require_instructor`, reusing `_person_of`, setting `Cache-Control: no-store`.

Result: **10 passed, 1 failed** in the two modules. The one failure is the
control above, which is a consumer of
`tests/fixtures/report_api.py::instructor_route_objects` and now fails on that
fixture's exactly-two count — the work order's step 2, expected and not a defect.
Gates over the four files: `ruff format --check`, `ruff check` and `mypy` all
clean.

Records corrected in the same change rather than left false
(`docs/MISTAKES.md` entry 1): the module docstring's "Two routes and no others"
and its refusal-pair and `no-store` sentences; `app.api.deps.require_instructor`'s
"E4-07's two report routes depend on this"; and the reporting module's
`TYPE_CHECKING` note and `_payload` docstring, which both claimed `_payload` was
the only function importing the schema (it was already two — `_stored_summaries`
— and is now three).

## 3. The full suite (2026-09-07)

Run in the next section of this file as it resolves.
