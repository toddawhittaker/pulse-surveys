# E4-05 — attempts

Worktree `/home/todd/projects/pulse-worktrees/e4-05`, branch
`e4/weekly-summary-task`, red-test commit `420357e`.

## Reds confirmed before any change (2026-09-06)

`tests/unit/` summary modules: **35 failed, 8 passed** — exactly the predicted
set. The eight greens are the two matcher controls, the theme-count bound, the
two-streams check, the registry slot, the no-registered-task check, the
sycophantic-variant search, and the mock's validity regression guard.
`tests/integration/test_ai_gateway_summary_roundtrip.py`: 5 failed. Every red is
a `pytest.fail` naming a missing deliverable or a `ValidationError` raised inside
a test body, so nothing is a collection or setup error.

## Attempt 1 — the contract split (worked)

`WeeklySummaryOutput` redefined to `stream` / `summary` / `themes` with the
model-produced `comment_count` removed; new `WeeklySummaryRecord(ContractModel)`
carrying `summary`, `response_count` (ge=0) and `held_note_type: str | None =
None`.

**The risk that had to be measured rather than assumed.**
`tests/unit/test_ai_contracts.py` finds a task's contract by matching a name
fragment — `summary` / `summar` — against the class names in the module, and a
tie is fatal there (the memory about fixtures discovering callables by name).
`WeeklySummaryRecord` matches that fragment too, so the pair is a tie.
`matching_models` resolves it through `composed_into_another`: the output is a
field of the record, so the record is the outermost model and becomes "the
summary contract" for that whole module. Every downstream test there —
auditability, serialisation round trip, undeclared keys, frozen-after-validation,
no-identity-field — then runs against the record and reaches the audit pair
through `summary.prompt_version`.

Measured rather than reasoned about: `tests/unit/test_ai_contracts.py` plus
`tests/unit/test_the_weekly_summary_contract.py` = **68 passed**. No dispute
needed and no rename required.
