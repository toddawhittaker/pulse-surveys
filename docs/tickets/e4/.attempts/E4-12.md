# E4-12 — attempt log

## 2026-09-08 — tests first and red (commit 322a7e7)

The test-author's suite landed formatted and lint-clean. The verifier's
red-run: 7 failed, 3366 passed over unit+integration, every red a behavioral
assertion failure, the split matching the manifest exactly; the isolated
`-m invariant` pass collected 306 with no skips and `check_invariants.py`
named the same four invariant reds.

The symlink walk fix (`recurse_symlinks=True` on both walks in
`tests/fixtures/copy_inventory.py`) shipped inside the tests commit, so its
red-first proof was staged rather than committed red: with the two call-site
arguments reverted, `-k symlink` ran 2 failed
(`test_the_copy_file_walk_descends_a_symlinked_directory`,
`test_the_coverage_walk_descends_a_symlinked_directory`) and 1 passed
(`test_the_copy_file_walk_still_follows_a_symlinked_file`), exit 1; with the
fix restored, all three pass. The run output is preserved in the session
scratchpad (`e4-12-symlink-red-proof.txt`) and summarized here because the
scratchpad does not outlive the session.

## 2026-09-08 — D1, the move (commit 255fcd4) — worked, with one departure

`git mv` of the four `instructorReport*Copy.ts` files into
`frontend/src/copy/`, imports rewritten in ten components and two routes, and
the three stale header paragraphs replaced (plus the same claim in
`lib/landings.ts`).

**The move alone did not parse.** `instructorReportStatCopy.ts` carries
`formatStatistic` and `formatRate` below its object literal, and both name a
copy key — `copy('instructor_report_stats.absent_figure')`. The inventory
parser refuses any quotation mark outside the literal, in either direction, so
the first run after the move went from 7 reds to 14: `collect_frontend_copy`
raised `CopyParseError` at that line and every rule reading the frontend half
failed with it.

Considered and rejected: disputing the parser rule (it is deliberate E2
machinery with its own refusal controls, and "a sentence returned by a helper
below the literal" is one of them — the rule reads a quotation mark, not a
meaning, and that bluntness is the point); a defaulted parameter or a
caller-supplied fallback (both change an exported signature the frontend tests
hold); reaching the entry without naming its key (nothing in TypeScript does
that without being clever). Taken: the two formatters moved to a new
`components/instructorReportFigures.ts` and import `copy`/`fillCopy` from the
copy module. Rounding a percent is presentation, not words, so this is where
they belonged. It is a departure from D1's "a move, not a rewrite" and it
crosses a module boundary, so it is called out rather than buried.

After the move: 4 reds left of the original 7 — the two gradebook rows (D4),
the credit note (D9), and the sweep, whose findings are down from 205 in 16
files to the 2 in `UnknownAddress.tsx` that D6 fixes. Frontend: typecheck and
eslint clean; vitest 111 passed, 1 failed —
`PulseTrendChart.test.tsx` reads `instructorReportTrendCopy.ts` by its old path
(`COMPONENT_SOURCES`, line 488) and holds the old import spelling in `ALLOWED`
(line 596). Both are the orchestrator's to repair.

## 2026-09-08 — D6, the fallback screen (commit c8cae5c) — worked

`frontend/src/copy/unknownAddress.ts`, two entries, texts unchanged;
`UnknownAddress.tsx` renders both by lookup. The tree-wide sweep went green with
it: 205 findings in 16 files at the start of the ticket, 2 in 1 file after the
move, 0 now. Frontend typecheck and eslint clean.

## 2026-09-08 — D4, the gradebook (commit b34d0f2) — worked

`backend/app/copy/gradebook.py` with `gradebook.line_item_label` and
`gradebook.ledger_line`, both texts copied from the constants rather than
retyped; `ags.PULSE_LABEL` and `grading.LEDGER_LINE` read the entries' `.text`.
`LEDGER_JOIN` untouched.

Checks that mattered, all green: the grading module's purity sweep
(`app.copy.gradebook` is none of network, AGS or job); MISTAKES 43's org-views
prose sweep, which reads non-docstring strings under `backend/app/` and sees
these two texts in their new file; `ruff format` and `ruff check`; `mypy`, 86
files. The byte-for-byte pins: 272 integration tests across the grading, AGS,
line-item, participation, passback and score modules, passed untouched.

The whole unit suite under `-n 4`: 1512 passed, 1 failed — only D9's credit
note left.

## 2026-09-08 — D9, the credit-rule note (commit c8fb921) — worked

`instructor_report_page.participation_credit_note`, rendered in the
Participation region after the rate-bar-or-absent block, in every week. Three
sentences: what the validity rate counts and what an invalid response costs
(§3.3), that credit is completed items out of total items with the per-week
arithmetic in the gradebook comment (§3.4), and that a later judgement can
lower a posted score. Written from the work order's draft; the second half was
reworded so the note does not repeat the gradebook column's label, which is a
governed string of its own now and would have been a second copy to keep in
step.

Constraints checked rather than assumed: it carries no marker from
`CONFIDENTIALITY_MARKERS`, so the report still counts exactly one line; it
carries nothing from the underperforming, ranking or instructor-counting
vocabularies; it names no score and no number.

The inline comment at the two `CommentGroup` calls was corrected in the same
commit — the single small-N notice stands on §4's threshold suppressing a week
rather than a group, not on §4.1 item 5, whose line on this surface is
`comments_note`.

## 2026-09-08 — records the ticket falsified (commit bc72028)

ADR 0155 recorded the report's two API refusal sentences as outside `app.copy`
"because the report is not a governed surface yet". The report is one now, so
the reason expired while the state stands. Amended in place and in the index,
with the residue left open rather than closed: placing those two sentences
needs a row in the inventory's governance map, and that map is behind the test
wall. Three files repeated the same expired claim in their own words — the page
copy module's header, the page's error-state comment, the report API client —
and all three are corrected. The page's module docstring called its
comment-group choice §4.1 item 5's, which ADR 0158 settles otherwise.

ADR 0157's index row was missing entirely; the record shipped unindexed. Added
from the record's own decision.

## 2026-09-08 — the gates proved able to fail, from a snapshot

Committed first, then mutated, then restored by copying the snapshot back
rather than by `git checkout` (`docs/MISTAKES.md` entry 3, and the E0-33
incident about a checkout wiping uncommitted work). Four plants, each run and
each naming the expected test, tree confirmed identical to `HEAD` afterwards:

- `underperforming` in a report copy entry — criterion 2 —
  `test_no_shipped_string_calls_a_section_underperforming`. This is what says
  the item-4 sweep now reaches the report surface at all.
- a second `no names` sentence on the report —
  `test_each_line_carrying_surface_carries_exactly_one_confidentiality_line`.
- a reassurance added to the gradebook label —
  `test_no_surface_recorded_as_owing_no_confidentiality_line_carries_one`.
- a sentence in `ResponseRateBar.tsx` —
  `test_no_shipped_component_or_route_writes_a_user_visible_string_of_its_own`.

## 2026-09-08 — the run at the end

Whole Python suite under `-n 4`: 3373 passed, 5m25s. Both halves of the §4.1
gate by hand: 306 invariant tests ran with no skips and none failed, and 237
invariant-marked tests each assert something. `ruff format` (473 files
unchanged), `ruff check`, `mypy` (86 files). Frontend: typecheck clean, eslint
clean, vitest 111 passed and 1 failed — `PulseTrendChart.test.tsx` alone, on
the moved path, which is the orchestrator's.

## 2026-09-08 — addendum: the report API's two refusals (commits 7ee1dc3, 52a7717) — worked

The fork this ticket stopped on came back settled: a governance row for
`instructor_report` on the report surface, a two-key canary and a
router-agrees-with-registry rule, red at `c0c0837` — 3 assertion failures, no
others.

`backend/app/copy/instructor_report.py` with `instructor_report.section_
unavailable` and `instructor_report.week_unavailable`. Both texts were extracted
from `app/api/instructor.py` by a script that read the two assignments and wrote
them out, rather than retyped, so there is no window in which a hand-copied
character could differ. The router's `SECTION_UNAVAILABLE` and
`COURSE_WEEK_UNAVAILABLE` read the entries' `.text` and keep their names, which
is the gradebook shape and is what keeps every importer and every transcription
of the sentences green.

**The router's header does not, in fact, make the claim the deferred entry says
it makes.** The entry reads "held by review and by the module's own header
rather than by a sweep", but a grep for the claim over
`backend/app/api/instructor.py` finds nothing about the registry, the inventory
or review — the header argues why each refusal says so little, which is still
true and is untouched. So the new fact went where the constants are defined,
beside them. Nothing was falsified there; what was falsified was elsewhere, and
is corrected in 52a7717: ADR 0155's consequence and index row (amended earlier
in this branch to leave the residue open, closed now), ADR 0158's prefix counts
(the report surface is five prefixes, the inventory ten), and the three frontend
records that repeated the open-residue claim in their own words.

**The gates proved able to fail**, committed first, mutated, restored by copying
a snapshot back, tree confirmed identical to `HEAD`:

- the registry entry reworded while the router held its own copy of the sentence
  — `test_the_report_apis_refusal_constants_say_what_the_registry_says`. Note
  the near miss beside it: a router constant re-typed by hand and *equal* to the
  entry is green, which the test's own docstring names as what it cannot see.
  What it kills is the drift afterwards, and that is what this plant showed.
- `underperforming` in the week refusal — `test_no_shipped_string_calls_a_
  section_underperforming`. This is the deferred entry's third clause: the
  items-4-and-5 gate has now been seen running over these two strings.
- both keys moved to a prefix no surface claims — four reds, including
  `test_every_collected_key_belongs_to_a_governed_surface`.

Runs: the inventory module 60 passed; the eleven report-router test modules
(found by grepping for the fixtures and the router's own path names, not by the
constants — no test module imports those) 69 passed; whole Python suite under
`-n 4` 3375 passed in 5m18s; the §4.1 pass 306 with no skips and both gate
halves OK (237 marked tests each assert something); `ruff format` 474 unchanged,
`ruff check`, `mypy` 87 files; frontend typecheck, eslint and vitest all green —
15 files, 113 tests, the trend-chart repair having landed in `facdc6f`.

`docs/tickets/e4/deferred.md` is deliberately untouched: the closure note is the
orchestrator's.

## 2026-09-08 — security fix round (commits 4573de4, 800be85) — worked

Three settled items off a privacy-authz review, on top of the recognizer
redesign that landed test-side at `254f995`.

**MED-1, the credit note reworded.** The first wording said a refused comment
"costs that student one item of their participation credit" — a stated narrowing
of exactly the channel ADR 0153 analyses, since the reader has the week's
validity readout on one side and the per-week ledger on the other and the
sentence taught the arithmetic between them. The ruled replacement went in
verbatim, with apostrophes taken to the file's typographic house style and
nothing else touched. Its argument: participation credit is named as a count
separate from the validity rate, and an unanswered item and a comment that did
not count are said to leave an item incomplete alike, so a ledger deficit no
longer maps to a refused comment.

Constraints re-checked **against the collected string rather than against the
source**, because what the gates read is what the collector parsed: no
confidentiality marker (`unidentifiab` included, which is one since this round),
nothing from either item-4 vocabulary, no capital-V "Validity rate". Both vitest
fragments and the end-to-end clause are present, and a scan of every collected
string found the clause in this entry and nowhere else.

The three end-to-end locators, reasoned rather than run — Playwright is not
driveable from here:

- `getByText('Validity rate', { exact: true })` compares an element's whole
  text, and this note is a paragraph of three sentences, so it cannot match
  whatever its case. The lowercase reading is the belt beside that brace.
- `getByText('Response rate')` is a loose, case-insensitive substring match, and
  "response rate" does not occur in the note — checked, not assumed. Its single
  match is unchanged.
- `getByText(CREDIT_NOTE_CLAUSE)` matches the note and nothing else collected.

**ADR 0153 re-read.** The record's own "what would change the answer" clause
fired, so it gets the dated paragraph it asks for rather than a new record. The
conclusion recorded: the data channel pre-dates the note and is ADR 0125's
accepted disclosure, the reworded note explains the arithmetic while teaching
that a ledger deficit does not identify a refused comment, residual accepted on
that ground, suppression unchanged.

**ADR 0158, two corrections.** The small-N paragraph argued the shape the
decision had when it was written — the recognizer not knowing unidentifiability
language — which excused every sentence of that kind on every surface rather
than the one. It is a named exempt key now; the record says so, adds the
alternative it should have rejected at the time, and states that whether a
suppression notice is item 5's copy at all is raised to the owner with the
exemption as the interim. LOW-2: "the tally counts the screen" was too strong.
It is a tally by key prefix, and `WeekEyebrow` renders two `student_survey`
entries at the top of the report (a third, the closes label, is conditional on a
prop the report does not pass). The limit is stated: item 4's sweeps are global
and reach those strings, item 5's count follows the prefix, it costs nothing
today because neither carries an identity promise, and a crossing is visible in
the importing component's own import line. Both index rows follow the record.

Runs: the three copy modules 98 passed; whole unit suite under `-n 4` 1517
passed; `-m invariant` 306 passed with 3071 deselected; `ruff format` 474
unchanged, `ruff check`, `mypy` 87 files; frontend vitest 15 files and 113
tests, tsc and eslint clean.
