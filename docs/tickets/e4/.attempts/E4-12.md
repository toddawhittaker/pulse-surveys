# E4-12 — attempt log

## 2026-09-08 — tests first and red (commit 322a7e7)

The test-author's suite landed formatted and lint-clean. The verifier's
red-run: 7 failed, 3366 passed over unit+integration, every red a behavioral
assertion failure, the split matching the manifest exactly; the isolated
`-m invariant` pass collected 306 with no skips and `check_invariants.py`
named the same four invariant reds.

The symlink walk fix (`recurse_symlinks=True` on both walks in
`tests/fixtures/copy_inventory.py`) shipped inside the tests commit, so its
red-first proof was staged rather than committed red: with the two call-site
arguments reverted, `-k symlink` ran 2 failed
(`test_the_copy_file_walk_descends_a_symlinked_directory`,
`test_the_coverage_walk_descends_a_symlinked_directory`) and 1 passed
(`test_the_copy_file_walk_still_follows_a_symlinked_file`), exit 1; with the
fix restored, all three pass. The run output is preserved in the session
scratchpad (`e4-12-symlink-red-proof.txt`) and summarized here because the
scratchpad does not outlive the session.
