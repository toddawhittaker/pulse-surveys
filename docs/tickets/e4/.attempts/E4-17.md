# E4-17 attempts

## 1. First run of the committed suite — confirmed the reds and the standing greens

Ran the four Python modules the ticket's tests live in, with `PYTHONPATH` pinned
to the worktree's `backend` (verified `app.__file__` resolves inside the
worktree first) and `DATABASE_URL` rewritten to `localhost`.

Four FAILED, three passed, no errors:

- `test_the_answer_says_how_many_weeks_this_students_own_section_runs` — FAILED
- `test_each_open_section_is_answered_with_its_own_week_count` — FAILED
- `test_the_open_survey_declares_the_week_count_as_a_required_integer` — FAILED
- `test_the_course_week_eyebrow_states_how_long_the_course_runs` — FAILED
- the no-derivation sweep and its per-shape control — passed
- `test_the_strings_the_2026_09_03_rulings_add_are_governed_copy` — passed

Every red is an assertion failure rather than a setup error, which is what
`docs/MISTAKES.md` entry 44 asks of a tests-first suite. The two standing guards
are green, so nothing about the sweep needs reporting upward.

## 2. The wire half — `OpenSurvey.length_weeks` and the read

Worked. One field on `OpenSurvey` in `backend/app/schemas/student.py`, a plain
`int` beside `course_week`, described the way its neighbours are; a docstring
paragraph carrying the ticket's §4/§4.1 argument, in the place
`EnrolledSection.course_label` already carries its own. One read in
`_open_survey`: `length_weeks=section.length_weeks`, off the `Section` the
function already holds. No new query, no new join, no derivation.

The three backend tests went green together, and the org-views SQL sweep
(`test_the_org_views_are_read_only_through_the_grant.py`) stayed green over the
new `Field(description=...)` — `docs/MISTAKES.md` entry 43 is why that module
was run before anything else: the description names a section, and a comma in
the wrong place there reads to that sweep as a `FROM` list. `ruff format
--check`, `ruff check` and `mypy` all clean on both files.

## 3. The rendering half — the copy string, the prop, and the stale records

Worked. `student_survey.course_week_eyebrow` grew to
`COURSE WK {week} / {total},`; the term-week entry is unchanged, because the
ruling adds a total rather than removing an axis and the copy suite pins that
entry to its exact FIX-01 text. `WeekEyebrow` takes `lengthWeeks` as a required
prop and fills the new hole through `fillCopy`, padded by the same `padWeek` the
week beside it goes through. `OpenSurvey` in `frontend/src/api/student.ts` gained
`length_weeks` and `StudentWeeklySurvey` passes it down.

The component's docstring lost the paragraph saying the total is absent and now
says where the number comes from and why it cannot be computed here.

Two records elsewhere asserted the old string and were corrected in the same
change (`docs/MISTAKES.md` entry 1 — grep the fact, not the identifier): the
"what is deliberately not here" note in
`frontend/src/components/instructorReportTrendCopy.ts`, which quoted the eyebrow's
wording to keep it apart from the chart axis, and the `.pulse-eyebrow` comment in
`frontend/src/styles.css`, which quoted it as evidence that the uppercasing
changes nothing. `docs/tickets/e4/E4-08-trend-components.md` quotes the FIX-01
ruling too and was left alone: what it says about FIX-01 is still true, and the
pointer it hands the reader — README decision 9 — already carries the
supersession.

Gates after this commit: vitest 66 passed across 12 files, `tsc --noEmit` clean,
eslint clean, and the copy-inventory and no-derivation suites green.
