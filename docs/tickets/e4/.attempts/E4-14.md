# E4-14 attempts

## Attempt 1 — the grant and migration, 2026-09-06

Confirmed reds first: `tests/integration/test_the_launch_replay_purge_runs_as_pulse_app.py`
(1 FAILED, `escaped` named `InsufficientPrivilege`) and
`tests/integration/test_identity_grants.py` (2 FAILED — the negative control's
expiry half, and the equality test's `missing` list naming
`pulse_app should hold SELECT on public.lti_launch_nonce.expires_at` — 38
PASSED). Matched the manifest exactly.

Built:
- `backend/app/views_sql/lti_launch_nonce_grants_v002.sql` —
  `GRANT SELECT (expires_at) ON public.lti_launch_nonce TO pulse_app;` with a
  comment block in v001's house style, pointing at ADR 0150 and the carried
  entry.
- `backend/migrations/versions/20260906_688cbcf91b15_the_nonce_purge_gets_its_column_back.py`,
  `down_revision = "c4a8e51db9f3"` (current head at the time), executing
  `lti_launch_nonce_grants_v002` via the `read_sql`/`SCRIPTS`-less single-script
  pattern `e5b83c60f7a1` (the line-item column grant) uses — that migration was
  the closest template: a pure-privilege revision with a column-scoped
  `REVOKE` on downgrade, no table/column/index touched. Docstring carries the
  standing note that E4-02/E4-03 build off the same head in parallel and
  whoever merges last re-points the chain.

Result: all three reds green, `test_identity_grants.py` 40/40,
`test_the_launch_replay_purge_runs_as_pulse_app.py` 1/1 — 41 passed together.
`alembic heads` resolves to `688cbcf91b15 (head)` with no branch fork.

Worked. No dispute needed — the test files already stated the exact expected
grant shape (`RUNTIME_COLUMN_PRIVILEGES` entry, the column name, the negative
control's two queries), so there was nothing to contest.

Next: record sweep (v001 note, `tasks.py` docstring, ADR 0089 note, carried
entry closure), ADR 0150, then full verification (ruff, mypy, alembic check,
wider test selection) and the dev-database upgrade.
