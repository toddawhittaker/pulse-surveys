# E4-19 attempt record

## Attempt 1 — 2026-09-07

**Confirmed reds first.** Ran the four named tests before touching anything:

```
pytest tests/integration/test_the_trend_points_carry_the_term_week.py \
       tests/unit/test_the_payload_sketch_and_the_schema_are_reconciled.py \
       tests/integration/test_the_report_payload_carries_the_week_axis.py -v
```

Result: 4 failed, 9 passed. The four reds were exactly as the ticket and
breakdown decision 12 describe — `TrendPoint` had no `term_week`, so both the
integration tests (missing key on trend points) and the reconciliation test
(schema/sketch member-set mismatch) failed. No import errors, no `KeyError`s —
each red was a failed assertion, matching the tests' own "which failure a red
is" note.

**Change made** (the settled shape from the ticket, nothing widened):

1. `backend/app/schemas/report.py` — `TrendPoint` gains `term_week: int`,
   with a docstring addition explaining the source (the window row's term
   week) and why the client must not derive it (break-week sections).
2. `backend/app/services/reporting.py` — `_payload`'s trend builder now
   passes `term_week=other.term_week` alongside the existing
   `course_week=other.course_week`.
3. `docs/tickets/e4/README.md` — the payload sketch's trend entry gained
   `"term_week": 7` to match the schema, in the same commit.
4. `docs/adr/0157-the-trend-points-carry-both-week-axes.md` — written new;
   0157 was confirmed free via `ls docs/adr | tail`.

**Result — all green:**

- The three named test files: 13 passed, 0 failed (the four target reds now
  pass; the other nine, already green, stayed green).
- Full suite: `pytest tests/unit tests/integration -n 4 -q` — 3322 passed, 0
  failed, in 312s. The only warnings are the known Celery/Redis teardown
  noise (`RuntimeError: Retry limit exceeded ... Celery result store`),
  unrelated to this change.
- `ruff format --check backend/` — 70 files already formatted.
- `ruff check backend/` — all checks passed.
- `mypy` (bare, per the Makefile's `typecheck` target, config from
  `pyproject.toml` covering `backend/app` and `tests/evals`) — success, no
  issues found in 85 source files.
- No schema/migration change, so no `alembic check` needed.

**Commits:**

- `6086c47` — behavior: schema + service + README sketch, one commit
  (the reconciliation test holds sketch and schema to each other, so they
  cannot move in separate commits).
- `243a2d7` — ADR 0157, separate commit per the refactor/behavior-vs-record
  split.

**Nothing disputed.** All four tests were correct reds against the shipped
`_SectionWeek`/`_payload` shape; no test construction or assertion looked
wrong. No test file was touched.

**Nothing left unsettled** for this ticket's scope. Frontend consumption is
out of scope (E4-11), and no other payload member moved (verified by the
"course_week field still names the course week" and "no other member moves"
style tests, which stayed green throughout).
