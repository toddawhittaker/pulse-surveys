# E0-42 (e0/boundary-docs-sweep) — attempts

Documentation-only ticket, built in the `pulse-docs` worktree while E0-39, E0-40
and E0-41 build in three sibling worktrees. File ownership is in the ticket: only
`docs/**` and `CONTRIBUTING.md`, never `.env.example`, `docs/adr/0075-*`,
`docs/adr/0077-*`, `tests/`, `backend/`, `.github/` or the root configs. ADR
numbers 0078 and 0079 are this ticket's.

## Attempt 0 — 2026-08-22 — verification before writing anything

Checked every claim the ticket makes before correcting a record on its word.

- **PR numbers.** `git log --merges` gives #56 `e0/review-debt-e0-16` (Batch F,
  E0-30), #57 `e0/e0-exit-doors`, #61 `e0/e0-exit-e2e`, #62
  `e0/interactive-testing`, #64 `e0/review-debt-e0-15` (Batch E, E0-28), #65
  `e0/compose-credential-surface` (Batch G, E0-19), #66 `e0/small-corrections`
  (Batch H, E0-37). #62 is the developer console and carries no ticket, so the
  E0-18 row names it as riding alongside rather than as an E0-18 pull request.
- **Item 3.** `58064c4` rewrote SPEC §8's scope columns (five nullable foreign
  keys, no `scope_node_id`), which falsifies ADR 0025's context quote; `fd703bb`
  added SPEC §2.1's "An edge climbs" paragraph, which falsifies ADR 0044's "It
  says nothing about whether two assignments in the same role may report to one
  another" and its "nothing about where `ADMIN` sits".
- **Item 4.** The only broken relative link anywhere under `docs/` is ADR 0074's
  `0063-the-seed-refuses-outside-development.md` (real name
  `0063-the-demo-seed-runs-only-in-a-development-environment.md`), confirmed by a
  scan of every `[…](…)` target in every Markdown file under `docs/`. `CLAUDE.md`
  contains no rule about knobs; `docs/AGENTS_INTENT.md` line 89 does ("No
  configuration knob for something with one correct answer"), so that is where
  both citations repoint.
- **Item 6.** `docs/MISTAKES.md` has no entry 32 and no `docs/mistakes/32-*.md`.
- **Item 10.** Verified in the tree: `ActorScope.holds_care` beside `purview`
  (`backend/app/services/authz.py:301`), `section_roster` selecting
  `member.user_id AS user_id`, `reveal_identity(actor_person_id=…,
  subject_user_id=…)` checking only the actor, `GRANT SELECT` on
  `assignment_scope`, `lead_faculty_course` and `containment_path` to `pulse_app`,
  `transitive_purview` raising `NotImplementedError`, the only graph Hypothesis
  module being `tests/integration/test_supervision_graph_properties.py` (storage
  shapes, not purviews), `/healthz` returning `settings.environment`, and
  `landing.py:155` as the only "Nothing needs attention." string in the tree.
- **Item 11.** SPEC §2.1's table still carries a Student row (line 27) while ADR
  0028 decides a student holds no role assignment. Raised in the PR body only.
- **Batch I ticket files.** E0-39, E0-40 and E0-41's ticket files exist on their
  own branches, not on this one, so the new index rows name the files rather than
  linking them — a link would be broken at this branch's HEAD, which the
  acceptance criteria forbid.
