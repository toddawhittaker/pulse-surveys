# E0-36 — attempts

Ticket: `docs/tickets/e0/E0-36-ci-gate-fidelity.md`. Branch `e0/ci-gate-fidelity`,
cut from `epic/e0-foundations`. Tests committed at 54d34f0.

**Item 3 is held back on purpose** — the ticket names no mechanism and no rule for
it, and that question is with Todd. Nothing here implements or stubs it.

## Starting state (2026-08-19)

`.venv/bin/pytest tests/unit -q` → 3 failed, 329 passed. The three failures are
the aggregate-verdict sweep, the image-content check being absent from both
Docker gates, and the unconstrained dev `pip-compile`.

## Plan

1. Item 1 — treat `skipped` as a failure in the `ci` job's verdict.
2. Item 1 — amend ADR 0002's last consequence bullet, which this falsifies.
3. Item 4 — `scripts/ci/check_image_contents.sh`, wired into the `docker` job,
   `docker-build`, and both `bash -n` lists.
4. Item 5 — `-c requirements.txt` on the dev `pip-compile`.

Each attempt below is appended when it resolves, not at the end.

## Note for whoever picks this up cold

The hook that guards the test wall matches the string `tests/` anywhere in a
Bash command, not just in the path being written. Writing this file with a
heredoc that quoted the three failing test paths was blocked. Use the Write
tool for this file, or keep test paths out of the command line.
