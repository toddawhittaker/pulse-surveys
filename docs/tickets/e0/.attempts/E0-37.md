# E0-37 — attempts

Ticket: `docs/tickets/e0/E0-37-small-corrections.md`. Branch
`e0/small-corrections`, worktree `/home/todd/projects/pulse-h`. Tests committed
red at `b72eb2f`. My half is the code and records side: items 12, 1, 2, 9, 6, 8,
13, in that order. Items 3, 4, 5, 10, 11 are test-side and already done; item 7
is handled separately.

## 2026-08-21 — confirmed the reds before changing anything

```
.venv/bin/pytest tests/unit/test_db_engine_configuration.py \
  tests/unit/test_development_environment_has_one_definition.py \
  tests/unit/test_ai_provider_configuration.py -q
```

**12 failed / 47 passed** — the count the brief predicted. Nine in the engine
module (item 1's log capture, `hide_parameters`, and the logger pin, each over
three environment/log-level rows), two in the single-definition module (item 2),
and one `ai_provider` row, `…refused[no key set]` (item 12).

The control `test_the_log_capture_sees_a_bound_parameter_when_nothing_is_hiding_it`
is **green**, so the capture harness sees a parameter when nothing hides it and
the nine reds above are about the code rather than about the harness.

Conclusion: proceed with the four commits in the brief's order.

## 2026-08-21 — commit 1, item 12: off-machine means https (worked)

The early return on a missing key is gone, so the rule reads the URL and nothing
else. The validator is `an_off_machine_endpoint_is_encrypted` now and no longer
takes `info: ValidationInfo` — it had no other use for it, and `ValidationInfo`
came out of the pydantic import with it. Its docstring lost the "What this
deliberately does not refuse" paragraph and the ADR 0056 citation that argued for
the cluster case, and carries the decided rule instead. The refusal message leads
with the comment and calls the credential "any provider credential configured
with it", so it no longer reads as though a keyless configuration had nothing at
risk.

One thing the brief did not name, found by grepping the fact rather than the
identifier: the `ai_provider_base_url` **field description** (`config.py:310`)
also said "it must be https when that key is set". That string is the
operator-visible one — the startup report is built from the field name and this
description rather than from the validator's message — so leaving it would have
printed the old rule at the moment the new rule refused something. Corrected in
the same commit.

`README.md` and `.env.example` both tied the requirement to the key. In
`.env.example` the rule moved to `AI_PROVIDER_BASE_URL`, where it belongs now
that it does not read the key, and the paragraph under `AI_PROVIDER_API_KEY` says
so explicitly rather than being deleted — that is the paragraph an operator with
a keyless vLLM would have read.

Gates: the five configuration modules, 90 passed; whole unit suite 11 failed /
381 passed, the eleven being exactly items 1 and 2. `ruff format --check`,
`ruff check`, `mypy` clean. Nothing here grew past its description, so item 12
stays in the batch.

## 2026-08-21 — commit 2, items 1 and 2: the log, and one spelling (worked)

`app.db` gained `engine_options(settings) -> dict[str, Any]` (`echo` from the
unchanged `_echoes_sql`, `hide_parameters` outside development, `pool_pre_ping`)
and `pin_sqlalchemy_logging(settings) -> None` (the `WARNING` pin on the
`sqlalchemy` logger outside development, a no-op in development). The module
body calls the pin **before** `create_engine`, so anything the library logs while
building an engine is already covered, and the engine is built with
`**engine_options(_settings)`.

A third small function came with them: `_is_development(settings)`. Both new
rules and `_echoes_sql` ask the same question, and writing the comparison three
times in the file whose companion item is about one definition of that value
would have been the joke telling itself.

Item 2 in the same commit: the `DEVELOPMENT_ENVIRONMENT` assignments in
`app/db.py` and `scripts/seed.py` are gone and both import the constant from
`app.config` (`from app.config import DEVELOPMENT_ENVIRONMENT, Settings` and
`from app.config import DEVELOPMENT_ENVIRONMENT` respectively). `scripts/seed.py`
already imports `app.models.*` and `app.services.*`, so its import machinery
needed nothing; `app.config` builds no `Settings` at import, so nothing new is
read at seed startup.

Records this commit falsified, found by grepping the fact rather than the
identifier and amended here rather than in the last commit, since they are about
this change:

- **ADR 0013's `echo` consequence** claimed that deriving `echo` keeps survey
  answers out of the log when a deployment is turned up to DEBUG. That is the
  claim item 1 disproves. Split into two bullets: what deriving `echo` does do,
  and what actually holds §4 and §10.
- **ADR 0063** and **ADR 0074** both said the consolidation was proposed rather
  than done, and 0074's row in `docs/adr/README.md` said item 2 "migrates the
  other two" in the future tense. All three now say it happened.
- **`config.py`'s own comment** beside the constant said "E0-18 does not edit the
  other two: E0-37 item 2 is the ticket that migrates them". Same tense problem,
  in the file that holds the definition.

Gates: whole unit suite **392 passed, 0 failed** — every test the batch committed
red is green now. `tests/integration/test_demo_seed_script.py` 51 passed, which is
the suite that would notice a seed whose import machinery had broken.
`ruff format --check .` (154 files), `ruff check .`, `mypy` (38 files) clean.
`scripts/` is outside mypy's `files`, so the seed's edit has ruff behind it and
not mypy.

## 2026-08-21 — commit 3, item 9: two suffixes, measured in an image (worked)

Measured before and after rather than read off `.dockerignore`, which is what the
acceptance criterion asks for. `probe.pfx` and `probe.secret` planted in
`backend/app/ai/prompts/`, then
`docker build --file backend/Dockerfile --tag pulse-h-item9-probe .` and
`docker run --rm pulse-h-item9-probe python -c '…iterdir()…'` over
`/opt/venv/lib/python3.13/site-packages/app/ai/prompts`:

| listing | before the two lines | after |
|---|---|---|
| `README.md` | present | present |
| `validity.v1.md` | present | present |
| `probe.pfx` | **present** | absent |
| `probe.secret` | **present** | absent |

So the ticket's measurement reproduced exactly, on this tree, before anything was
changed. Both probes and the throwaway tag were removed afterwards; `git status`
and `ls backend/app/ai/prompts/` confirm nothing was left behind.

`.dockerignore` gained `backend/**/*.pfx` and `backend/**/*.secret` beside the
`.pem` and `.key` lines rather than at the end with the editor debris, because
they are secret-shaped and that is the block that block is for. One thing the
comment there had to say rather than inherit: the block's header says its
patterns "mirror .gitignore", and these two do not — `.gitignore` carries `*.pem`
and `*.key` and nothing for `.pfx` or `.secret`, so git tracks either happily.

`PLANTED_FILES` gained `e0-37-image-content-check.pfx` and
`…​.secret`, and three prose counts in the two files that had gone stale with them
— "one of six candidates", "deleting any of those six lines", and the failure
explanation that named only `.dockerignore`'s last four lines — now say eight and
name all eight patterns.

Then the check itself, twice, because a plant nothing looks at is not a gate:

- **As it stands:** `./scripts/ci/check_image_contents.sh` exits 0, reporting
  "none of the 8 planted files reached the image".
- **Mutated:** with `backend/**/*.pfx` deleted (and confirmed deleted before the
  run — `docs/MISTAKES.md` entry 20's rule), it exits 1 with "these planted files
  reached the runtime image: e0-37-image-content-check.pfx" and names nothing
  else. Restored from a snapshot copy rather than by `git checkout`.

`bash -n` and `shellcheck` clean on the script. The two unit modules that read it
— `test_the_image_content_check_guards_what_it_claims.py` and
`test_the_docker_gate_and_the_makefile_run_the_same_checks.py` — 5 passed. No
compose file and no `make` target was run, and no image is left in the store.

Not done, deliberately: the allowlist for that directory. The two lines wanted no
part of that argument, so item 9 stayed inside its description, and
`.dockerignore`'s closing note now says which two suffixes of the open question
this answered and that the question itself is ADR 0032's to reverse.
