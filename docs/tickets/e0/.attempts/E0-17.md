# E0-17 — attempts

Append-only. Newest entry at the bottom. Written as each attempt resolves, not
at the end, because this file is the only thing that survives a session ending
mid-ticket.

## 1. Reading, before any code (2026-08-17)

Read the ticket in full including "Two things this script can switch off without
meaning to", SPEC §2.1/§2.2/§8/§13/§6.3, ADR 0027, ADR 0038, ADR 0044, the epic
README's "What the built tickets settled", and both new test modules at
`1babbde`.

What the reading settled before writing anything:

- **The script cannot connect through `DATABASE_URL`'s role.** ADR 0009/0012:
  `DATABASE_URL` names `pulse_app`, which holds `CONNECT` plus `SELECT` on three
  views and nothing else. The address comes from `DATABASE_URL` and the identity
  from `DB_SUPERUSER`/`DB_SUPERUSER_PASSWORD`, exactly as
  `backend/migrations/env.py` does it.
- **The script must not import `app.db`.** That module builds an engine out of a
  whole `Settings()` at import time, against the application role.
- **Every supervision edge has to climb ADR 0044's role rank**, which E0-11 put
  in E0-09's trigger. `INSTRUCTOR 1 < LEAD_FACULTY 2 < CHAIR 3 < ASSISTANT_DEAN
  4 < DEAN 5 < VP_ACADEMICS 6`; `CARE` and `ADMIN` hold no rank and may carry no
  edge at either end.
- **`test_no_seeded_person_carries_a_routable_email_address` asserts the sweep
  is non-empty.** The only column in the schema whose name reads as an address
  is `user_identity.identity_email`, and a `user_identity` row needs a `user`
  row, which needs an `lti_platform` row. So the seed cannot write no
  registration at all — the question is only *which* platform it registers.

## 2. First build: the script, and the Makefile tolerance (2026-08-17) — worked

`scripts/seed.py` written whole, plus the `make seed` tolerance removed. The
choices, so a future session does not re-litigate them:

- **Triggers are not disabled.** Nothing in this loader sets
  `session_replication_role`. A few hundred rows in one transaction buys nothing
  worth an unpoliced supervision graph, and the module docstring says so where
  somebody about to add it will read it.
- **The registration is a fictional platform**, `https://lms.pulse-demo.invalid`,
  not `mock-lms`. ADR 0038 untouched.
- **`ENVIRONMENT` must equal `development`**, checked before the URL is even
  built, so a production run never opens a connection.
- **Idempotency is match-or-insert on natural keys.** One `upsert` helper;
  `person` is matched on `user_id`, which is why every demo person is linked to
  a `user`.

Result: `tests/unit/test_seed_target_is_enforcing.py` 2 passed,
`tests/integration/test_demo_seed_script.py` 21 passed, whole suite 256 unit +
373 integration passed, `ruff check`, `ruff format --check`, `mypy` clean.

## 3. Proving the tests actually exercise the seed (2026-08-17) — worked

21 tests passing in 3.6 s looked too fast to have run a container, a migration
and two subprocesses, so three mutations were applied and reverted rather than
believed (`docs/MISTAKES.md` entries 3, 12, 16 and 20). Each was checked to have
landed before the run.

| Mutation | Expected | Got |
|---|---|---|
| assistant dean leads `MIS 320` (inside a department they supervise) instead of `PSYC 110` | the "outside the departments" test fails | 1 failed, that one |
| chair of Psychology reports to the assistant dean instead of to the dean | the assistant-dean shape test fails | 2 failed, that one and the one above, both correctly |
| `upsert` never finds an existing row | the second run hits a constraint | 1 failed, the second-run test |

## 4. The environment guard has no test, and cannot get one here (2026-08-17)

`test_the_seed_registers_no_mock_platform_unless_a_deployed_run_is_refused` only
runs its `ENVIRONMENT=production` branch when the seed registers the mock, and
this seed does not — so the guard ships with nothing in the suite executing it,
which is `docs/MISTAKES.md` entry 9 exactly. Measured by hand instead, and the
measurement is recorded in ADR 0063 rather than left in a terminal:

```
$ ENVIRONMENT=production python scripts/seed.py   →  exit 2, refusal printed
$ ENVIRONMENT= python scripts/seed.py             →  exit 2, refusal printed
```

Both refuse before `seed_url()` is called, so no connection is opened. This is
named in the pull request as work for the test author: the implementer may not
write under `tests/`.

## 5. Finished, and what is left open (2026-08-17)

Two commits on `e0/seed-script`: `2715c41` (behaviour — `scripts/seed.py`, the
`make seed` tolerance) and `0daad69` (documentation — ADRs 0063 to 0066, ADR
0008's amendment, `.env.example`'s reader count, README's demo-institution
section, `docs/MISTAKES.md` entries 3, 1 and 13 bumped).

Gates, all run in this worktree, none of them piped into `tail` or `head`:

| Gate | Result |
|---|---|
| the whole pytest suite, unit and integration | 629 passed |
| `-m invariant` plus `scripts/ci/check_invariants.py` | 16 ran, none skipped, none failed |
| `ruff check .` | clean |
| `ruff format --check .` | 104 files already formatted |
| `mypy` | clean, and `mypy scripts/seed.py` separately, because the default `files` list is `backend/app` and the script is not otherwise typechecked |

`make ci` and `make docker-build` were **not** run: four checkouts on this
machine share the pinned Compose project name, and `make ci` takes the stack down
with its volume. The Docker gate is run serially by the orchestrator.

Open, and named in the pull request rather than left to be found:

1. **The `ENVIRONMENT` guard has no test** (attempt 4 above). Only the test author
   may write one.
2. **E0-18 needs an `lti_platform` row for the mock and does not get one here**
   (ADR 0065). Its launch path will be rejected until somebody decides where that
   row comes from and how it stays out of a deployment.
3. **`design/`'s 27 course numbers still fail SPEC §8's bands.** Raised, not
   reconciled, exactly as the ticket asks.
4. **`DEVELOPMENT_ENVIRONMENT` is spelled in two files.** Consolidating it into
   `app/config.py` crosses a module boundary; proposed, not done.
