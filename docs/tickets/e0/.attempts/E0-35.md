# E0-35 — attempts

## 2026-08-19 — documentation pass, after the code and tests landed

Entering cold on `e0/writer-and-marker-sweeps` at commit `30d32ec`. The three
sweeps and their mutation battery are already in the tree and green; the only
work left on this ticket is the records.

Read before touching anything: the ticket, the three new sweep modules'
docstrings, ADR 0014, ADR 0021, ADR 0045, ADR 0062, `docs/adr/README.md`'s format
section, E0-21, E0-24 and E0-27's `## Status` blocks, and the epic README.

Verified rather than assumed, because the ADR is about to state it as a
consequence:

- `grep -rn "guard_write" backend/app/` returns two hits, both in
  `backend/app/services/authz.py` — the `__all__` entry and the definition. No
  caller exists, so the sweep's subject set is genuinely empty in E0.
- `backend/app/services/authz.py:818` — `LMS_OWNED_TABLES` is
  `frozenset({"course", "section", "enrollment", "user"})`, and
  `guard_write(table=...)` raises unconditionally for any name in it. So a
  "sanctioned writer" cannot both write `course` and call the guard and proceed;
  the rule is satisfiable in E0 only because nothing writes.

Planned, in this order, one commit each:

1. ADR 0069 — the sweep-over-hook decision, plus its row in `docs/adr/README.md`.
2. ADR 0014's consequences — what rule 1 now enforces, and at what grain.
3. ADR 0021's consequences — the rule now has a check, and the check is syntactic.
4. The records the three sweeps make false: E0-21, E0-24 and E0-27's status
   blocks, the epic README, and ADR 0045's stale "E0-21 carries the residue".

Two test modules also carry a stale pointer and I may not write under `tests/`:
`tests/unit/test_lms_owned_column_marker.py` and
`tests/unit/test_lms_owned_writes_are_refused_at_the_chokepoint.py`. Listed for
routing in the report rather than edited.

### Outcome — all four worked, four commits

1. `c44ef7d` — ADR 0069 plus its index row. Recorded as Todd's settled decision
   of 2026-08-18, not as a choice being made now. Three limits in ADR 0062's
   register, the plain sentence that this does not close the seam, and the
   sanctioned-writer question with a "done when" and no invented mechanism.
2. `a06e332` — ADR 0014's consequences. The marker is documentation; the
   chokepoint-reads-the-marker reason is void; the decision stands on the other
   reason. Three costs stated, including that the closure reaches LMS-owned
   tables and nowhere else. The stale "E0-21 carries the residue" pointer went.
3. `2d00f88` — ADR 0021's consequences. The rule has a check, at module grain
   rather than the function grain the record names, and the check is syntactic.
   E0-35's "amend it to say unenforced" branch does not apply and is said not to.
4. `2113a53` — the false records: ADR 0045's two consequences, the `## Status`
   blocks of E0-21, E0-24 and E0-27, and the epic README in five places.

Verified afterwards: `321 passed` on `pytest tests/unit`, unchanged, and every
relative Markdown link added across the nine touched files resolves to a real
file (checked with a throwaway script, not by eye).

**Two blocks from the test-edit hook, both my own fault and both already-known
shapes**: a `grep` with `2>/dev/null` before a `tests/` path, and a `cat >> …
<<'EOF'` heredoc whose *body* named a sweep module. A documentation ticket on
this repo is the worst case for that hook, because every record being amended is
a record about the test suite. `docs/MISTAKES.md` entry 27 covers it; its
`Caught:` counter is not incremented here because that file carries Todd's
uncommitted work this session, and the increment is reported to him instead.
