# E4-20 — a representative demo story for the Monday report

Attempts, appended as each resolves. Branch `e4/demo-seed-story`; the two red
tests are committed at `40fe6d8`.

## 1. Confirmed the reds before writing anything — worked

`python -m pytest tests/integration/test_the_demo_story_seeder_refuses_a_
deployment_and_an_unlaunched_section.py -p no:randomly` gives
`2 failed`, both on `require_the_story_seeder()` with the sentence
"`scripts/seed_demo_story.py` does not exist". FAILED rather than ERROR, which
is what the manifest predicted, so the module's fixtures do their work and the
missing deliverable is what stops each test.

## 2. The fourth mock section, `BIOL-310-R7FF` — worked

`mock-lms/app/seed.py` only. A `MockContext` (`MOLECULAR_GENETICS`), a
`DEMO_STORY_CLASS_SIZE` of 20, and `the_demo_story_roster()` assembling the
shared instructor plus twenty students of the section's own. The placement comes
free: `seeded_platform` builds one placement per context, so adding the context
to that tuple — last, so `require_offers()[0]` stays the instructor's
`BIOL-215-R3WW` launch — adds the placement in the same order.

Assembled outside the `sections` loop rather than as a fourth row in it, because
that loop enrolls the shared learner in every section it walks and this is the
one section she must not be in.

Measured after the change: four contexts, four placements, 41 users, 46
enrollments; rosters of 12, 8, 5 and 21; the learner offered three placements
and the instructor four.

**One record the change made false, and what was done about it.** The shared
learner's launch-page label read "A student enrolled in every section", which
stops being true with a fourth section she is not in. It now reads "A student
enrolled in every section but the demo story's" — which keeps the old sentence
as a prefix, so `docs/adr/0066`'s citation of that phrase still finds it in the
file. The comment above her, the "three sections" sentences in the module
docstring, the roster-size list in `seeded_platform`, and the "all twenty-one"
count in the launch-cast comment were all corrected in the same commit.
