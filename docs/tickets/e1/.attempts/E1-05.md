# E1-05 — attempts

What was tried, and what happened. Appended as each attempt resolves, so a
session that ends mid-ticket leaves a usable record.

## 1. Confirm the reds and the controls (2026-08-25) — done

Ran the nine named controls first, on the tree at 09ffb61:

```
tests/unit/test_registration_address_constraints.py — the four at the end
tests/unit/test_the_process_wide_authorization_endpoint_is_gone.py — the three
tests/unit/test_mock_lms_service.py — the walk control and the standing sweep
```

12 passed, exit 0 (four of the node ids are parametrised, which is where the
extra three come from). No control is red, so the tests are not broken and the
work is the code's.

Then the ten modules the manifest names: 128 failed, 95 passed, 37 errors, exit
1. The 37 errors are the `two_registered_platforms` fixture boundary the
manifest predicts — `register_platform_row` calls `pytest.fail` naming
`lti_platform.authorization_endpoint` during setup — plus the door suites whose
fixtures now write the column.

## 2. Model, migration, chokepoint (db0eba6) — worked

Two nullable Text columns on `lti_platform`, a one-row `tool_signing_key`
mirroring `uq_institution_one_row`, and
`refuse_invalid_registration_addresses`. ADR 0077's four host helpers imported
from `app.config` by their private names rather than re-derived; the rename to
public names is a cross-module refactor and is proposed in the pull request
instead of done.

Migration autogenerated (34bf86adb774), documented, then upgrade / `alembic
check` / downgrade / upgrade — all exit 0, "No new upgrade operations
detected". `tests/unit/test_registration_address_constraints.py`: 118 passed,
exit 0, first run, no repair round.

## 3. The removal sweep cannot go green — dispute E1-05-01

`test_no_tracked_file_outside_the_records_names_the_process_wide_setting` still
fails with every production reference deleted. What remains is three lines of
prose in two **test** module docstrings, which spell the identifier in full and
sit outside the three history directories the sweep exempts. Nothing on the
implementation side of the wall removes them.

Measured rather than read: after deleting the `Settings` field, the
`.env.example` block, both readers and both docstrings, the failure names those
three lines and nothing else. Objection written to `docs/disputes/E1-05-01.md`;
stopped on that one assertion, continued with the rest. The other three tests in
that module — the two planted controls and the history canary — are green.

## 4. Seed: the endpoint, the chokepoint call, and the signing key — worked, with one blocked test

`seed_mock_platform` writes `MOCK_PLATFORM_AUTHORIZATION_ENDPOINT`
(`http://localhost:8080/oidc/authorize`, the browser horizon) and leaves
`auth_token_url` NULL for E1-06. `seed_people` gained `configuration` so the
fictional registration goes through the chokepoint too — a chokepoint one
writer walks past is not one. `seed_tool_signing_key` generates RSA 2048 in
PKCS#8 PEM, keeps an existing row, and checks the development guard at its own
write.

Verified against the real `python scripts/seed.py` rather than a harness: an
old-shape row planted as superuser (`authorization_endpoint` NULL) came back
with the column filled and **the same id**, `auth_token_url` still NULL, one
`tool_signing_key` row of 1704 characters. A second run kept the same key id
and left two platform rows; neither run's output contains `PRIVATE`. Reading
`tool_signing_key` as `pulse_app` is refused — "permission denied for table
tool_signing_key" — which is the missing grant working as designed.

Suite: 58 passed, 1 failed across `test_tool_signing_key_custody.py` and
`test_demo_seed_script.py`. The one failure is
`test_the_seed_fills_the_authorization_endpoint_on_a_registration_that_predates_it`,
which errors in `demo_databases()` before its first assertion: it holds
`mock_lms_config` open, which resolves `app` out of `mock-lms/`, and the
in-process alembic run then cannot import `app.models`. Confirmed with a probe
in isolation, not read off the traceback. Objection: `docs/disputes/E1-05-02.md`.

## 5. Gates and records (f9c7cdc, fda3ba9) — worked

`ci.yml`'s unconditional job gained the private-key walk control's node id and
the removal sweep's whole module. The whole module rather than node ids because
every test in it is either that sweep or one of its three controls, so the file
is the set that has to travel together. Nothing removed or weakened.
`test_every_repository_wide_sweep_runs_in_a_job_the_filter_cannot_switch_off`
was red before the edit and is green after; 33 passed across the three CI gate
modules.

ADRs 0081 (registration addresses) and 0082 (key custody), indexed. Four records
the change made false, found by grepping the fact rather than the identifier:
ADR 0075's status, ADR 0077's header sentence about "the two launch-door
fields", ADR 0079's description of a single launcher link, and
`models/lti.py`'s module docstring. The carried entry got its closure note, and
`deferred.md` got the production key-supply entry plus the answer to its own
item 2 — `tool_signing_key` holds no subject, so `PERSON_TABLES` is unchanged.

## 6. Final measurements (2026-08-25)

| Gate | Result |
|---|---|
| `pytest` (whole suite) | **2 failed, 1496 passed**, exit 1 — both failures are the two disputes |
| `pytest -m invariant` isolated | 102 passed, 1396 deselected, exit 0 |
| `scripts/ci/check_invariants.py` | exit 0 — "102 invariant test(s) ran, none skipped, none failed" |
| `ruff format --check .` | exit 0 |
| `ruff check .` | exit 0 |
| `mypy` | exit 0, 38 files |
| `mypy mock-lms/app` / `mypy mock-idp/app` | exit 0 each |
| `alembic check` | exit 0 — "No new upgrade operations detected" |

The invariant count is unchanged at 102: this ticket adds no §4.1 object.
`tool_signing_key` is granted nothing, so `RUNTIME_BASE_TABLE_PRIVILEGES`
contributes no entry, and it is not a person table.

Nothing else in the suite moved. The 2 failures are exactly the two named in
`docs/disputes/`; every other test the ticket touches is green.

## 7. Both disputes ruled in my favour; the rename approved (2026-08-25)

The arbitrator ruled the tests at fault on both objections (02fceae) and the
test author repaired them (bede805): the removal sweep stopped spelling the
setting in its own prose and `docs/disputes/` joined its records directories,
and the idempotence test reads the mock's two path constants out through a new
`mock_lms_paths` fixture, so the resolution closes before the in-process
migration runs. No assertion changed in either. I re-ran both before touching
anything else: 8 passed, exit 0.

## 8. Renaming `app.config`'s four host helpers (f376925) — worked

Approved off the report's proposal. `_url_host`, `_is_on_this_machine`,
`_is_a_loopback_host` and `_is_a_deployment` lost their leading underscore. The
underscore stopped being true when `app.models.lti` became a second caller, and
importing an underscored name across a module boundary said the opposite of what
the code does.

Rename only — no behaviour, no signatures. Verified by reading the diff line by
line rather than by trusting the substitution: every changed line in the two
modules is a rename or the one comment the rename falsified, which is the import
note in `models/lti.py` that called them `app.config`'s own vocabulary and
proposed this refactor. Checked for collisions first; none of the four public
names existed anywhere under `backend/` or `scripts/`.

Prose corrected where it named the helpers as live code: the cross-references
inside both modules, ADR 0081, and ADR 0077's E1-05 bullet. Both ADRs now record
the old spelling too, so a reader of either can still find the history.
`docs/tickets/e0/.attempts/E0-39.md` is left alone — an attempts file records
what was true in its own session.

| Gate | Exit |
|---|---|
| `.venv/bin/ruff format --check .` | 0 |
| `.venv/bin/ruff check .` | 0 |
| `.venv/bin/mypy` | 0, 38 files |
| the two suites that import the helpers | 0 — 213 passed |
| `pytest tests/unit` (after the follow-up below) | 0 — 757 passed |

**Two references I could not fix.** The OIDC provider configuration test module
names `_is_on_this_machine` (line 217) and `_url_host` (line 1442) in comment
and docstring prose. Both are stale as of f376925, and both are under `tests/`,
which is read-only for this role. Neither affects a result — they are prose and
that module is green — but a reader who greps either name now finds nothing.

## 9. A record the rename did not touch, found while grepping for one it did (c01c8a7)

`backend/app/config.py` listed "the LTI private key" three times as a credential
that would arrive as a `SecretStr` field — in the module docstring, at the
`SecretStr` group comment, and in the timezone validator's docstring. E1-05
settled it the other way: custody is a `tool_signing_key` row, so no field there
will ever hold it and `.env.example` gains no placeholder for it.

Falsified by commit 45d19ed rather than by the rename, and missed in round 5's
record sweep because I grepped the *identifier* — the deleted setting's name —
and this claim never spells one. Entry 1's rule is to grep the fact, and this is
the shape it means. Its own commit, prose only.
