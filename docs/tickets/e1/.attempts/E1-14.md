# E1-14 attempts

## Attempt 1 — 2026-08-26, implementer

**Starting state:** the test-author's round was already committed at `fcef1ff`
("pin the keep-verdict behaviour on `/healthz` and `/dev`"). Ran the pins first:
`PYTHONPATH=$PWD/backend .venv/bin/pytest tests/unit/test_healthz.py
tests/unit/test_dev_console_exposure.py` — 11 passed, as the manifest predicted
(green-now, since the verdict is "keep" and nothing in `backend/` needed to
change). Confirmed `app.__file__` resolved into this worktree, not the main
checkout.

**What I did:** documentation only, per the work order's "VERDICT SETTLED"
section (keep the `/healthz` `environment` field, leave `/dev`'s `POST → 405
Allow: GET` exactly as measured).

1. Wrote `docs/adr/0087-healthz-and-dev-keep-disclosing-the-environment.md`
   (four sections, 95 lines). Context names the guard list (ADR 0074 for
   `/docs` and SQL echo, ADR 0079 for `/dev`, ADR 0078 for the cookie's
   `Secure`, ADR 0063 for the demo seed) and the `/dev` method-mismatch measured
   in ADR 0079. Decision keeps both as measured, reasoning: the name is not a
   secret, the only consumer is an orchestrator health check, the value is
   `production` in production, and every guard the name points at still has to
   be reached and passed on its own regardless of whether a caller already
   knows the environment. Alternatives rejected: drop the field (real
   operational cost — orchestrator consumer, `README.md:51` — against a
   disclosure that opens no guard) and gate behind authentication (no session
   model exists yet for these scaffold-era routes, same reasoning ADR 0074 and
   ADR 0079 each give for rejecting an authenticated gate). Consequences: the
   disclosure is now pinned in both directions by the three new/joined tests,
   nothing in `backend/` changes, the orchestrator and README keep working.

2. Amended ADR 0079 with a dated note ("Amended 2026-08-26 by E1-14") right
   after the existing 2026-08-25/E1-05 amendment note, in the same style —
   records that this ticket settles the decision-section and consequences-
   section notes about the unasserted `405`, points at ADR 0087, and says
   explicitly the gate itself (0079's actual decision) is unchanged. Did not
   touch 0079's Decision or Alternatives sections otherwise.

3. Updated `docs/adr/README.md`: added the 0087 index row, and appended one
   sentence to the 0079 row's existing amendment prose noting the 2026-08-26
   amendment and linking to 0087.

**Verification:**
- `git status --porcelain` / `git diff --stat` after the changes: only
  `docs/adr/0079-*.md` (modified), `docs/adr/README.md` (modified), and
  `docs/adr/0087-*.md` (new). **Nothing under `backend/` changed** — confirmed,
  as the work order predicted under "keep".
- Re-ran the pins after the doc changes: 11 passed, unchanged.
- No `.py` file touched, so `ruff format --check` / `ruff check` / `mypy` have
  nothing new to check; not run against files outside this diff.
- Did not add an entry to `docs/tickets/e1/deferred.md`. The ticket's own
  "Out of scope" section (authentication for observability surfaces generally;
  any other `/healthz` field) already states these boundaries as pre-existing
  scope decisions, not something this ticket's work newly discovered and chose
  to leave — there is nothing new to defer.

**Result:** done. No dispute raised — the test-author's manifest already
confirmed "keep + record" reads as defensible from the ADRs and the carried
entry directly, and nothing in writing the ADR changed that reading.
