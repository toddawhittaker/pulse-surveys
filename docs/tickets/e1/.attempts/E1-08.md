# E1-08 — the launch door on pylti1p3 — attempts

Heavy lane, ⚠ line-by-line. Turning the committed red suite green:
`tests/unit/test_session_module.py`, `tests/integration/test_lti_launch_door.py`,
`tests/fixtures/lti_services.py`.

## Attempt 1 — dependency pin (build order step 1) — DONE

Pinned `pylti1p3==2.0.0` and `jwcrypto==1.5.9` in `pyproject.toml`, ran
`make lock`. Confirmed the closure gained ONLY those two (`requirements.txt`
diff shows exactly `jwcrypto` and `pylti1p3` added — `requests`/`pyjwt`/
`cryptography`/`typing-extensions` were already present, matching the
dependency note). License gate (`scripts/ci/check_licenses.py`) run locally
with jwcrypto in the closure: it lands in the "review — weak copyleft" bucket
(LGPL-3.0-or-later) beside psycopg, prints but does not fail, exit 0. CI does
not pass `--strict-unknown` (grep of `ci.yml`), so the gate stays green.
Result: worked.

## Attempt 2 — session module + config.session_secret (build order step 2) — DONE

Wrote the shared session module to the settled interface (SessionClaims,
issue_session/verified_session, issue_csrf_token/verify_csrf_token, cookie
helpers, session_from_request, fragment_redirect). HS256, algorithm named on
encode and decode. door/role stored by enum name. CSRF = nonce.HMAC(secret,
jti:nonce), jti-bound, constant-time verify. session_from_request: Bearer
present is answered alone (no cookie downgrade); else cookie. fragment_redirect
gives a 302 to /app/<role>#session=<token>.

Added session_secret: SecretStr (required, no default) to app/config.py and a
SESSION_SECRET placeholder to .env.example (and local env).

Result: the session unit module is 13/13 green; the env-example sync module and
the config-settings module stay green.

## Note — the replay-guard grant collides with a read-only test

The replay guard needs pulse_app to INSERT (and, for the purge, DELETE) into
lti_launch_nonce on the launch's own connection. Every successful launch INSERTs
the nonce, so the grant is load-bearing for the positive controls, not only the
replay test. But RUNTIME_BASE_TABLE_PRIVILEGES — the exact allowlist the runtime
grants are compared against — is defined inside a hook-protected test and has no
lti_launch_nonce entry. Shipping the grant turns that test red and there is no
way to extend the allowlist without editing the test. Filed as a dispute
(E1-08-02); the grant ships because the ticket cannot validate a launch without
it.
