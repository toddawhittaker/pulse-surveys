# E1-10 — attempts

Worktree `/home/todd/projects/pulse-surveys-e1-10`, branch
`e1/launch-provisioning-guard-write`, tests committed red at 33abb6f.

## 2026-08-26 — the reds, confirmed before anything was written

`.venv/bin/pytest tests/unit -q` → **14 failed, 795 passed**. Twelve in the new
sanction module (missing `SANCTIONED_WRITERS`, `sanction_for`, `WriteSanction`),
one in the guard sweep (no `provisioning.py`), one in the column-marker sweep
(the recorded `course.title_is_fallback` names no column yet).

The five integration modules the red commit names → **45 failed, 76 passed**.
Total 59, which is exactly what the manifest predicted, and nothing outside those
nine modules was red.

## Attempt 1 — the sanction mechanism. Worked.

`guard_write` gains `sanction: WriteSanction | None = None`; `SANCTIONED_WRITERS`
is the catalog and `_catalog_grants` reads *it* rather than the sanction handed
in. `sanction_for` raises `UnknownSanctionedWriterError`, which is deliberately a
`LookupError` and not an `AuthzError`: an unknown writer name is a bug in the
calling module, and inside that family somebody's `except AuthzError` would report
it to a person as "you may not do that".

All 34 tests in the two chokepoint modules green on the first run.

## Attempt 2 — the schema. Worked, with one decision made by measurement.

`course.title_is_fallback`, `section.lms_context_memberships_url`,
`launch_defect` + `launch_defect_kind`, one revision off `c4f81a2e6b39`, grants in
`launch_provisioning_grants_v001.sql` executed by the revision.

**The enum was nearly wrong.** `test_the_kind_column_refuses_a_defect_kind_this_ticket_does_not_name`
requires the *database* to refuse an unknown kind with a `DatabaseError`, and the
grant test writes a kind as a literal string through `text()`. Two hazards:
SQLAlchemy's `Enum` raises `LookupError` (not a `DatabaseError`) for an unknown
value when `validate_strings` is on, and a `str` bound to an enum column fails if
the driver sends it typed. Both measured rather than assumed: `validate_strings`
defaults to `False`, so an unknown string travels to Postgres and comes back as
`DataError`; and psycopg 3 registers `StrDumperUnknown` last, so a `str` is sent
with the *unknown* oid and Postgres coerces it. `values_callable` keeps the
database labels equal to the member values, which is what the hand-written
`INSERT` in the grant test writes.

## Attempt 3 — the writer. Worked on the second shape.

First shape: look the `user` row up with `select(User)`, then insert if absent.
It made every provisioning test pass and turned
`tests/unit/test_no_service_reads_an_identity_table_directly.py` red — an
`invariant`-marked sweep that forbids any module under `services/` from turning
`User`, `Person` or `UserIdentity` into rows. **That collision is real and the
sweep is right**: `user` leads to identity and the launch writer has no business
reading it.

Second shape, which is also the better design: do not read it at all. The row is
insert-if-absent and never updated, so `UNIQUE (lti_platform_id, lms_user_id)`
answers the only question there is — the same reasoning `claim_nonce` gives for
spending a nonce with no `SELECT`. `session.add(User(...))` inside a savepoint
that tolerates the duplicate key. Green, and no sweep is dodged: the E0-35 sweep
still sees the write (`add` is one of its write calls and the class is spelled
inside it).

Also settled here, and worth writing down because both are load-bearing:

- **The writes are spelled where a syntactic sweep can see them.** E0-35's
  detector reads the *class name inside the write call*, so `session.add(course)`
  is invisible to it and `session.add(Course(...))` is not. The section write is
  `session.add(apply_section_code(session, Section(...)))` for that reason, and
  the address rides in the constructor rather than being assigned afterwards.
- **Course and section are written inside one savepoint.** `apply_section_code`'s
  refusal is the only defect that cannot be known before a write, and criterion 5
  is atomic across the two tables.

## Attempt 4 — the collateral red I could not fix. Dispute filed.

Full suite: `pytest tests/unit tests/integration -q` → **2 failed, 1681 passed**,
and both failures are in
`tests/integration/test_the_upgrade_refuses_a_stored_edge_that_does_not_climb.py`,
which is E0-11's and has nothing to do with this ticket. It seeds through
`Base.metadata` into a database standing at revision `9a71c4be0d3f`, and
`seed_row` ends `.returning(*table.columns)` — so **every** declared column is
named in the `RETURNING`, and `course.title_is_fallback` does not exist at that
revision.

Measured rather than argued: the same two tests are green at the branch point
(33abb6f) in a detached worktree with `PYTHONPATH` pinned to it — `2 passed`. No
definition of the column avoids it, because the failure is in a `RETURNING` list
that names columns whatever their defaults are. `docs/disputes/E1-10-01.md` is
the objection; the suggested repair is to seed at head and downgrade, which also
removes the hidden requirement for every later ticket that adds a column.

## Attempt 5 — the fix round's new module. Worked.

`tests/integration/test_provisioning_consults_the_catalog_at_every_write.py`
arrived red, 4 of 4, and every one of them failed on the *same* assertion: the
writes stopped when the catalog stopped granting them — each site does consult the
guard — but `LmsOwnedWriteRefused` escaped `provision_from_launch` and failed the
launch request. Today's catalog cannot trigger it; a future disagreement between
this module and the catalog would lock people out, which is the failure direction
the ticket's rule forbids, on the one path where the guard is working.

The fix is three catches and one log helper, and where each piece goes is the
whole of the design:

- **Logged at the write site**, because that is the frame that knows which table
  it asked about. `"course or section"` was my first message and it was wrong —
  the ruling asks for *the* table.
- **Undone at the savepoint**, because that is the only frame holding anything to
  undo. `_ingest_the_context`'s `both_or_neither` already exists for the atomicity
  a defect needs, and a refusal on the `section` write after the course has been
  written is exactly the partial row it exists to prevent. So the site logs and
  re-raises; the boundary rolls back and returns.
- **No boundary for `user`**, because the chokepoint is asked before the row is
  built, so there is nothing partial. It catches and returns, and the context
  ingestion still runs: a grant lost on `user` says nothing about `course`.

No `launch_defect` row and no sixth kind — the closed set is pinned by
`test_the_defect_record_carries_exactly_the_fields_this_ticket_enumerates` and by
`test_the_kind_column_refuses_a_defect_kind_this_ticket_does_not_name`, and a
deployment's own bug does not belong on the surface E11 builds for data quality.
The error line is the visibility (`docs/MISTAKES.md` entry 26).

Same round, one precision fix the battery flagged: the staff-match docstring said
"exact string match, never a substring", and the two role URIs are not substrings
of each other. What a whole-value comparison defends against is
`any("Instructor" in role for role in roles)` — the natural implementation, which
the TeachingAssistant sub-role satisfies because it spells its parent role in its
own path. The docstring and ADR 0091 say that now, and ADR 0090 gains the
consequence that a writer decides what a catalog disagreement costs it.

## Gates

Re-run after the fix round: the new module **4 passed**; the ten modules together
**170 passed, EXIT=0**; `pytest tests/unit tests/integration` **1687 passed,
EXIT=0** (the two migration tests the dispute was about are repaired and green);
`pytest -m invariant` **102 passed, 1585 deselected**.

`ruff format --check .` and `ruff check .` clean over the whole tree; `mypy` and
`mypy mock-lms/app` clean; `alembic check` clean, run against a throwaway database
created beside the dev one and dropped afterwards, **so the shared dev database
was never migrated** — another worktree's session would otherwise find it stamped
at a revision its tree does not hold. The downgrade/upgrade round trip was run on
the same throwaway database and `alembic check` was clean after it.

`make ci` and the Docker gates were not run: they are the orchestrator's, serially.

## Attempt 6 — round 3, the security review's three findings

Reds confirmed first: **13**, across the two new modules and the four edited ones,
with `tests/unit` green at 811 — every red is behavioural or schema-shaped, none
is a control.

**The HIGH — the binding.** The rule that satisfies all four of its tests is
symmetric and is worth stating as one sentence rather than as three branches:
*the section the binding finds and the section the parsed label names have to be
the same section.* Both absent is a fresh discovery; both present and the same row
is the ordinary path; anything else is a collision. That covers the copied course
(binding finds nothing, label finds somebody else's row) and the renamed context
(binding finds this context's row, label finds nothing) with one comparison, and I
would have written two special cases if I had implemented the ruling's three
bullets literally.

The check has to sit **above** the savepoint, not inside it, because the ruling
says the course title is not written either — and `_upsert_course` writes the
title before `_upsert_section` is ever called. So the lookups are read-only and
happen before `session.begin_nested()`.

**The MEDIUM — one rule set, not two.** `refuse_invalid_registration_addresses`
takes three named columns and could not judge a fourth address, so its per-address
loop body is extracted into `_refuse_an_unacceptable_address` and both callers
reach it. `ROSTER_SERVICE_ADDRESS_COLUMN` joins `FETCHED_COLUMNS`, which is what
makes rule 4 — link-local, the cloud metadata service — cover the roster address:
the rules key on the column name, and that tuple is what "this container fetches
it" is spelled as. The refusal's message still says "the registration's", which is
now slightly wrong for this caller; I left the wording alone (its unit tests
assert on it) and the log line beside it says which address was refused.

The environment those rules gate on is read with `os.environ.get(…, "")` per call.
`is_a_deployment("")` is **true**, so an unset variable puts the rules in force
rather than switching them off — the fail-closed direction, and the spelling
`scripts/seed.py` already uses.

**The LOW — a second grants file, not an edit.** `launch_provisioning_grants_v001.sql`
is applied on every reviewer's database and ADR 0041 makes it immutable once the
branch is pushed, so `_v002.sql` carries the narrowing *and* the corrected
justification, quoting the false sentence it replaces. The revoke has to come
first: a column grant and a table grant are separate ACL entries, so granting
`SELECT (id)` beside a table-wide `SELECT` would change nothing.

**Two things measured rather than assumed**, both on a throwaway testcontainers
Postgres (the deployed image, both runtime roles) because the dev database was
down by the time I got there and starting Compose from this worktree is not mine
to do:

- **The offline migration test caught a real defect.** My first backfill read
  counts with `op.get_bind()`, and in `alembic upgrade --sql` there is no
  connection — `test_migration_comparison_settings.py[offline]` went red with
  `'NoneType' object has no attribute 'scalar_one'`. `9a71c4be0d3f` skips its
  validation offline and emits a comment, which it can because it only *checks*;
  this one changes data before a `NOT NULL`, so a script that skipped it would not
  apply. It is one PL/pgSQL `DO` block now, which both modes carry.
- **Both branches of that block, driven.** With a stored section and no
  registration the upgrade refuses and names the situation (returncode 1); with
  one registration it binds the row to it as
  `pre-binding-section-<uuid>`; `alembic check` is clean before and after; the
  downgrade/upgrade round trip works and the enum comes back with exactly the
  seven labels in order, which is what `ADD VALUE IF NOT EXISTS` is for — Postgres
  cannot remove a label, so the downgrade leaves them.

**The seed moved a function.** Sections are bound to the *demo* platform's
deployment — the fictional platform the demo people belong to — under a synthetic
`pulse-demo-context-…` id. That registration used to be written half-way down
`seed_people`, which runs *after* `seed_sections`, so it is `seed_demo_platform`
now. Binding them to the mock platform instead would have said these eighteen
sections came from the mock LMS, which is not true of any of them.

## Gates, round 3

The two new modules **11 passed**; the four edited modules plus the round-2 module
**88 passed**; `pytest tests/unit tests/integration` **1702 passed, EXIT=0**;
`pytest -m invariant` **102 passed, 1600 deselected, EXIT=0**; `ruff format
--check .` and `ruff check .` clean; `mypy` and `mypy mock-lms/app` clean;
`alembic check` clean on a throwaway container database, before and after the
backfill.

## Attempt 7 — the round-3 re-pass's four findings, closed as records

No build round: nothing was red and nothing was HIGH. One message string and three
records, and the one worth remembering is the first, because it was **my
over-claim** rather than a gap in the code. ADR 0091 said a collision writes
nothing "the course title included", and that is true only of the launch that
collides: a launch whose section identity *agrees* takes the ordinary path and
writes the title of the course its label names — which may be a course several
contexts share, because `course` is one row per `(prefix_id, lms_number)` however
many sections hang off it. The code is right and §2.1 is why (the LMS owns the
title); the sentence claimed more than the code provides, and now says which half
holds and that the rest is accepted.

The other three are consequences the design has and the record did not carry:
first-writer-wins on `(course, term, lms_section_code)`, with a copy launched
first denying the genuine context until an operator repairs it by hand — loud,
unbounded in time, reconciliation deferred; and the concurrent first launch, whose
loser has its `IntegrityError` tolerated as already-there and so records its
`context_collision` on its *next* launch rather than its first. Both are in ADR
0091's consequences, the reconciliation and the `os.environ`-versus-`Settings`
read are `deferred.md` entries 4 and 5 with done-whens.

The migration's refusal message loses "or drop them (the demo seed re-runs with
`make seed`)". Its reader is somebody whose upgrade is failing under time
pressure, and a message that sanctions deleting sections is a message that deletes
responses. It points at registering the platform now; the logic is untouched, and
both branches were re-driven on the throwaway container to say so.

## Gates, round 4

`tests/integration/test_migration_comparison_settings.py`,
`test_alembic_baseline.py` and
`test_the_upgrade_refuses_a_stored_edge_that_does_not_climb.py` together **7
passed**; `pytest tests/unit` **811 passed**; `ruff format` and `ruff check` clean
over the changed files; the container probe still refuses with no registration,
still backfills with one, and `alembic check` is still clean either side of it.
