# E5-10 — the report page joins the benchmark payload

## Attempt 1 — 2026-09-14, light lane

Branch `e5/report-benchmark-join`, cut from the epic head `b166ca4`.

### What was built

**The reconciliation landed in the component types, not in a mapper on the
page** (ADR 0171). `OverlaySeries`, `OverlayPoint` and the workload column types
are respelled as the wire spells them — `course_week`, a `mean` that is a whole
`{suppressed, reason, figure}`, and a workload column that is two independently
sealed figures — so the page passes `streams.<stream>.benchmark` and
`workload_benchmark` straight through. The wire types are written once in
`frontend/src/api/instructor.ts`; the components keep structurally identical
copies of their own, because `PulseTrendChart.test.tsx` refuses an import from
`../api` in the trend family and this ticket does not widen that guard.

**Suppression is read per week and per cell.** A trend week is drawable only if
its own seal says exactly `false` beside a real number; a series with no drawable
week takes E5-07's notice, and a series with some draws those and leaves a gap
at each withheld week. A workload cell is sealed on its own, which makes a column
that reports its median and withholds its mean expressible for the first time.

**The deferred panel crash is closed.** A member arriving without its `points`
array now yields no drawable week rather than `undefined` into the week-axis
spread. The deferred entry has a dated resolution paragraph rather than a
deletion.

### The divergence list (E4-11's precedent, criterion 3)

Four divergences between E5's payload sketch and the shipped schema, every one
resolved by moving the frontend to the schema:

1. a series-level `suppressed`/`reason` — does not exist on the wire;
2. a suppressed series carries `points: []` — on the wire every published week is
   a point and each point says whether it is suppressed;
3. a point's `mean` is a bare number — on the wire it is a whole sealed figure,
   and the number is at `mean.figure`;
4. a workload column is one flag over a bare `mean` and `median` — on the wire
   the two are sealed independently.

Swept by `scratchpad/sweep-e5-10.py`, three patterns each proven against a line
that certainly carries the divergence and a line that certainly does not before
the tree is read. Over `frontend/src` at the epic head it reports **46 hits in
three files**; over this branch, **0 hits, 71 files read**.

### Gates, foreground, exit status checked, never piped to a pager

| Gate | Command | Result |
|---|---|---|
| Frontend unit suite | `npm run test --workspace frontend` | 25 files, **261 tests passed**, exit 0 |
| Frontend types | `npm run typecheck --workspace frontend` | exit 0 |
| Frontend lint | `npm run lint --workspace frontend` | exit 0 |
| Root types (tests/e2e included) | `npx tsc --noEmit -p tsconfig.json` | exit 0 |
| Root lint | `npx eslint . --max-warnings=0` | exit 0 |
| Playwright collection | `npx playwright test --list` | 82 tests in 23 files, both new cases collected, exit 0 |
| Backend unit suite | `pytest tests/unit -q -p no:randomly` | **1542 passed**, exit 0 |
| Backend integration suite | `pytest tests/integration -q -n 4` | **2219 passed** in 6m23s, exit 0 |
| Formatting | `ruff format --check backend tests scripts` | 522 files already formatted, exit 0 |
| Lint | `ruff check backend tests scripts` | All checks passed, exit 0 |
| Types | `mypy backend` | no issues in 128 source files, exit 0 |
| Schema | `alembic check` | **exit 255 — "Target database is not up to date"**. Not this diff: no migration is touched, and the shared development database sits at `e7a2d5c94b31` while the head is `f1c86b40d2e7`. Recorded rather than repaired, because migrating a shared stack is not a light-lane build's call. |

### Verified by mutation rather than by reading

Each was applied, the suite run, and the tree restored:

| Mutation | Reds |
|---|---|
| the points-array guard removed from the overlay reader | 4 |
| `sentWeeks` returning its argument uncast | 18 |
| a fully withheld series never emptied | 10 |
| the figure half of the trend seal dropped | 1 |
| the workload flag read as `!== true` | 1 |
| the median cell fed the mean's figure | 7 |
| the withheld note chosen without reading the flag | 5 |
| both trend panels handed the instructor stream's benchmark | 1 |
| the workload benchmark not passed to the pair | 3 |

### What did not run, and why

**The two new end-to-end cases have never been executed**, and neither has the
rewritten assertion in `instructor-report.spec.ts`. They are written, they
typecheck, they lint and Playwright collects them; nothing has driven them
against a stack. Two things about the development stack stand in the way, and
both are decisions about shared infrastructure rather than about this diff:

  - **the running `mock-lms` image predates E5-12.** It has no
    `PRIOR_TERM_SECTIONS` and its launch page offers no `BIOL-310-U5FF`,
    `BIOL-310-U6WW`, `BIOL-310-R5FF` or `BIOL-215-E5WW`, so the prior term
    cannot be launched at all. The database agrees: it holds one term, `Fall
    2026`, and no `Spring 2026` row for `scripts/seed_benchmark_history.py` to
    find;
  - **the running `api` image serves a frontend built without this change**, so
    a drive against it would be a drive against the page E4 shipped.

Both are cleared by a rebuild of the stack's images from this worktree followed
by `alembic upgrade head` and `python scripts/seed.py`, which is the Docker gate
the orchestrator runs serially. This attempt did not run it: the brief reserves
it, and the database is shared with a build running in parallel.

## Attempt 2 — 2026-09-14, the end-to-end drive

The stack was rebuilt from this worktree and its database recreated, migrated to
`f1c86b40d2e7` and seeded, so the specs attempt 1 could only collect were driven.
Two things came back, one of them this branch's.

### The report publishes four accessible tables, not two

`instructor-report.spec.ts` counted the tables inside the report and expected
two; `exit-instructor-report.spec.ts` did the same one project later. Both were
right while each panel drew one line. E5-07 gives every **drawn** comparison
series a table of its own, and with the benchmark members wired this section's
report publishes four: the two panels' own, and one per panel for the series the
server did not withhold.

Neither count was raised to four. `tests/e2e/support/reportTables.ts` answers the
question once for both specs — the two panel tables required **by name**, and the
overlay tables required to be exactly one per drawn overlay line, which is the
property E5-07 built and the one a literal four would stop holding the moment a
world's suppression changed. The line count is the server's decision read off the
page and the table count is the accessible rendering of the same decision, so
neither is read from the other's source.

The panel captions are matched with `exact`. Without it "Weekly ratings:
Instructor" also names that panel's overlay table — a role's accessible name
matches as a substring — and the locator resolved to two, which is this file's
own `getByText` trap one locator along. The overlay half was verified by
mutation: pinning the university table count to zero turns the case red, so the
world it runs in does draw a series for it to be about.

The exit drive's next assertion needed the same correction for a different
reason. It compared the first two tables the report publishes to prove the two
panels are drawn from two streams; with the overlays on the page those two are
the instructor panel's own table and the instructor panel's overlay table, which
are two series of one stream and differ whatever the panels draw. It reads each
panel's own table by name now, and requires the instructor panel's to be
non-empty before comparing them.

### Why the three-lines case is still red at this commit

Not this branch's, and not the page's. `app.services.benchmarks.resolve_default_set`
reaches a section's comparison set through `lead_faculty_course` — the hero
course's lead, that lead's courses, then the ones matching on length and level —
and `scripts/seed.py` maps leads to BIOL 101, MATH 040/210/505, STAT 250, CSCI
240 and PSYC 110 and never to BIOL 310. So `BIOL-310-R7FF`'s default set is empty
by the service's own rule while `resolve_university` answers nine sections, and
the spec's canary reports exactly that: no drawable comparison week in the
payload. E5-12's seeder recount counted by section code and never asked the
service, which is why its verdict said the cohort was fine.

A lead for BIOL 310 lands on the epic in a separate change before this branch
merges, and this spec is re-driven then. Nothing was added here for it: the
canary is doing its job, which is refusing to make a claim about the page over a
payload that carries no figure.

### The full suite, driven from this worktree

Run in the projects' own order. `--no-deps` was needed for the two exit projects
because Playwright skips a project whose dependency failed.

| Project | Command | Result |
|---|---|---|
| chromium | `npx playwright test --project=chromium` | **55 passed, 4 failed**, exit 1 |
| grade-passback-exit | `npx playwright test --project=grade-passback-exit --no-deps` | 1 passed, **1 failed**, 5 not run, exit 1 |
| instructor-report-exit | `npx playwright test --project=instructor-report-exit --no-deps` | **8 passed**, exit 0 |
| chromium, re-run afterwards | `npx playwright test --project=chromium` | 43 passed, 6 failed, exit 1 |

Every one of this ticket's own surfaces is green: all six cases of
`instructor-report.spec.ts` and all eight of `exit-instructor-report.spec.ts`,
in both chromium passes.

**Four failures, one cause, and it is neither this branch nor the product.**
`MATH-140-E1FF`'s enrollments are dated **2026-09-14** — the real day the earlier
partial runs were driven — where the seeded world wants 2026-08-17. Three specs
read that section at a pretended `2026-09-11T19:00` and are refused at the door,
which is E4-22's time-bomb already fired: `00-enrollment-anchor.spec.ts`'s MATH
case, `student-survey-confidentiality.spec.ts` and
`student-survey-heading-and-next-window.spec.ts`, plus `exit-grade-passback.spec.ts`'s
P1 for the same reason.

The cause is in the logs rather than inferred. Runs 2 and 3 drove
`instructor-report.spec.ts` **on its own**, and that file's `beforeAll` clears the
clock and launches the instructor into `MATH-140-E1FF` at the real clock — by
design, for the reason its own comment records. `00-enrollment-anchor.spec.ts`,
whose whole job is to reach both sections first at their own start days, was not
in those runs, so nothing anchored E1FF; `enrollment.started_on` is a first-seen
fact and the grant does not permit rewriting it, so no later run repairs it.
`BIOL-215-R3WW` shows the other half of the same mechanism: it is dated
2026-09-07, its own start, because `instructor-report-benchmarks.spec.ts` sorts
before `instructor-report.spec.ts` and anchors it. The repair is a fresh
database, which this attempt did not make: the stack is shared.

**The re-run of chromium after the exit projects is not a second verdict.**
`exit-instructor-report.spec.ts` writes a term of responses into `BIOL-215-R3WW`
and moves the clock across six weeks, and its own header says a suite re-run
against that stack needs the database rebuilt. Its two extra failures are that
state and nothing else: `student-survey.spec.ts` asks for the confidentiality
line in a submit bar that a week already answered does not have, and
`student-survey-accessibility.spec.ts` counts one live region on a screen that
now carries more than one section. In CI each run is a fresh stack in project
order and the question does not arise.
