# E5-11 — attempts

## 2026-09-14 — confirming the reds before touching anything (round 1, backend half)

Ran the four modules the test commit (0a8a1f2) touches, in the worktree
`/home/todd/projects/pulse-surveys-worktrees/e5-11` with `PYTHONPATH` pinned to
its own backend: **12 failed, 20 passed**, exactly the manifest's prediction. All
twelve are the parametrised planted-member refusal in
`test_the_student_payload_schema_carries_nothing_of_a_benchmark.py` — six models
in `app.schemas.student` times two planted spellings — and every one of them
fails on the assertion, not on an error, so the tests are red rather than broken.
The route sweep, the value sweep, the sequence sweep, the E2-09 item-1 module and
the denial-module inventory were green on arrival, as the manifest said.

Invariant collection on this branch before the implementation: **440**
(`pytest -m invariant --collect-only -q | tail -1`), against 418 on the epic
base — the 22 cases this round's tests add.

## 2026-09-14 — checking that nothing constructs a student model with an extra member

**Done before the change, because `extra="forbid"` turns a silent drop into a
raise and a live caller passing an extra keyword would become a 500.** Every
construction of the six models is in `app/services/survey_read.py` (five of them)
and `app/api/student.py` (the empty `StudentSurveyView` on the no-enrolment
path). All pass declared members by name; none splats a mapping. The only `**`
occurrences in either file are bold markers in prose. So there was nothing to
report as a finding and nothing to weaken the config for.

## 2026-09-14 — the refusal itself

**Worked, first try.** `model_config = ConfigDict(extra="forbid")` on all six
models in `backend/app/schemas/student.py`, placed after each class docstring, as
`app/schemas/survey.py` already places it on the submission request and its
answers. The module docstring gains one paragraph saying why the refusal is
there: §4.1 item 1 and §5.4, E5 being where such figures start to exist, and
entry 2's rule that the forbidden state is what to assert.

The four modules then ran **32 passed**. The whole backend suite ran **3783
passed** under `-n 4` in 7m09s, so no other contract or fixture was relying on
the silent drop.

## 2026-09-14 — gates, on the final tree

| Gate | Result | Exit |
|---|---|---|
| the four modules | 32 passed | 0 |
| `pytest -m invariant` | 440 passed, 3343 deselected | 0 |
| `pytest -q -n 4` (whole backend suite) | 3783 passed | 0 |
| `ruff format --check backend tests scripts` | 524 files already formatted | 0 |
| `ruff check backend tests scripts` | All checks passed | 0 |
| `mypy backend` | no issues in 128 source files | 0 |
| `scripts/ci/check_invariant_assertions.py tests` | 339 invariant-marked tests each assert something | 0 |

Collection floor, criterion 5: **418 on the epic base → 440 on this branch**, all
22 of them invariant-marked and inside the isolated pass.

## What is not done here

Round 2 — the e2e DOM sweep and its instructor canary — waits on E5-10 seeding a
benchmark-bearing world for the browser. Nothing under `tests/e2e/` or
`frontend/` was touched in this round. The Docker gate is run serially elsewhere,
and neither `make ci` nor `make docker-build` was run from this worktree, because
both wipe the shared dev database.

Nothing was disputed. ADR 0175 records the choice of mechanism.
