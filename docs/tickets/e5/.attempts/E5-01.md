# E5-01 — attempts

## 2026-09-13 — baseline confirmed before anything was written

Ran the seven new or changed modules plus `test_identity_grants.py` from the
worktree, with `PYTHONPATH` pinned to the worktree's `backend` and `app.__file__`
checked first. **58 failed, 74 passed** — exactly the verified baseline. All six
must-be-green controls green:
`test_a_course_numbered_in_each_band_really_stores_that_level`,
`test_a_course_no_set_names_can_be_deleted`,
`test_the_probe_connects_as_the_application_role_and_can_see_what_it_is_granted`,
`test_at_least_one_model_module_is_registered`,
`test_nothing_excused_from_the_sweep_sits_on_a_table_that_holds_a_person`,
`test_every_identity_bearing_column_is_discoverable_through_the_marker`.

## 2026-09-13 — probed the composite foreign key onto a generated column first

The breakdown warned that `course.level` is a **stored generated** column and
that a composite foreign key onto it might be refused, in which case the answer
was to stop and report rather than improvise a trigger. Probed it against
Postgres 17 in a throwaway database (`e5_01_fkprobe`, created and dropped) with
the exact shape the ticket settles: a unique over `(id, level)` where `level` is
generated, a composite key referencing it, and a delete of the referenced row.
**Accepted, and `RESTRICT` fired.** So the settled mechanism stands and nothing
had to be invented.

## 2026-09-13 — the schema, and 129 of the 132 green

`backend/app/models/benchmark.py` with `ComparisonSet` and
`ComparisonSetMember`; `UniqueConstraint("id", "level")` added to `Course`;
`benchmark` registered in `app/models/__init__.py`; migration
`b4d7e2a91c58` off head `e5a2b81c47d3`.

Result over the same eight modules: **129 passed, 3 failed**. `ruff format
--check`, `ruff check` and `mypy` clean over everything touched. `alembic upgrade
head`, `alembic check`, `alembic downgrade -1`, `alembic upgrade head`,
`alembic check` — all clean, no operations detected either time.

The three that did not go green are `test_two_sets_may_not_share_a_name`,
`test_two_sets_with_different_names_are_both_stored` and
`test_one_course_may_be_named_in_two_different_sets`, and none of them reaches
the database: each fails with

    TypeError: seed_rows.<locals>.seed() got multiple values for argument 'name'

`seed_row` carries a positional-only `/` and a docstring paragraph saying it is
load-bearing precisely because `name` is also a column name; the `seed_rows`
fixture's wrapper drops it. **Proved the attribution rather than asserting it**:
with the `/` restored in the wrapper at run time from a scratch pytest plugin —
no file under `tests/` edited, plugin not committed — the two modules run **48
passed, 0 failed** against the schema exactly as committed.

Filed `docs/disputes/E5-01-01.md` and stopped on those three. No schema-side
answer exists: the column's spelling is fixed by `NAME_COLUMN` in the fixtures,
by `REACHED_TABLES_THAT_CARRY_NOTHING["comparison_set"]`'s exact column tuple,
and by `NAMES_THAT_HOLD_NO_PERSON[("comparison_set", "name")]`.

## 2026-09-13 — the whole suite, and a second dispute nobody's ticket caused

Ran the full suite (`pytest -q -n 4`): **3464 passed, 5 failed, 5m44s**. Three
are the dispute above. The other two are in
`tests/integration/test_the_passback_schema_survives_a_downgrade.py`, which were
green before this ticket.

That module walks the database down one revision at a time until `grade_sync` is
gone, bounded by `MOST_STEPS_DOWN = 12`. Before E5-01 there were exactly twelve
steps between head and the revision that drops it, so the walk succeeded on its
last permitted step and **any** thirteenth revision exhausts the bound. Proved it
the same way: with the bound raised to 13 at run time from a scratch plugin and
E5-01's revision untouched, the module runs **2 passed**. Filed
`docs/disputes/E5-01-02.md`. E5-03's migration, being built in parallel off the
same head, will meet the same wall.

## 2026-09-13 — green after both objections were sustained

Both disputes were sustained and the test author's fixes landed on the branch:
`e86c3a8` restores the positional-only marker in the seed wrappers — including a
third copy in `tests/fixtures/authz_data.py` that a sweep of the class found —
and `90fc58f` derives the downgrade walk's bound from
`ScriptDirectory.walk_revisions()` in all four modules that walked with a
constant. My schema, migration and records were untouched by either, which I
confirmed against the tree before re-running anything.

Re-ran everything on the final tree, foreground, same environment discipline,
with `app.__file__` re-checked inside the worktree first:

- the eight ticket modules — **132 passed, 0 failed**;
- `test_the_passback_schema_survives_a_downgrade.py` plus the two modules that
  gained the derived bound, `test_the_report_migration_round_trips_on_a_seeded_
  database.py` and `test_the_line_item_grant_is_given_back_by_a_downgrade.py` —
  **7 passed, 0 failed**;
- the whole suite, `pytest -q -n 4` — **3469 passed, 0 failed**, 5m57s;
- `ruff format --check` (4 files already formatted), `ruff check backend/`, and
  `mypy backend/app` (73 source files) — all clean;
- `alembic upgrade head`, `check`, `downgrade -1`, `upgrade head`, `check` — exit
  0, "No new upgrade operations detected" both times.

Two things the ruling leaves open and this ticket did not take: four modules
still hold private copies of the seeding helper, owned by the next ticket that
needs them, and E5-03's migration now walks under a derived bound rather than a
constant, so the two E5 revisions no longer race each other on that count.

## What is deliberately not here

No grant to `pulse_app` on either table, no index on `comparison_set_member`
beyond the constraints' own, and no service, route or resolution logic. The
grants belong to E5-04 and E5-06, each spending what it grants.
