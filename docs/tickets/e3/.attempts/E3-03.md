# E3-03 — attempts

## 1. Baseline confirmed before writing anything (2026-09-04)

Ran the ten new modules on 49a f8a7's tree. 37 failed, 12 passed — exactly the
manifest's prediction, with all twelve controls green. The tests are sound and
the reds are behavioural (`pytest.fail` naming the absent deliverable), so the
work is to write the module.

## 2. `app.models.lti` is refused by criterion 8's detector — worked around, not disputed

The work order settles tier 3's source as the section's earliest `nrps_call` row
and cites `backend/app/models/lti.py`. The unit sweep in
`tests/unit/test_the_grading_module_reaches_no_network_ags_or_job.py` matches
`AGS_WORDS` against every segment of every imported module path, and `lti` is one
of those words — so `from app.models.lti import NrpsCall` is refused in any
spelling, at module level or inside a function (the sweep walks the whole tree).

Not disputed. The assertion is satisfiable and its stated purpose — keeping
E3-04's AGS client out of the formula — is defensible; it merely over-matches a
persistence module the roster-sync log happens to share with the protocol types.
The module therefore reads `nrps_call` through `Base.metadata.tables`, with the
reason written beside the constant. Worth raising in the pull request: a later
ticket may prefer to narrow the word list, or to give the roster-sync service an
"earliest call" accessor, which is the DRY home for this read but crosses a
module boundary this ticket does not own.

Rejected on the way: `from app.models import lti` followed by `lti.NrpsCall`,
which the detector does not see because it records an `ImportFrom` node's module
and not its aliases. That is gaming the parser rather than meeting the criterion,
and it is not what the sweep is for.

## 3. The module, first run

`backend/app/services/grading.py` written to the work order's contract. First
run of all ten modules: 49 passed, 0 failed. Nothing needed a second pass —
`ruff format` reflowed the file and `ruff check` and `mypy` were clean.

## 4. Mutation battery over the arithmetic

Twenty-one mutations, each applied to the shipped module on its own and the ten
modules re-run, then the file restored from an in-memory snapshot rather than by
checkout. Nineteen were killed:

the tier-1 boundary weakened from `>=` to `>`; the tier-3 selection strengthened
from `>` to `>=`; tier 3 consulted before tier 1; `quantize` with no rounding
argument (half even) and the percentage rendered with `%g`; a refused comment
still counting its item; an unclassified comment refused rather than counted (the
inner-join reading); only one of the two refused tokens refused; a denominator
walking only the weeks the student responded in; a literal five as the week's
item count; the ledger reversed, the ledger dropping a missed week, and the
ledger numbered on the term axis; an entry for every enrolled student regardless
of elapsed weeks; a student omitted for having submitted nothing; the section's
elapsed set applied to every student; `ended_on` truncating the credited weeks;
the numerator counting whole weeks rather than items; and a
`response.is_valid` filter on the numerator.

Two survived, and neither is a defect in the module:

1. **Tier 3 firing with no roster sync to compare against** (`first_sync_day is
   None or ...`). On every world the suite builds, an undated member's
   `started_on` is the section's own start date, so resolving it into a week
   answers course week 1 — the same answer tier 2 gives.
   `test_an_undated_member_of_a_section_with_no_sync_history_starts_at_week_one`
   names this in its own docstring: the mutation "would answer differently the
   moment a seeded `started_on` fell mid-term", and no test poses that world. The
   shipped rule is the work order's — a section with no sync history is tier 2
   outright — and the mutation differs only for a student first sighted mid-term
   in a section nothing has ever synced.
2. **The governing verdict ordered by `id` alone.** `id` is
   `gen_random_uuid()` (`UuidPrimaryKey`), so `ORDER BY id DESC` picks between two
   classification rows at random rather than by insertion. Run ten times against
   `test_a_refused_comment_costs_its_item.py`, the mutation was killed on 7 of 10.
   The module orders by `classified_at DESC, id DESC`, mirroring
   `app.services.validity._latest_verdicts` as the work order requires; the
   finding is that the test module's stated kill for that mutation is
   probabilistic, which is worth the verifier's attention rather than a change
   here.

## 5. Verification

- The ten E3-03 modules: 49 passed.
- Whole suite, `pytest -n 4`: **2720 passed**, 0 failed, 4m36s.
- The §4.1 invariant pass by hand: 217 invariant tests ran, none skipped;
  `check_invariants.py` and `check_invariant_assertions.py` both clean.
- `ruff format --check .`, `ruff check .`, `mypy backend`: all clean.

No migration in this ticket, so no Alembic work. Criterion 9's record sweep is
deliberately not done here — it is the last content commit and runs on the
orchestrator's instruction.

## 6. Criterion 9 — the record sweep, non-test half

Run as the last content commit. The test half is 5ba4b1d and is the test author's.

**Four greps, not one, and the fourth is the one that mattered.** The phrase grep
("valid weeks", "weeks elapsed", "weeks completed", the division variants) found
the sixteen lines the ticket predicted. The `is_valid` identifier grep found the
data-path hits. A fact grep for the week-as-pass-or-fail wording found two more.
The fourth — every occurrence of the word *participation* in live code, read one
by one — found the two that all three earlier greps missed, and both were live
code:

- `backend/app/models/survey.py:388-392`, the comment sitting directly on the
  `is_valid` column, saying "E3's participation formula reads this column". The
  identifier grep listed line 390 (the column) and not 391 (the sentence), because
  the sentence does not contain the identifier. This is `docs/MISTAKES.md` entry
  1's rule about greping the fact rather than the identifier, demonstrated on the
  file the ticket names as the shape to look for.
- `backend/app/models/survey.py:267-269`, the uniqueness argument on `Response`:
  "the denominator is weeks rather than rows … while participation stays at one".
  The denominator is items now, and the correct argument is stronger — a second
  row for one week hangs a second set of answers off that week and can carry a
  student past the week's own denominator.

### Corrected (nine records, all live)

| Record | What was false |
|---|---|
| `backend/app/models/survey.py` `Response` docstring | the `is_valid` justification cited "§3.4's participation score is computed over these rows"; replaced with the reasons that still stand (append-only verdicts, the student's own read path) and a paragraph saying plainly that the score is not counted from it |
| `backend/app/models/survey.py` `Response` docstring, the uniqueness paragraph | "the denominator is weeks rather than rows" |
| `backend/app/models/survey.py` the `is_valid` column comment | "E3's participation formula reads this column"; and the blank-comment sentence now carries both halves |
| `backend/app/services/validity.py` module docstring | a blank comment has "no verdict and no effect"; now says no effect *on validity* and that it does cost its item |
| `backend/app/services/validity.py` `recompute_response_validity` | "what makes a week count" is a question with one place to read — that column is not what makes a week count any more |
| `backend/app/models/term.py` `SurveyWindow` docstring | quoted the weeks ratio as the denominator |
| `backend/app/views_sql/survey_submission_grants_v001.sql` (two comments) | the DELETE rationale quoted the weeks ratio; the `pulse_care` paragraph said "whether a student's week counts" |
| `mock-lms/app/ags.py` module docstring and one refusal message | "valid weeks completed over weeks elapsed" twice |
| `backend/migrations/versions/…f1a3c7d02b64…` docstring | the borderline; see below |
| `docs/tickets/e3/carried-from-e2.md` | "E3 is the first reader" of `response.is_valid` — amended in place, E2's own line kept above it |

### The borderline, decided out loud

**A merged migration's prose is corrected when it makes a forward-looking claim
that has become false, and left alone when it records what the revision did.**
`f1a3c7d02b64`'s docstring did both: it explained why `is_valid` is `NOT NULL`
with no server default (a record of the revision, kept verbatim) and predicted
that "E3's participation formula" would read the column (a claim about a reader
that does not exist as described). The prediction was removed and replaced with a
dated parenthetical saying what it used to claim and why it is wrong, so the file
still tells a reader arriving in 2027 what happened. No SQL, no revision
identifier and no `down_revision` was touched, so nothing about the migration's
behaviour moved. The precedent for editing a shipped, versioned artifact for prose
alone is 54012cb, which withdrew two false claims from
`reveal_student_identity_v001.sql` and shipped as documentation; the rule the
`_vNNN` suffix protects is semantic change, and a comment is not one.

By the same test, `20260903_b1e7d4a90c26`'s "§3.4's participation ratio is then
counted over two different calendars" was **kept**: "ratio" names no formula and
the sentence is true under either one.

### Kept as dated history, with reasons

- `docs/disputes/E2-16-01.md:64,143` and `E2-16-02.md:142` — the ticket names
  these as correct history; a dispute is the record of an argument as it was made.
- `docs/tickets/e2/E2-08-submit-path.md:67-68` and `:78` — **criterion-9 item 3.**
  The quotation the test author kept in
  `test_a_submission_is_stamped_by_the_clock_service.py:68` cites E2-08's
  acceptance criterion, and that criterion lives in a merged E2 ticket. It is
  history, not a live record: the behaviour it demanded (submission timestamps
  come from the clock service, never from a server default) is still correct and
  still enforced, and only its forward-looking clause — "because E3's
  participation formula will read both" — aged, since `grading.py` reads
  `response.week_id` and not `first_submitted_at`. Classified, not corrected.
- `docs/tickets/e2/**` generally, and `docs/tickets/e2/deferred.md:177` — merged
  ticket texts; the `deferred.md` line is about `is_valid`'s own mechanics and is
  still true.
- The E2 entry inside `docs/tickets/e3/carried-from-e2.md` — E2-13 wrote it as a
  hand-off, so E2's sentence is left standing and a dated amendment added under
  it. E3-08 re-lists from the entry as amended.

### Kept because still true

SPEC §3.3, §3.4 and §14.3 (corrected in the breakdown's own pull request);
`backend/app/jobs/tasks.py:180`, `backend/app/schemas/survey.py:95`,
`backend/app/services/validity.py:426`, ADR 0114, ADR 0115 and ADR 0122 — all
about what `is_valid` says and who writes it, which the ruling did not change;
`survey_window_grants_v001.sql:31`, `roster_sync_grants_v001.sql:31`,
`backend/app/models/identity.py:387` and `backend/app/services/authz.py:1147` —
the denominator is still derived from windows and enrollment rows;
`docs/tickets/e3/README.md:21,51,84,181,279` — the breakdown's own corrections;
the shipped student copy ("written feedback counts toward full participation
credit"), which the item formula makes more precisely true, not less.

### Both greps re-run after the edits — the results, not an assumption

- **Phrase grep, non-test tree:** 11 hits. Every one is either a correct statement
  of the new formula (`grading.py:14`, SPEC 115 and 554, the E3 ticket and
  README), a dated dispute (three lines), or one of this sweep's own dated
  corrections (`f1a3c7d02b64:18`). No hit asserts the superseded formula as
  current.
- **Phrase grep, test tree:** **zero hits.** The test author's half is complete.
- **Fact grep A** (week as a pass or a fail, what makes a week count): 8 hits, all
  statements of the item rule — `grading.py`, ADR 0131 and its index row, SPEC
  §3.4, and the E3 ticket's own criterion 9 text.
- **Fact grep B** (a participation score sentence naming validity state, in either
  order, within 120 characters): **zero hits** across the whole non-test tree.
- **Fact grep C** (forward references of the "E3 will read" shape): 3 hits — the
  corrected column comment, the merged E2-08 criterion kept as history, and the
  E3 README's own correction.
- **Fact grep D** (every `participation` in live code, read individually): 52
  hits, all classified above.

### Gates after the sweep

- Whole suite, `pytest -n 4`: **2721 passed**, 0 failed, 4m33s.
- §4.1 invariant pass by hand: 217 ran, none skipped; both CI checkers clean.
- `ruff format --check .` (344 files), `ruff check .`, `mypy backend` (90 files):
  all clean.
