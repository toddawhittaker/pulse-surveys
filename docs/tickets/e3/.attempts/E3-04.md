# E3-04 — attempts

Ticket: `docs/tickets/e3/E3-04-ags-client-and-mock-enforcement.md`. Heavy lane;
the tests are committed at `c9fd371` and `tests/**` is read-only here.

## Start state, confirmed before writing anything

`.venv/bin/pytest tests/integration/test_mock_lms_ags_requires_a_token.py
tests/integration/test_the_ags_client_is_a_conformant_service_client.py -q
-p no:randomly` → **45 failed, 22 passed**, which is the manifest's prediction.

All eight controls are green: the three in the client module
(`test_the_wire_carries_an_authenticated_ags_call_and_refuses_an_unmounted_host`,
`test_the_section_stores_the_gradebook_address_the_launch_advertised`,
`test_the_container_this_suite_composes_is_the_shape_the_mock_serves`) and the
five in the enforcement module (the challenge reader, the scope-prefix premise,
the six-address route table, the per-scope grant, the forged-token twin). None
appears in the FAILED list, so the tests are sound and the reds are the code's.

## Step 1 — the mock's AGS routes ask for a token

Two commits. The refactor first: the translation from `ServiceTokenError` to a
status and an RFC 6750 §3 challenge lived inside the roster route, and six more
routes were about to want it, so it came out as `require_a_token`. Nothing else
changed and the roster suite stayed at 14 passed.

The behaviour then. `authorised_token` takes one scope or several and passes on
membership of any (`scope in granted`, never a prefix or a substring); the 403
challenge's `scope` parameter lists every accepted scope space-delimited. All six
AGS routes call `require_a_token` as their first statement, before the query
parameters and before `require_context`/`require_line_item`. Both containers'
`ge=1` bounds came out of the route signatures — `page` and `limit` are now
unbounded strings judged by `app.paging::page_number` behind the credential, and
a value that is not one answers 400 naming the parameter (`_not_a_page`).

**Worked.** `test_mock_lms_ags_requires_a_token.py` 48 passed. The four
neighbouring suites that drive these routes — the AGS line-item/score module, the
paging module, the NRPS enforcement module, the credentials grant, and the roster
conformance module — 103 passed together.

One thing to note for anyone reading `page_number` later: it is asked about
`limit` as well as `page` now. The grammar is the same one (a run of at most nine
ASCII digits, one or more), so the reuse is exact rather than approximate, and
`page_size` still clamps an over-large value rather than refusing it.

## Step 2 — the client

`backend/app/lti/ags.py` plus `backend/app/lti/platforms/{__init__,base,mock}.py`.
Green on the first run: `test_the_ags_client_is_a_conformant_service_client.py`
19 passed, mypy clean over all 76 backend sources.

Choices worth knowing about, because a later reader will wonder:

- **The request is made on the connector's own transport, not through
  `ServiceConnector.make_service_request`.** That method discards the status code,
  which is exactly what `ags_call` records and what ADR 0129 gives a meaning; and
  its `Link` reader is the one `roster_sync` documents as wrong in two ways. The
  token still comes from `connector.get_access_token([scope])`, so there is no
  unauthenticated path in the client.
- **`timestamp` is a `str`, not the `datetime` the work order's sketch names.**
  The tests hand a string (`fixtures/ags_client.py::A_TIMESTAMP`) and criterion 3
  asserts the platform received those exact bytes, `+00:00` and all. A `datetime`
  would be re-rendered by the client, which is the mutation ADR 0052 is about.
  Reported as a departure.
- **`scoreGiven` is placed into the JSON text rather than serialised.** The score
  is checked against RFC 8259's number grammar, a uuid marker stands in for it
  while `json.dumps` writes the rest, and the marker's quoted form is replaced
  once. A float round-trip is byte-stable only for canonical `repr` strings, so it
  would silently break on `61.50`.
- **The 409 re-read uses AGS's own `user_id` filter**, so the platform is asked
  about one student rather than the class — and the `ags_call` row is written
  against the *unfiltered* results address, because settled decision 5 keeps an
  LMS user id out of that table and the filter puts one in the query string.
- **`PinnedResolutionAdapter` is imported from `roster_sync` (a public name), not
  copied.** It is a security control and two copies are two places for one to go
  stale. The Link-header parser *is* copied, because the roster's is private.
  Rehoming both is proposed in the PR rather than done — it crosses a module
  boundary.

## Full suite, on the two behaviour commits

`.venv/bin/pytest -n 4 -q` → **2853 passed** in 5m48s, exit 0. The start state was
45 failed / 2808 passed, and 45 + 2808 = 2853, so nothing outside the ticket's own
reds moved in either direction. `ruff format --check .` and `ruff check .` clean
over 363 files; `mypy` clean over all four scopes.

## Step 3 — the records

ADRs 0132 (client home, copied shape, no retry), 0133 (line-item identity: id,
then `resourceId`, never the label), 0134 (the per-route scope map, any-of
semantics, and what stays tokenless). Index rows appended in order. ADR 0099 gets
a superseded-in-part header, an inline note on the paragraph that deferred AGS,
and a note on the consequence that predicted the page bounds moving; its index row
says the AGS half is superseded. Both carried entries in
`docs/tickets/e3/carried-from-e2.md` get "Closed by E3-04" blocks in the E3-01
style.

Two corrections found while sweeping: `AgsCall.url`'s inline comment said the row
may carry the token endpoint, which contradicts the class docstring three
paragraphs above it and which E3-04's own tests forbid; and `app/lti/__init__.py`
promised five modules and listed one.

MISTAKES 29 (a value repaired before the check that should have refused it) and 34
(a pipeline discarding a non-zero exit) bumped 1→2 and 4→5, each with its instance
paragraph. Nothing re-ordered.

## Final verification, on the committed tree (all four commits)

- `.venv/bin/pytest -n 4 -q` → **2853 passed**, exit 0, 5m14s.
- Invariant gate by hand: `pytest -m invariant` 217 passed / 0 skipped;
  `check_invariants.py` OK (217 ran, none skipped, none failed);
  `check_invariant_assertions.py` OK (161 marked tests each assert something).
- `ruff format --check .` — 363 files already formatted. `ruff check .` — clean.
- `mypy` over all four scopes the Makefile's `typecheck` target runs
  (`backend`, `mock-lms/app`, `mock-idp/app`, `mock-ai/app`) — all exit 0.
- `git status --short` empty; no stray files.

No dispute was opened. No migration: every table and column this ticket touches
already exists from E3-02, and nothing here wanted one.

## Security round — two code findings

Reds confirmed first, at a9a0679: `2 failed, 71 passed` on the two AGS modules,
and the two failures were exactly the two named tests.

**MEDIUM, the transport-failure warning.** `_Caller.made` handed `%s` of the
`requests` exception to `logger.warning`, and a `ConnectionError`'s own text
quotes the URL it could not reach — the *dialled* one, query and all. On the 409
re-read that URL carries `?user_id=<sub>`, so the student's subject reached a log
stream through a string nothing wrote it into, past a method that redacts the
address everywhere it does write one.

The repair states the invariant instead of patching the branch: `row_url` — the
caller's redacted address where one was given, the dialled address otherwise — is
the only address `made` writes down, logs or puts in an error, and the exception
contributes its **class name** and nothing else. Both `RequestException` branches
were the same shape and both are fixed; the `LtiServiceException` branch never
interpolated the failure and was already clean. The original still travels as
`__cause__`, so a debugger loses nothing.

**LOW, the conflict error's message.** `AgsConflictError` interpolated the whole
held Result — the platform's `userId`, the score it holds, and any comment — into
its own message. An error's `str()` is what every logger prints without anybody
deciding to. The message now names the scores address and the section and points
at the attribute; `held` is unchanged, which is what keeps the existing conflict
test green.

**Worked.** Both named tests green; the two modules together **73 passed**,
exit 0. mypy clean, ruff clean.

**LOW, the false section header.** `mock-lms/app/tokens.py`'s comment above the
service-side half still read "NRPS only — AGS stays unauthenticated until its
first client exists", which this ticket made false. The module docstring's
paragraph three screens up said the same thing in different words ("used by NRPS
alone") and the reviewer did not name it; both are corrected, because a record
sweep that repairs the line it was pointed at and leaves its twin is the shape of
`docs/MISTAKES.md` entry 13.

### Verification after the fix round

- Two AGS modules: **73 passed**, exit 0 (was 2 failed / 71 passed at a9a0679).
- Full suite `.venv/bin/pytest -n 4 -q` → **2859 passed**, exit 0, 5m03s. The six
  new tests since the last run are the three test-side commits' (9a9eb5b,
  9454835, a9a0679); 2853 + 6 = 2859, so nothing else moved.
- `ruff format --check .` (363 files) and `ruff check .` clean; `mypy` clean over
  all four scopes.

A sweep of every `logger.*` call in `app/lti/ags.py` afterwards: every argument
is now a section id, a container / line-item / scores address, an HTTP status, a
failure's class name, or a `PULSE_*` constant. The one remaining place an
exception's text is interpolated is the address refusal, whose text is composed
by `app.models.lti` out of the column name and the refused address — a container
or a line-item id, never a filtered Result URL, because `judged()` is called on
those two only.
