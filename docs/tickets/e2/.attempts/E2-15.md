# E2-15 — attempts

Read cold by a future session. Newest attempt last.

## 2026-09-03 — first sitting, worktree `/home/todd/projects/pulse-e215`

Branch `e2/student-surface-and-local-gate-repairs`, heavy lane. The three test
modules were already committed red at `a591759` by the test author; nothing
under `tests/` was touched by this sitting, and E2-14 was building in a
parallel worktree over the modules this ticket's work order named as its own.

### The colours were confirmed before anything changed

The first command of the sitting, not the last:

```
PYTHONPATH=/home/todd/projects/pulse-e215/backend \
  .venv/bin/pytest tests/unit/test_the_submit_bounce_insufficient_sentence_is_complete.py \
    tests/unit/test_the_test_gates_target_drops_the_paid_eval_gate.py \
    tests/integration/test_the_student_survey_paths_pin_cache_control_no_store.py -q
```

3 failed, 2 passed — exactly the manifest's prediction, and each red was an
assertion failure rather than an import or collection error: the GET answered
`Cache-Control: None`, the shipped sentence did not equal the settled
replacement, and `test-gates`'s prerequisites read `["test", "e2e", "evals"]`.
The two greens are the controls: the POST's header, which has always been set
and was pinned by nothing, and the existence of an `evals` target.

`python -c "import app; print(app.__file__)"` printed a path under
`pulse-e215` before any of that, so no run in this file tested the main
checkout's code.

### What was changed, one item at a time

**1. `backend/app/api/student.py` (commit `c9ba911`).** `student_survey` gains
a `response: Response` parameter and sets `Cache-Control: no-store`, exactly as
`submit_weekly_survey` has always done. The assignment sits **before** the
`claims.user_id is None` branch rather than beside the main return, so both of
the handler's two answers carry it — the empty-enrollment answer is still this
student's answer. A paragraph was added to the handler's docstring saying why
the header is there and that it is set before the branch; the module docstring
needed nothing, because its "the router owns the protocol" paragraph is about
the write's refusal mapping and stayed true.

**2. `backend/app/copy/submit.py` (commit `7be3888`).** The truncated
infinitive became "too brief to be useful." The wording is the work order's,
byte-exact, and was not chosen here. Only three words moved; the quoted example
and the register are untouched. Nothing else in the repository quotes this
sentence — checked by grepping the phrase across `.py`, `.md`, `.ts` and
`.tsx`, which finds SPEC §3.2's own prose and this ticket's records and no
second copy of the string.

**3. `Makefile` (commit `023c937`).** `test-gates: test e2e`. The `evals`
target is untouched. Two comments in the same file were corrected in the same
commit: the header paragraph that claimed the file matches the workflow, and
the `evals` target's own note, which now says `make ci` does not run it.

**4. `README.md` and `CONTRIBUTING.md` (commit `4e4d93d`, documentation).**
README's line about `make ci` being "the same set of gates" names the one
exception now and says the paid gate is `make evals` locally and
path-or-dispatch-conditioned in CI. Its earlier claim — `make evals` is the one
local command that spends — was false while `make ci` reached the runner and is
true again as written, so it was left alone, as the work order directed.
CONTRIBUTING.md carried its own copy of the "same gates locally" sentence
(line 144) and would have been the record left asserting something the change
made false (`docs/MISTAKES.md` entry 1), so it gains the same exception. Its
CI-stage table still lists the eval floors, which is correct: that table
describes the workflow's stages, and the workflow still runs them.

### Verification

Every gate run bare or redirected to a file with its status read — never
through a pipe (`docs/MISTAKES.md` entry 34). Tools by absolute path from the
main checkout's venv; every pytest run with
`PYTHONPATH=/home/todd/projects/pulse-e215/backend`.

* The three ticket modules — **5 passed**, exit 0.
* Whole local suite, `pytest tests/unit tests/integration -n 4` — **2612
  passed**, exit 0, 250s. Run whole rather than scoped, because the copy
  registry and the student router are shared entry points and a change to
  either reaches suites this ticket never names.
* The §4.1 invariant gate by hand, all three of its steps: `pytest -m
  invariant` — **188 passed, none skipped**, exit 0; `check_invariants.py` —
  exit 0, "188 invariant test(s) ran"; `check_invariant_assertions.py` — exit
  0, "138 invariant-marked test(s) each assert something".
* `ruff format --check .` — exit 0, 310 files. `ruff check .` — exit 0.
* `mypy` (the repository's own configured run) — exit 0, 70 files. `mypy
  mock-ai/app` — exit 0.
* `make -n ci` — exit 0, and the printed expansion contains the string
  `evals` **zero times**. That is the reading half of criterion 3's
  behavioural claim; the verifier owns the executed proof.

No `make ci`, no `make docker-build`, no `make e2e` was run from this worktree.
The Compose stack was neither brought up nor taken down.

### Deliberately not done

* **Acceptance criterion 4** — the boundary review's disposition lines already
  point at "E2-15 item 1/2/3"; the criterion asks for them to point *here*
  "when this merges", which needs a merge commit that does not exist yet. Left
  for the merge step and named in the report.
* **No ADR.** Number 0122 was reserved for this ticket. Nothing here is a
  contestable construction decision the records do not already carry: the split
  and its rejected alternative are in the Makefile comment, the commit message
  and the two README sentences, and the spec is silent on none of it in a way
  an ADR would settle.
* **No test touched**, and no file under `tests/` read through an edit tool.
* **`docs/MISTAKES.md`, `docs/tickets/e2/deferred.md`,
  `.github/workflows/ci.yml`, `frontend/` and `scripts/seed.py`** were not
  touched, per the work order.
* **No `make evals` line added to README's command block.** The new sentence
  directly below it names the command; a second mention of a paid gate in a
  copy-paste block is an invitation.
