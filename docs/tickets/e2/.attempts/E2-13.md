# E2-13 — attempts

The E2 exit ticket, heavy lane. The test author's commit (899a0e7) carries the
new end-to-end spec and the withdrawal of the `EVALS` probe from the detect-probe
module. This log is the implementer's half: the workflow change that turns those
two assertions green, and the record edits the ticket owes.

## 2026-09-03 — the two reds, confirmed before anything was touched

`.venv/bin/pytest tests/unit/test_the_detect_probes_see_the_files_their_jobs_run.py`
answered **2 failed, 8 passed**, and both failures were the withdrawn probe
rather than anything else: the emitted-set comparison over planted trees, and the
published-set equality, which reported `{'evals', 'node'} == {'node'}` with
`evals` as the extra. Nothing was disputed; the module is right and the workflow
was the thing behind it.

## 2026-09-03 — the workflow half. Worked first time.

Three things went, together: the `evals:` line in the `detect` job's `outputs:`
block, the comment block above it that narrated why an unread probe was still
being published, and the `[ -f tests/evals/runner.py ]` emit that filled it.

**Checked for a reader before deleting, rather than trusting the entry.** Every
`needs.detect.outputs.*` reference in the workflow was listed: the live ones are
all `node`, on the two supply-chain gates it guards, and the only other name that
appears is `frontend`, inside comments explaining what that withdrawn output used
to gate. No live reader of `evals` anywhere, which is what the deferred entry
claimed and what ADR 0002 requires before the tolerance can go.

**One thing was edited that the work order did not name**, and it is the sweep
rule rather than an extra: the probe step's closing paragraph said "There is
deliberately no `e2e` probe and no `frontend` one" and listed the two removals
with the tickets that made them. After this change there are three, so the
paragraph now names all three. Leaving it would have left the file asserting a
count of withdrawn probes that its own code had just falsified.

Re-run of the module: **10 passed, 0 failed.** Commit `d308127`.

## 2026-09-03 — ADR 0114's open question becomes a decision

Ruling 1, given 2026-09-03: bounced comment text is not stored. Three edits in
ADR 0114 and two outside it.

Inside: the header's date and ticket lines say the record was written on
2026-09-01 and the question it left open was ruled on 2026-09-03; the
rejected-alternative paragraph on storing bounced text stops saying "not decided
here at all" and records the rejection; and the "Stated as an open limitation
rather than settled" paragraph becomes the decision, with the three grounds it
was accepted on and the condition that reopens it — a missed recall floor, or a
§3.3 bounce set widening past `insufficient` and `nonsense`. The preceding
paragraph stays as the statement of what it costs, with its framing corrected
from "this record does not decide that it should be" to "that is the accepted
cost of the decision below".

**The sweep found two records outside the ADR** (`docs/MISTAKES.md` entry 1). A
grep for the fact rather than for the words — `open limitation`,
`limitation rather than`, then `bounced comment`, `bounced text`, `refused text`,
`not decide that it should be`, `ruling is owed` — turned up
`backend/app/services/validity.py` and `backend/app/services/submissions.py`,
whose module docstrings both described the situation as a limitation stated
elsewhere rather than decided. Both now say what was decided; the submissions
docstring also separates the two entries it had run together, since the
*count* of bounced verdict rows is still open while the *text* question is not.
No test asserts on either docstring — checked before editing, because a docstring
sweep would have been the expensive way to find that out. Commit `0bc6e02`.

## 2026-09-03 — the two spec edits, approved in writing

§11 item 4 keeps its number and reads settled-for-v1 in the approved wording.
§7.2's service list gains `mock-ai` after `mock-idp`, aligned to the block's
column.

**One edit beyond the approved two, for the same sweep reason:** the heading over
the §11 list said "**Still open:**" and now says that a question settled later
keeps its number and records the settlement in place. Without it the spec would
have carried a list headed "still open" whose fourth item says it is settled.

**Verified before writing, not after:** `tests/evals/validity/floors.py` declares
`precision=0.92` and `recall=0.90` with the provenance sentence naming
`validity.v2` on the aliased model over 98 cases, and the headroom sentences are
there — so the approved wording describes the tree. The full unit suite was run
over the spec edit because several modules parse `docs/SPEC.md` at test time
(§7.4's task table, the verdict sets): **1301 passed**. Commit `3eabda7`.

**Not done, and named rather than assumed:** SPEC §13's repository-layout tree
lists `mock-lms/` and `mock-idp/` and does not list `mock-ai/`. That is the same
omission §7.2 had, one section further on, and it is outside the two approved
edits — so it is reported rather than taken.

## 2026-09-03 — the deferred cleanup pass

Four entries moved, each in the file's own answered-by style, in place under the
entry rather than as a summary at the top.

- **Token usage — closed, and the done-when was already met.** Checked rather
  than taken on trust: `tests/evals/runner.py:153` is `usage_lines`, its body
  runs to 178, and `runner.py:142` calls it per task, so a run prints input,
  cached share, cache-written, output and total. The retry caveat the entry asks
  about is printed beside the figures.
- **`detect.outputs.evals` — closed** by the workflow commit above and the
  test-side withdrawal that preceded it.
- **SPEC §11 question 4 — closed** by the approved spec edit. The closure notes
  one thing the entry did not anticipate: the set is pinned to `validity.v2`, not
  the `validity.v1` the entry was written against, because the prompt moved after
  it was written.
- **Bounced verdict rows — ruled, and still open.** Ruling 2, 2026-09-03: bounded
  by a cap on the attempts a window will classify; linkage rejected in ADR 0055's
  direction. The cap's value belongs to the ticket that implements it, and the
  drift surface still owes its filter, so the entry carries both halves forward
  with the ruling written into it.

The bounced-text entry closed in the earlier commit with the ruling it records.
Commit `a21857e`.

## 2026-09-03 — the completeness walk, entry by entry

Done before `carried-from-e2.md` was written rather than after, so the file was
written from the walk instead of the walk checking the file.

**`carried-from-e1.md`, in file order.** The §4.1 catalog sweep's whole-row join
form — fixed inside E1, not carried. `PERSON_TABLES` — carried (E13 for the
structural source; the standing question was asked of E2-05's four tables).
Signing-key custody — carried (E3). The address rules — fixed inside E1, but the
unpinned fetch-time addresses are recorded residue and carried. The
`lti_platform` writer events — fixed inside E1, ORM blind spots carried with the
line above as one entry, since they are one fact about writing that table.
TypeScript 7 — carried with the dated check. The three webfonts — closed by
E2-10, not carried (its own new residue is carried from `deferred.md`). Security
response headers — closed inside E1; the E11 write-time rejection residue
carried. Mock IdP key-set spelling — closed by E1's Batch B. Wrong-launch
selector vocabulary — closed by E1's Batch B. The algorithm pin's forgery proof —
resolved inside E1. Squatted section binding — carried (E11). Web-login linkage —
carried (E9 or E11). AGS token — carried (E3). Logout — carried (E9).
Local-account fallback — carried (E13). Deep Linking — done, §7.3 was edited with
the ruling. Mock scope-check superstring — carried. Reveal-subject guard —
carried (E4). M9, the unscoped roster-ingestion trigger — **closed inside E2 by
E2-02**, per the breakdown's own table and the commits on that branch. The
`views_sql` exemption and import guard — **closed inside E2 by E2-01**. The
low-findings block's seven bullets — six closed inside E2 (statement-pin
docstrings and the `authz.py` stale count by E2-01, verified by grep: nothing in
that file says "three org views" now; the `DESC` index, the `unpinned_hosts`
docstring and the roster walk's cycle terminator by E2-02; the struck-precedent
docstring and the restore refusal by E2-03), the generative purview note carried
to E9, and the denial-module sweep listed with the condition that turns its
closure into a carry.

**`deferred.md`, in file order.** Token usage — closed here. `detect.outputs.evals`
— closed here. §11 question 4 — closed here. The `answer` column/kind pairing —
answered by E2-08, not carried. Bounced verdict rows — carried. CSRF dependency
sweep — carried. Bounced comment text — closed here. Launch-path roster enqueue —
carried. Structural battery rows — carried as a record-only note. Bounce names no
position — carried. Week eyebrow — carried. Self-hosted faces — carried (E13).
Dev-clock resubmission 500 — carried. Two records saying "five landing views" —
corrected by E2-11, not carried. Model identifier three sites — carried. Floor
headroom variance — carried (E10). Copy-collector symlink walks — carried (E4).

**One entry has no prior record and is carried anyway.** The work order listed
"the care route's five-landings prose" as still open in `deferred.md`, and it is
not there — the entry that closed was a different pair of files, corrected by
E2-11. The live one is `frontend/src/routes/care/index.tsx`, whose docstring says
"none of the five landings has any" motion; the count is four landings plus the
survey screen since E2-10. It survived the earlier corrections because the
sentence is about motion rather than about the count, so it does not match a
search for the claim it makes. Carried with its detail, since there is no source
record to point at. The neighbouring stylesheet comment that says "the five
landing views are a heading and a line each" is narrating the ticket that wrote
it, which reads as history; the entry says so rather than deciding it.

**One residue was found and deliberately not carried.** E2-01's security fix round
recorded an `importlib.import_module` route past the one-importer sweep in that
test module's own "What this cannot see" block. It is recorded residue in the
place its readers look, with no done-when and no owner, and it is neither a
`carried-from-e1` entry nor a `deferred.md` one — so adding it to the hand-off
would have widened an inventory the work order settled. Named here and in the
report instead.

Commit `c8dd663`. A count of entries is written nowhere, in the file or in the
commit message: the first draft of that commit message carried one and it was
wrong, which is the argument against writing one at all.

## Verification

- The probe module: **2 failed, 8 passed** before; **10 passed** after.
- `.venv/bin/ruff format --check .` — 307 files already formatted.
- `.venv/bin/ruff check .` — all checks passed.
- `.venv/bin/pytest tests/unit -n 4` — **1301 passed**, run twice: after the spec
  edit and again at the end.
- Not run here, by instruction: `make ci` (it takes the stack down with its
  volume and another session's database goes with it) and the end-to-end suite (a
  `.env` gap blocks the stack). The two end-to-end specs at the test author's
  commit are unrun locally and the orchestrator owns that.
- Every gate was redirected to a file and its exit status read, never piped to
  `tail` (`docs/MISTAKES.md` entry 34).
