# E2-09 — attempts

Read cold by a future session. Newest attempt last.

## 2026-09-01 — first sitting, worktree `/home/todd/projects/pulse-e209`

Branch `e2/student-read-path`, cut from the epic branch at `e5126a8`; the tests
are committed at `82dbc2a` and were verified red before anything was written:
**10 failed, 4 errors — 14 items**, the four errors being the fixtures failing by
name on `app.api.deps.require_student` and on `app.copy`, exactly as the manifest
predicted.

### What was built, and why each piece is where it is

* **`backend/app/copy/`** — `__init__.py` holds the frozen `CopyEntry(key, text)`
  and `copy_modules()`, which discovers modules with `pkgutil.iter_modules` and
  keeps **no central list**: a list is a second place to register a string, and
  the string that matters is the one somebody forgot to add to it.
  `student_read.py` holds this path's one string, `student.not_a_student`.
* **`require_student` in `backend/app/api/deps.py`** — reads
  `session_from_request`, refuses anything that is not `LandingRole.STUDENT` with
  `401`, `WWW-Authenticate: Bearer` and the registry's text. Implemented exactly
  as the work order settles it, because E2-08 is implementing the same function
  in parallel from the same words.
* **`backend/app/services/survey_read.py`** — the assembly. Live enrollments by
  `user_id` and the institution's day, `open_window_for_section` per section with
  `at=None`, the highest-version question set, and the reader's own submission
  keyed on `(user_id, section_id, week_id)` together.
* **`backend/app/schemas/student.py`** and **`backend/app/api/student.py`** —
  the contract and a thin `GET /student/survey` with no parameters at all,
  registered in `app.main`.
* **`backend/app/views_sql/student_read_grants_v001.sql`** and revision
  `d3a71b5c8e42` — `SELECT` on `week`, `question_set`, `question`, `response`,
  `answer`, for `pulse_app` and nobody else.

### Result

**13 of the 14 items green.** The whole suite is **2428 passed, 2 failed**
(4m50s under `-n 4`); `pytest -m invariant` is **176 passed, 1 failed** over 177
collected; `ruff format --check`, `ruff check`, `mypy` and `alembic check` are all
clean. Both failures are raised as objections rather than worked around:

1. `docs/disputes/E2-09-01.md` — `StudentReadDoor.get_without_a_session()` sends
   the student's `pulse_session` **cookie** out of the `TestClient` jar, so the
   request it calls sessionless is a valid student session delivered by cookie and
   is answered `200`. Diagnosed at the boundary, not guessed: a one-line print in
   `require_student` showed `cookies={'pulse_session': …, 'pulse_csrf': …}
   auth=None`, and the payload decodes to the seeded student's own session. The
   same defect makes
   `test_a_session_that_is_not_this_students_is_refused_exactly_as_no_session_is`
   green only by accident — `get_as_an_instructor()` is evaluated first in the
   dict literal and overwrites the jar's cookie, so its "no session at all" case
   is actually an instructor's session a second time.
2. `docs/disputes/E2-09-02.md` — `RUNTIME_BASE_TABLE_PRIVILEGES` is a closed
   inventory behind the test wall and does not name the five new `SELECT`s. The
   fourth instance of a shape ruled on three times (E2-04-02, E2-06-03).

### Criterion 2's mutation proof

Run as a **runtime injection from a plugin outside the repository**, loaded with
`-p`, rather than as an edit to the shipped module: the permission system refuses
an edit that deletes the `user_id` filter from a query — correctly, since that is
what the edit is — and the same measurement is available without touching the
tree. Each plugin asserts the substitution landed before yielding and puts the
real function back afterwards.

1. **The enrollment predicate, loosened** — `_live_enrollments` with `user_id`
   dropped, answering with every section of the term. `4 failed, 4 passed`:
   `test_no_student_visible_route_names_a_section_the_student_is_not_enrolled_in`
   and all three parametrisations of
   `test_naming_another_section_is_answered_exactly_as_naming_one_that_does_not_exist`
   go red, naming the other section by its own values —
   `assert not ['Q1WW', 'b8cd7fc4-…', 'c0eeb7d5-…']`. The four that stay green are
   the two route-table controls, the classmate denial and the session denial,
   which this mutation does not touch.
2. **The own-submission lookup, with the author left out** — `_own_submission`
   keyed on `(section, week)` alone, E2-05's uniqueness key minus `user_id`.
   `1 failed, 7 passed`: exactly
   `test_a_classmates_submission_is_never_in_this_students_answer`, which is a
   precise kill rather than a module-wide red.

### Things worth knowing next time

* **The `week` grant was missing before this ticket.**
  `survey_windows.derive_windows_for_section` already selects `week` rows on the
  `pulse_app` connection and no revision had granted it. Repaired here because
  this read needs it; stated in the SQL header and in E2-09-02 rather than left to
  be found.
* **Every one of the five grants is load-bearing, measured one at a time.**
  Granting them in order, the read path stops on the next relation each time:
  `week` → `question_set` → `question` → `response` → `answer`. Without the
  revision at all, 12 of the 14 items fail with `InsufficientPrivilege` (36
  occurrences in one run); the two survivors are the route-table controls, which
  read no rows.
* **The grants ship as a `views_sql/*.sql` file, not inline in the revision.**
  ADR 0041's decision names the grants file explicitly ("the roles, two views, the
  `SECURITY DEFINER` reveal function, and the grants") and every grant in this
  tree follows it, E2-06's `survey_window_grants_v001.sql` a day ago included. The
  ticket's criterion 4 — "no new module under `backend/app/views_sql/`" — is read
  as forbidding a new **view**: it cites E2-01's sweep, whose exempt list is
  Python modules, and a grants file creates no view and adds nothing to the
  policed inventory. Flagged in the report and the PR body rather than decided
  quietly.
* **The Bearer-only shortcut was available and refused.** Making
  `require_student` ignore the cookie would turn E2-09-01 green in one line; it
  contradicts `session_from_request`'s documented contract, makes the CSRF
  double-submit machinery dead code, and would be a silent divergence from the
  contract E2-08 is building against.
* **A response body with a varying value would red the parameter test.**
  `test_naming_another_section_is_answered_exactly_as_naming_one_that_does_not_exist`
  demands byte-identical bodies across reads when two plain reads agree, so
  nothing in this answer may carry "now" or any other moving value.
* **The open/closed flag has to be a boolean that flips.** `booleans_in` collects
  every boolean by path and requires one that is `true` inside the window and not
  `true` on either side; `survey_is_open` is that field, and the window itself is
  `null` when nothing is open.
* **The test-edit hook matches the whole Bash command positionally.** `mv … &&
  pytest tests/…` is blocked because the `mv` sits before the `tests/` token. Two
  separate calls, with nothing suspicious ahead of the path, run fine.

## 2026-09-01 — second sitting: both objections ruled to the test side, re-verified

`11e45a7` applied the two repairs and **changed nothing in the implementation**.
`StudentReadDoor` gained a `carrying_no_cookie()` context manager that swaps the
`httpx` jar out for the duration of a refusal call — both refusal drivers use it,
so each request carries exactly one credential and it is the one the method's
name says;
`test_a_session_that_is_not_this_students_is_refused_exactly_as_no_session_is`
now takes the anonymous reading **before and after** the instructor's launch and
requires the two to be identical, which asserts the order-independence the old
version only arranged; and `RUNTIME_BASE_TABLE_PRIVILEGES` gained the five ruled
`SELECT` tuples with a record entry beside the constant.

Everything re-run on `11e45a7`, all green:

| gate | result |
|---|---|
| the ticket's own two modules | **14 passed** |
| those plus `test_identity_grants.py` | 51 passed |
| the whole suite, `-n 4` | **2430 passed, 0 failed**, 4m49s |
| `pytest -m invariant` | **177 passed**, 2253 deselected, 0 skipped |
| `scripts/ci/check_invariants.py` on its junit XML | `OK: 177 invariant test(s) ran, none skipped, none failed.` |
| `ruff format --check` / `ruff check` / `mypy` / `alembic check` | clean, clean, 83 files clean, no new upgrade operations |

The invariant pass collects the same 177 items it collected in the first sitting;
what changed is that the one red in it is now green. Eight of the 177 are
`test_the_student_read_path_names_nothing_outside_the_enrollment.py`, which is the
number to check after any rebase.

Both mutations re-run against the repaired tests, and each still kills precisely
what it should: the loosened enrollment predicate `4 failed, 4 passed`, the
own-submission lookup without the author `1 failed, 7 passed`.
