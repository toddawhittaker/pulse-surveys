# E2-12 — attempts

## 2026-09-02 — the red phase confirmed before anything was written

Ran the controls and the reds against the two committed test commits (8db86c9,
7f8ae9d) before touching a line of code, because the work order's predicted
colours are worth nothing unread.

- The two control modules
  (`test_the_eval_runner_refuses_rather_than_reporting_a_pass.py`,
  `test_the_validity_eval_set_carries_the_cases_the_heuristic_gets_wrong.py`):
  31 passed, 0 failed.
- The five red modules: 20 failed, 6 passed. The six greens are the controls
  living inside those modules — the inert-classification control, the runner's
  `live=True` construction, the two-endpoint distinguishability control, the
  development-literal control, the `--enforce-floors` invocation, and
  `test_the_inert_clause_comes_first_in_every_eval_step_condition`, which the
  manifest addendum already corrected from red-assert to green-and-vacuous.
- Whole suite: **176 failed, 2368 passed** in 266s under `-n 4` — the measured
  red-phase state the manifest addendum records, to the test.

37 green controls in total, and every red failing through a sentence rather than
a collection error. The manifest is accurate and is the plan of record.

Environment note for a future session: the venv is not on PATH, `PYTHONPATH` has
to name this worktree's `backend`, and `DATABASE_URL` needs its `@db:` rewritten
to `@localhost:`. `import app` was checked to resolve inside this worktree before
any run was believed.
