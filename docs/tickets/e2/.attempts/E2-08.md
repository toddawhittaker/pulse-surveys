# E2-08 — the submit path: attempts

## 2026-09-01 — the red baseline, confirmed before any code was written

Ran the ticket's six modules on the untouched tree in the worktree
`/home/todd/projects/pulse-e208` at `c8f473d`:

- `tests/integration/test_the_submit_test_machinery_stands_up_what_it_claims.py`
  and `tests/unit/test_the_submit_paths_copy_is_externalised.py`: 12 failed,
  8 passed.
- the four criteria modules: 23 failed, 3 passed.

46 tests, 35 red and 11 green — the counts the work order predicts. Every one of
the 23 integration reds dies on the named guard in `tests/fixtures/submit.py`
("`app.api.student` defines 0 POST routes on the built application"), and the 11
unit reds on the named missing `app.copy` package. No unnamed exception, no
collection error.

**Worked.** This is only a measurement, but it is the one every later claim in
this file rests on.

## 2026-09-01 — the route-discovery walk cannot see an included router

Before writing anything, measured the pinned web stack the way
`tests/fixtures/submit.py`'s `submit_route` reads it:

```
$ .venv/bin/python -c "... FastAPI(); app.include_router(r); print(app.routes)"
Route /openapi.json ... Route /redoc ...
_IncludedRouter None None None
```

`fastapi==0.141.1` appends one `fastapi.routing._IncludedRouter` per included
router, carrying no `.path` and no `.endpoint`. `submit_route` walks
`client.app.routes` and keeps the members whose `endpoint.__module__` is
`app.api.student`, so a router registered the way `app.main.create_app` registers
every other one is invisible to it.

**Did not work, and it is not mine to repair.** This is the same library
behaviour `docs/disputes/E2-04-01.md` raised and the arbitrator ruled on
2026-09-01; the repair it required was the shared flattening walk that now lives
in `tests/fixtures/routing.py`, and `submit_route` reintroduces the blind copy.
Objection filed as `docs/disputes/E2-08-01.md`. The implementation is written
against `include_router` regardless — see below — and verified out of band.

## 2026-09-01 — the task-discovery helper cannot see a Celery task either

`reclassification_entry_point` keeps only members of `app.jobs.tasks` whose
`__module__` is that module. Every task there is a `celery.local.PromiseProxy`,
whose `__module__` is `'celery.local'` — a class attribute, so the lookup never
reaches the proxy. Measured over all twenty public members of the module: the
candidate set is **empty**, including `ping`, which has been there since E0-03.

**Did not work, and it is not mine to repair.** Objection filed as
`docs/disputes/E2-08-02.md`; the repair is `getattr(value, "run", value).__module__`,
which keeps the clause's purpose (excluding the imported service functions) and
answers `'app.jobs.tasks'` for all seven tasks.

## 2026-09-01 — the whole path, built

`app/copy/` (package + `submit.py`), `require_student` in `api/deps.py`,
`api/student.py` with one POST route registered in `main.py`,
`services/validity.py`, `services/submissions.py`, `schemas/survey.py`, the task
and beat entry in `jobs/`, and the migration with `survey_submission_grants_v001.sql`.

**Worked**, measured under an out-of-band plugin supplying the two fixture
repairs above: all 46 of the ticket's tests pass — 20/20 in the copy and
machinery modules, 26/26 across the four criteria modules.

Two decisions had to be made against the work order and are recorded as ADRs
rather than buried: ADR 0114 (unreachable/refused/invalid → 503 + `Retry-After: 60`,
plus the route's whole status table including the 422 bounce) and ADR 0115 (a
resubmission revises its answer rows in place, because `classification.answer_id`'s
`ON DELETE RESTRICT` refuses the delete-and-reinsert the work order names — the
two settled decisions are incompatible).

## 2026-09-01 — MISTAKES 41's three protections were not enough on their own

First run of `test_a_submission_is_prompt_while_the_broker_is_unreachable`:
**6.05s** against SPEC §10's 2.5-second budget, with
`apply_async(retry=False, ignore_result=True)` inside a broad `except`. `retry=False`
governs the publish retry only; `kombu.Connection.default_channel` runs
`_ensure_connection` with kombu's own defaults (`interval_start=2, interval_step=2`)
before the publish is attempted, so a broker that refuses *instantly* is still
retried on a schedule of its own.

**Fixed** by publishing on a connection made for the call, with
`transport_options={"max_retries": 0, "socket_connect_timeout": 1.0,
"socket_timeout": 1.0}` and `connect_timeout=1.0`. Measured: closed port 6.04s →
**0.037s**; blackholed address, where no refusal ever comes, **1.04s** instead of
the operating system's own timeout; a broker that answers publishes in 0.046s.
Scoped to this connection, never to `celery_app.conf`, because a *worker* whose
broker blips must reconnect rather than give up.

`app.services.roster_sync.request_section_sync` has the same shape and the same
six seconds on the launch path. It is a shared module outside this ticket, so it
is proposed in the pull request rather than changed here.

## 2026-09-01 — the gates, measured

- **Whole suite, tree as it stands:** 2433 passed, 29 failed in 5m00s under
  `-n 4`. Twenty-four of the failures are this ticket's own tests, all of them
  behind `E2-08-01` (23) and `E2-08-02` (1); the other five are the three records
  three more objections raise (`E2-08-03`, `E2-08-04`, `E2-08-05`). No other
  module moved, and the run's wall-clock time is in line with the four minutes a
  clean run of this suite takes.
- **Whole suite with the two fixture objections applied out of band:** 2457
  passed, 5 failed in 4m43s — the five record failures and nothing else. The
  ticket's own 46 all pass.
- **Invariant pass:** 167 ran (non-empty), 4 failed — the four
  `invariant`-marked halves of `E2-08-03` and `E2-08-04`. This ticket's own two
  invariant tests are among the 167 that passed.
  `scripts/ci/check_invariant_assertions.py` is clean: 123 marked tests each
  assert something.
- **`ruff format --check`, `ruff check`, `mypy`:** clean.
- **`alembic upgrade head` then `alembic check`:** clean, on a scratch Postgres
  at the pinned image with both runtime roles provisioned. A `downgrade -1`,
  `upgrade head`, `check` round trip is clean too, and the grants come back
  (nine rows) rather than being left revoked.

One finding worth an entry in `docs/MISTAKES.md` that this worktree may not
write, so it is reported to the orchestrator instead: entry 41's three
protections do not bound a request-path publish on their own — see the paragraph
above for the 6.04s and what fixed it — and the same six seconds sit on
`app.services.roster_sync.request_section_sync`'s launch-path enqueue today.

## 2026-09-01 — re-verified after all five objections were ruled to the test side

The repairs landed as `bbab42c`, with a forty-seventh test asserting ADR 0115's
product rule: a comment a classification names cannot be withdrawn by a
resubmission — 409, the `submit.comment_already_judged` sentence, and the stored
rows unchanged. That test's docstring says in as many words that a path which
refuses only the withdrawal while applying the rest is a dispute rather than a
pass, so it was read before it was run: `_write_answers` looks for a verdict
naming a withdrawn row and raises **before** any value is written, and
`store_submission` rolls the transaction back on the way out, so the second
submission's revised rating never reaches the row either. Whole refusal, nothing
to fix.

Everything green on the tree as it stands — no plugin, no substitution:

- the ticket's **47 tests: 47 passed** in 30s;
- the **whole suite: 2463 passed, 0 failed** in 4m46s under `-n 4`, which is the
  wall-clock time a clean run of this suite takes — the half of
  `docs/MISTAKES.md` entry 41's corollary that is about timing;
- `pytest -m invariant`: **171 passed**, none skipped;
  `scripts/ci/check_invariants.py` clean, and
  `scripts/ci/check_invariant_assertions.py` clean at 123 marked tests;
- `ruff format --check`, `ruff check`, `mypy`: clean;
- `alembic upgrade head` then `alembic check`: clean on a scratch Postgres at the
  pinned image with both runtime roles.

The three record commits followed, code having stopped moving: entry 41's fourth
protection and its counter, the deferral file's new entry, and the answer to the
deferral E2-05 left addressed to this ticket.

## 2026-09-02 — the security review's three MEDIUMs, and how reliable the pass was

Recorded before the fixes, because the weight to put on each finding depends on
it. The **generic pass was mis-scoped and was re-run against the correct base** —
the first run diffed against the default base rather than the pull request's, so
it read the whole repository as this ticket's change. **Findings 2 and 3 are
single-source**: one reviewer saw each, with no second pass corroborating. And an
**early corroboration claim was withdrawn by the reviewer before the final
report**, so nothing in the round rests on it. All three findings were ruled
regardless and all three are real on their own terms; the note is here so that a
later reader does not infer agreement between passes that did not happen.

## 2026-09-02 — the three fixes

**Worked.** All 51 of the ticket's tests pass.

1. **CSRF.** `csrf_verified_student` in `app.api.deps` verifies the
   `X-Pulse-CSRF` header against the session's `jti` and answers 403 with the
   registry's sentence; a Bearer-authenticated request returns before the check.
   It is a *second* dependency rather than an addition to `require_student`,
   because a read path has nothing for the check to protect and E2-09 carries the
   plain one. Which carrier a session arrived on is asked of one new function,
   `app.services.session.bearer_token`, split out of `session_from_request` whose
   behaviour is unchanged — two readings of that question in two modules is how an
   exemption widens with nothing going red.
2. **Length.** `max_length=4000` on the request model's comment field, with the
   number and its reason beside it.
3. **Bounce keeps its verdict.** The gate moved *before* the write, so a bounced
   submission never creates a response or an answer to roll back, and the verdict
   is recorded against no answer and committed. `app.ai.tasks` grew the split that
   makes it possible — `verdict_for_comment` judges, `classify_comment_validity`
   is that plus `record_classification` — so the model is asked once and the
   caller decides where the row goes.

**Did not work the first time, and the whole suite is what said so.** The split
put a second public callable carrying "validity" into `app.ai.tasks`, and the
AI-task fixture finds the task by that fragment: 51 tests across the two gateway
suites failed saying they could not tell which of the two was the task. That is
the hazard `docs/MISTAKES.md` entry 13's neighbours describe — a discovery by name
fragment meeting a second name — and the ambiguity was mine rather than the
fixture's, so the judging half was renamed `verdict_for_comment` and the two
suites are green at 60 of 60. The ticket's own suites never saw it, which is entry
41's corollary again.

## 2026-09-02 — the re-mutation battery, and two survivors

Nine mutations of what this round touched, each checked to have landed before its
run and restored afterwards, with a control over the four tests on the restored
tree (4 passed).

Seven were killed: the CSRF check removed (cookie half red, Bearer half green);
the Bearer exemption removed (Bearer half red, cookie half green); `max_length`
removed; the bound set to 3999; the bounce discarding its verdicts again; the
bounce keeping only the model's verdict and not the floor's; and the accepted path
recording its verdict against no answer.

Two survived, and **neither is a defect in what shipped** — the code verifies the
token rather than checking its presence, and the bound is on the request model
rather than in the service. What survives is each test's ability to tell:

- **The header's presence accepted without verifying it.** The test sends the
  cookie and the header together or neither, and never sends a *wrong* header, so
  presence and verification answer identically on both halves. The docstring names
  that mutation as killed.
- **The bound moved into the service.** It survives because of this round's own
  reordering: the gate now runs after all value validation, so a service-side
  bound also refuses before the provider is asked and also leaves no
  classification row.

Raised as `docs/disputes/E2-08-06.md` with the table and the one-line repairs,
which are the test author's. It blocks nothing and asks for no further round.

## 2026-09-01 — one thing deliberately not chased

A `PytestUnraisableExceptionWarning` appeared once in the 2457-test parallel run:
`celery.result.AsyncResult.__del__` reconnecting to a result backend whose
testcontainers Redis had already stopped. It is a garbage-collection ordering
artifact of any `apply_async` in this codebase rather than of this path — the two
runs that exercise the enqueue against a live broker and a closed port produced
none — and `filterwarnings` makes only `DeprecationWarning` an error, so it stays
a warning. Recorded rather than fixed.
