# Architecture decision records

A record of construction decisions that [`docs/SPEC.md`](../SPEC.md) does not
answer and that a reasonable engineer might have made differently.

## When to write one

Both halves have to be true: the spec is silent, **and** the choice is genuinely
contestable. Choosing a JSON library needs no record. Choosing how identity
separation is enforced in the database does.

Write it in the same pull request as the decision, not afterwards. An ADR
written later is a reconstruction, and reconstructions leave out the option that
seemed obvious at the time and turned out to be wrong.

## When not to write one

- **The spec already decides it.** Link to the spec section instead. An ADR that
  paraphrases a spec section is noise that makes the real ones harder to find.
- **The decision contradicts the spec.** An ADR is not sufficient and not the
  right instrument. Raise it, and update the spec — a record of having gone
  around the spec is not the same as the spec being right.

## Format

`NNNN-slug.md`, four sections, under a page:

1. **Context** — what forced a choice, and which spec section left it open.
2. **Decision** — what was chosen, stated plainly.
3. **Alternatives rejected** — each with the reason it lost. This is the section
   that earns the document; a list of alternatives with no reasoning is a list.
4. **Consequences** — what this costs, what it constrains later, and what has to
   be true for it to keep working.

Number sequentially, never reuse a number, never renumber. **Add a row to the
table below in the same commit** — an unindexed record is one nobody finds, and
three of them accumulated on one branch before anyone noticed, because the index
is the artifact nobody re-reads once it exists.

**0029, 0033 and 0034 do not exist, and no record is missing.** Three tickets
were built in parallel worktrees and each was given a range to number within —
0025–0029, 0030–0034, 0035–0039 — so that two branches could not both claim one
number. Each ticket used fewer than its range allowed and the remainder was left
unused rather than back-filled, because renumbering is the one thing this file
forbids. A gap in the sequence means a range ran out early, never that a record
was lost.

A superseded record stays where it is with a line at the top pointing at its
replacement. Where a later decision changes only *part* of an earlier one, say
amended rather than superseded, on the earlier record's status line and beside
the specific paragraph that moved — and in the table, so the qualifier is
visible without opening the file. Calling a record superseded when its decision
still stands sends a reader looking for a replacement that does not exist.

## Records

| # | Decision | Status |
|---|---|---|
| [0001](0001-identity-separation-by-database-role.md) | Identity separation enforced by database role and grant | Accepted — one consequence amended by [0009](0009-a-superuser-identity-is-sanctioned-for-migrations-and-bootstrap.md) and again by [0040](0040-pulse-migrate-is-the-bootstrap-identity-under-another-name.md), which also resolves `pulse_migrate`; [0043](0043-the-reveal-function-has-an-owner-of-its-own.md) adds the definer role it does not name |
| [0002](0002-ci-gates-ship-tolerant.md) | CI gates ship tolerant and name the ticket that enforces them | Accepted, recorded retroactively |
| [0003](0003-deferred-authz-seams-fail-closed.md) | Deferred authorization seams fail closed by raising | Accepted |
| [0004](0004-agent-roster-mechanism.md) | Agent roster mechanism: hooks, computed gating, session-scoped warmth | Accepted |
| [0005](0005-dependency-locking.md) | Python dependencies are locked with pip-compile, hashes and all | Accepted |
| [0006](0006-settings-lifetime.md) | Settings are built inside `create_app()` and hung on `app.state` | Accepted |
| [0007](0007-container-images-pinned-by-tag-and-digest.md) | Container images are pinned by tag and by digest | Accepted |
| [0008](0008-env-has-two-readers-and-the-database-credential-is-split.md) | `.env` has two readers, and the database credential is split into parts | Accepted — the count amended to three by [0012](0012-the-migration-environment-builds-its-own-superuser-connection.md) and to four by E0-17's `scripts/seed.py`; everything else the record decides is unchanged both times |
| [0009](0009-a-superuser-identity-is-sanctioned-for-migrations-and-bootstrap.md) | A superuser identity is sanctioned for migrations and bootstrap | Accepted — one row of its provisioning table amended by [0040](0040-pulse-migrate-is-the-bootstrap-identity-under-another-name.md) |
| [0010](0010-the-celery-application-is-built-at-import-time.md) | The Celery application is built at import time, at module level | Accepted — answers for Celery the entry-point question [0006](0006-settings-lifetime.md) left open |
| [0011](0011-ci-validates-the-image-by-running-the-base-compose-file-alone.md) | CI validates the image by running the base Compose file alone | Accepted |
| [0012](0012-the-migration-environment-builds-its-own-superuser-connection.md) | The migration environment builds its own superuser connection | Accepted — settles the two open rows in [0009](0009-a-superuser-identity-is-sanctioned-for-migrations-and-bootstrap.md)'s provisioning table |
| [0013](0013-the-database-session-is-synchronous.md) | The database session is synchronous, and the engine is built at import | Accepted — answers for the database the entry-point question [0006](0006-settings-lifetime.md) left open |
| [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md) | LMS-owned columns are marked by an `lms_` name prefix | Accepted, **amended by E0-35, 2026-08-19** — E0-11 chose a table grain instead of the marker's ([0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md)), and E0-35's sweep now enforces the direction this record could not, at table grain. The marker is documentation, and "the chokepoint must be able to read it" — one of the two reasons for a name over an `info={}` dict — is void. The decision stands on the other reason; the closure reaches LMS-owned tables and nowhere else |
| [0015](0015-course-level-is-a-stored-generated-column.md) | Course level is a stored generated column, and the bands are its only authority | Accepted |
| [0016](0016-primary-keys-are-database-generated-uuids.md) | Primary keys are database-generated UUIDs | Accepted — UUIDv7 revisited when the Postgres image moves to 18 |
| [0017](0017-prefix-codes-are-unique-across-the-deployment.md) | Prefix codes are unique across the deployment, not per institution | Accepted — rests on one institution per deployment, which was stated in the record as a latent assumption and is a rule since 2026-08-20 ([0072](0072-one-institution-is-a-unique-index-on-a-constant.md)); the consequence about a confusing `uq_prefix_code` violation is retired with it |
| [0018](0018-cross-table-length-rules-are-enforced-by-a-composite-foreign-key.md) | Cross-table length rules are enforced by a composite foreign key carrying the term's length | Accepted |
| [0019](0019-a-naive-datetime-is-refused-by-the-column-type.md) | A naive datetime is refused by the column type, not by Postgres or a service | Accepted |
| [0020](0020-a-sections-end-date-is-its-last-day.md) | A section's end date is its last day, inclusive | Accepted |
| [0021](0021-a-sections-derived-calendar-has-one-writer.md) | A section's derived calendar is NOT NULL and has exactly one writer | Accepted, **amended by E0-35, 2026-08-19** — "exactly one writer" was convention when this was written and now has a sweep behind it, at module grain rather than the function grain the record names. The sweep is syntactic, so a second writer reached through a helper in another module or an ORM cascade is still invisible, and its own positive control is what the claim rests on |
| [0022](0022-identity-bearing-columns-are-marked-by-a-name-prefix.md) | Identity-bearing columns are marked by an `identity_` name prefix | Accepted — follows [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md), and takes the name where the two markers meet; the discovery rule beside it widened in E0-10, the marker itself unchanged |
| [0023](0023-overlapping-enrollments-are-refused-by-an-exclusion-constraint.md) | Overlapping enrollment windows are refused, by a GiST exclusion constraint | Accepted — settles the choice E0-08's criterion 5 leaves open |
| [0024](0024-the-person-to-user-link-is-carried-by-person.md) | The person-to-user link is carried by `person`, and is one to one | Accepted — rests on one registered platform per person, stated in the record |
| [0025](0025-an-assignments-scope-is-one-nullable-foreign-key-per-level.md) | An assignment's scope is one nullable foreign key per containment level | Accepted — settles what SPEC §8's singular `scope_node_id` references |
| [0026](0026-entry-doors-are-derived-from-the-role-as-generated-columns.md) | Entry doors are derived from the role, as stored generated columns | Accepted — follows [0015](0015-course-level-is-a-stored-generated-column.md) |
| [0027](0027-supervision-edges-are-policed-by-one-row-level-trigger.md) | Supervision edges are policed by one row-level trigger, holding an advisory lock | Accepted — takes the trigger [0015](0015-course-level-is-a-stored-generated-column.md) rejected, for a rule that spans rows; every relation schema-qualified against a `pg_temp` hijack; holds under READ COMMITTED and SERIALIZABLE, and refuses REPEATABLE READ rather than being silently wrong under it; extended by [0044](0044-a-supervision-edge-must-climb-the-role-rank.md), which added two rules to the same function and made the cycle walk defence in depth |
| [0028](0028-a-student-holds-no-role-assignment.md) | A student holds no role assignment | Accepted — a student's access is resolved from `enrollment` |
| [0030](0030-a-verdict-is-an-enum-whose-value-is-the-stored-token.md) | A verdict is an `enum.Enum` whose value is the stored token | Accepted |
| [0031](0031-every-task-contract-carries-the-prompt-version-and-model-id.md) | Every task contract carries the prompt version and model ID, and the gateway supplies them | Accepted — reads E0-12's "every model" over §7.4's narrower "every classification" |
| [0032](0032-a-prompt-file-is-immutable-once-a-classification-cites-it.md) | Prompts are named `<task>.v<N>.md` and a committed prompt file is never edited | Accepted — half enforced by test, half convention, said so in the record |
| [0035](0035-the-mock-platform-signs-with-standard-library-rsa.md) | The mock platform signs with standard-library RSA | Accepted — bounded to `mock-lms/`, and the bound is part of the decision |
| [0036](0036-the-mock-platform-publishes-its-registration-as-a-document.md) | The mock platform publishes its registration as a document keyed by column | Accepted |
| [0037](0037-the-mock-platform-is-configured-by-compose-literals.md) | The mock platform is configured by Compose literals, and earns no `.env.example` entry | Accepted — applies the rule in [0008](0008-env-has-two-readers-and-the-database-credential-is-split.md); E0-16 reached the same answer for `mock-idp`, so it now covers both mocks |
| [0038](0038-the-mock-platform-ships-in-the-base-compose-file.md) | The mock platform ships in the base Compose file, and is kept out of a deployment by what it holds | Accepted |
| [0039](0039-the-two-app-packages-are-typechecked-in-two-runs.md) | The two `app` packages are typechecked in two mypy runs | Accepted — three runs since E0-16 added `mock-idp/app`; the record generalises rather than being superseded |
| [0040](0040-pulse-migrate-is-the-bootstrap-identity-under-another-name.md) | `pulse_migrate` is the bootstrap identity under another name, and is not created | Accepted — amends one row of [0009](0009-a-superuser-identity-is-sanctioned-for-migrations-and-bootstrap.md)'s provisioning table and resolves the third role named in [0001](0001-identity-separation-by-database-role.md), its own role count amended by [0043](0043-the-reveal-function-has-an-owner-of-its-own.md) |
| [0041](0041-a-read-view-ships-as-an-immutable-versioned-sql-file.md) | A read view ships as an immutable, versioned `.sql` file that a migration executes | Accepted — follows [0032](0032-a-prompt-file-is-immutable-once-a-classification-cites-it.md); the layout is a decision this record holds and the suite does not enforce, measured and said so |
| [0042](0042-the-care-pool-has-its-own-credential-and-opens-on-first-use.md) | The Care pool has a credential of its own, and opens on first use | Accepted — answers for the Care queue the entry-point question [0006](0006-settings-lifetime.md) left open; one rejected alternative reversed by E0-10, so `api` alone holds the credential and `CARE_DATABASE_URL` is optional |
| [0043](0043-the-reveal-function-has-an-owner-of-its-own.md) | The reveal function has an owner of its own, holding a readable list of grants | Accepted — amends the role count in [0040](0040-pulse-migrate-is-the-bootstrap-identity-under-another-name.md); the views deliberately keep the migration identity, and the record says what that leaves open. **The list was three and is four since E0-26** ([0071](0071-the-reveal-answers-only-a-committed-record.md)): the reveal reads its subject out of a committed `audit_log` row, so the definer holds `SELECT` there too. The rule that the owner holds exactly what the bodies do is what makes that a decision rather than a drift |
| [0044](0044-a-supervision-edge-must-climb-the-role-rank.md) | A supervision edge must climb the role rank, and the trigger is what refuses one that does not | Accepted — extends [0027](0027-supervision-edges-are-policed-by-one-row-level-trigger.md); settles the choice E0-09 left in neither place; bounds the graph at six deep and subsumes acyclicity; the same-role half was disputed and upheld ([E0-11-01](../disputes/E0-11-01.md)), the ruling finding that `LEAD_FACULTY → LEAD_FACULTY` is refused by §4.1 invariant 2 and every other same-role pair is spec-silent |
| [0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md) | The chokepoint refuses an LMS-owned write at table grain, plus one row | Accepted — chooses the grain E0-11 refuses to inherit from [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md)'s marker, and says what it does not catch. Two consequences moved on 2026-08-19: E0-35 closed [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md)'s open half on the guarded tables with a sweep beside this chokepoint rather than a change to it, and put a tripwire on "a caller can bypass it by not calling it" ([0069](0069-three-rules-held-by-a-docstring-are-swept-out-of-the-source.md)). How a *sanctioned* writer satisfies that tripwire is E1's, by Todd's decision |
| [0046](0046-a-purview-is-a-materialised-node-set-per-containment-level.md) | A purview is a materialised, downward-closed node set per containment level | Accepted — the value every read path is handed; Care is deliberately not one of its fields, and the deferred union it composes with is [0003](0003-deferred-authz-seams-fail-closed.md)'s |
| [0047](0047-the-posted-score-readback-is-a-mock-only-route.md) | The posted-score readback is a mock-only route, not a widened AGS `Result` | Accepted — Todd's decision, 2026-08-17; a conformant `Result` has no room for the fields E0-15's criterion 4 names, so the inspection surface sits outside the AGS namespace and nothing teaches E3 to expect them from a platform |
| [0048](0048-enrollment-windows-ride-on-a-namespaced-nrps-extension.md) | Enrollment windows ride on a namespaced NRPS member extension | Accepted — Todd's decision, 2026-08-17; NRPS 2.0 defines no date on a member while SPEC §3.4 and §9.2 both read one from NRPS, so the mock supplies it the way a real platform does. What a platform supplying none should do is left to E1 |
| [0049](0049-the-mock-gradebook-is-per-application-state-in-memory.md) | The mock platform's gradebook is per-application state, held in memory | Accepted — bounded to `mock-lms/`, like [0035](0035-the-mock-platform-signs-with-standard-library-rsa.md); the log grows unbounded and the service cannot be run at two replicas, both said in the record |
| [0050](0050-the-mock-roster-exposes-an-address-and-no-name.md) | The mock roster exposes an address and no name | Accepted — narrows E0-14's "no personal data at all" by exactly one field; every address is at an RFC 2606 reserved domain, and E1 cannot build a name display against this platform |
| [0051](0051-a-disagreeing-score-maximum-is-refused-rather-than-rescaled.md) | A disagreeing `scoreMaximum` is refused rather than rescaled | Accepted — a **deliberate narrowing of AGS 2.0**, which permits the mismatch and expects the platform to scale; it is what keeps E0-15's no-rescale ruling from producing a wrong grade in silence, and what E3 must learn is to post against the line item's own maximum |
| [0052](0052-an-equal-score-timestamp-is-accepted-as-a-retry.md) | An equal score timestamp is accepted as a retry | Accepted — fills a gap AGS leaves rather than narrowing it, and so runs opposite to [0051](0051-a-disagreeing-score-maximum-is-refused-rather-than-rescaled.md) in the same round; the deciding case is E3 re-sending an identical body after a network timeout, and what follows is that `409` means "stop retrying and re-read" |
| [0053](0053-the-gateway-speaks-openai-through-pydantic-ai.md) | The gateway reaches an OpenAI-compatible endpoint through `pydantic-ai-slim[openai]` | Accepted — Todd's decision, 2026-08-18, and **SPEC §7.4's sentence stands**: the spec's library, without the eight extras that put `anthropic`, `google-genai`, `mcp`, `logfire`, `keyring` and D-Bus in the image handling student comment text (30 packages against 100, resolved rather than estimated). Carries three decisions of its own — the model is asked for the contract *minus* the audit pair, output is native JSON rather than a tool call, and the library's feedback retry is off because it would append a message after the student's comment |
| [0054](0054-a-floored-classification-names-the-floor-in-its-audit-pair.md) | A fail-open classification names the floor in its prompt version and model ID | Accepted — `character-floor` / `no-model`, so §3.3's fail-open is visibly the submission being accepted rather than a classification silently skipped; the consequence is that a stored prompt version no longer always names a file under `app/ai/prompts/`; **amended 2026-08-18** — a provider reporting `"model": "no-model"` in its response envelope forged the floor's own marker, so the marker moved to the gateway, which refuses it from a provider, while the prompt half needs no guard because a provider never supplies one |
| [0055](0055-a-classification-row-names-its-task-and-no-comment.md) | A classification row names its task, carries its verdict as a token, and names no comment yet | Accepted — one table for verdicts whose vocabularies overlap (`nonsense` belongs to two tasks), closed per task by a check constraint `alembic check` cannot compare; SPEC §8's append-only becomes the grant `pulse_app` holds rather than a rule the next writer has to know |
| [0056](0056-only-a-timeout-fails-open.md) | What fails open, what raises, and what the provider URL may carry | Accepted, and **rewritten in its own ticket after a second review pass measured two of its rows wrong**: `httpx.ConnectTimeout` is a subclass of `TimeoutException`, so a blackholed route floored with zero requests reaching a server, and an HTTP 503 — the ordinary shape of a hosted outage — raised. The record now carries the whole taxonomy as a table: reached-and-did-not-classify floors, never-arrived and answered-about-the-request raise. It also refuses a credential in `AI_PROVIDER_BASE_URL` itself, which had bypassed both the transport rule and `SecretStr` masking |
| [0058](0058-the-mock-provider-publishes-its-registration-and-its-seed.md) | The mock provider publishes its registration and its seed as one mock-only document | Accepted — answers the gap E0-16's test author named: an authorization request needs a `client_id` and a `redirect_uri` the ticket never spells, and the two-hat person cannot be found by signing in. Keyed by protocol name rather than by column, which is where it differs from [0036](0036-the-mock-platform-publishes-its-registration-as-a-document.md); names the seven members E1 and E0-18 may depend on — `roles_claim` among them, since discovery publishes the claim's name only as one unlabelled entry of `claims_supported` — so a test can hold exactly them |
| [0059](0059-each-mock-ships-its-own-copy-of-the-standard-library-signer.md) | Each mock ships its own copy of the standard-library RSA signer | Accepted — extends [0035](0035-the-mock-platform-signs-with-standard-library-rsa.md)'s bound to `mock-idp/` on the same terms; the two copies are identical below the docstring and `diff` is the whole guarantee, which is a cost the record states rather than hides |
| [0060](0060-the-mock-provider-authenticates-a-seeded-subject.md) | The mock provider holds no password: it signs in a seeded subject, and refuses by door | Accepted — SPEC §2's door rule computed from a person's assignments, one predicate read by the form and by the handler; the service is a signing oracle for its own fake identities, exactly as [0038](0038-the-mock-platform-ships-in-the-base-compose-file.md) says of the platform |
| [0061](0061-a-session-states-roles-in-a-namespaced-claim.md) | A session states roles in one namespaced claim, and states no scope | Accepted — the claim E1 reads; purview stays a function of the supervision graph (SPEC §2.1), so no org node reaches a token and a real IdP's claim mapping stays visible work rather than skippable |
| [0062](0062-a-request-is-parsed-once-at-the-edge.md) | A request is parsed once, at the edge, and nothing downstream re-derives it | Accepted — written after three review rounds found the same shape five times: a value transformed between the wire and the check meant to judge it. Bounded to `mock-idp/`; the mirrored question on the client side is E1's, and only the rule about echo semantics carries over. Rule 1 has a gate — an AST sweep for six normalising calls, permitting four shapes — whose three limits the record states |
| [0063](0063-the-demo-seed-runs-only-in-a-development-environment.md) | The demo seed runs only where `ENVIRONMENT` is `development` | Accepted — an equality rather than a deny-list, checked before a URL is built; surrounding whitespace stripped and case not folded, the first deliberate and the second inherited, both now pinned. The guard shipped with nothing in the suite executing it, said so, and ten tests now do. Whether `.env` may supply the name when the process environment does not was disputed ([E0-17-01](../disputes/E0-17-01.md)), found spec-silent, and settled by Todd in favour of resolved configuration; the residual gap — address from the process, permission from the file — is accepted by decision rather than closed |
| [0064](0064-the-demo-seed-is-idempotent-by-natural-key.md) | The demo seed is idempotent by matching natural keys, not by reloading | Accepted — a key must be scoped to a row the seed created or be a value it invented; `prefix.code` was neither and adopted a real institution's prefix, measured, so the seed now refuses rather than matching there. `role_assignment` has no `UNIQUE` constraint at all and its key is the seed's own property |
| [0065](0065-the-demo-institution-registers-a-fictional-platform.md) | The demo institution registers a fictional platform, and not the mock LMS | Accepted; second half superseded by [0068](0068-the-demo-seed-registers-the-mock-platform-behind-its-guard.md) — the fictional registration for the demo's own people stands, but the mock is registered too as of E0-31, so this no longer leaves [0038](0038-the-mock-platform-ships-in-the-base-compose-file.md) unamended |
| [0066](0066-seeded-people-are-named-for-what-they-do.md) | Seeded people are named for what they do, not for who they might be | Accepted — disagrees with `design/` deliberately, as the course numbers already do; nothing enforces it and the record says so |
| [0067](0067-schema-drift-outside-base-metadata-is-caught-by-a-probe-schema.md) | Drift in what `alembic check` cannot see is caught by rebuilding the model into a probe schema | Accepted — Postgres deparses both sides so no normaliser is hand-written; the grant expectation stays hand-derived per entry 19, and a grant added to a `views_sql` file is still a review control ([0041](0041-a-read-view-ships-as-an-immutable-versioned-sql-file.md)) |
| [0068](0068-the-demo-seed-registers-the-mock-platform-behind-its-guard.md) | The demo seed registers the mock platform, behind the guard it already had | Accepted — supersedes the second half of [0065](0065-the-demo-institution-registers-a-fictional-platform.md) and amends [0038](0038-the-mock-platform-ships-in-the-base-compose-file.md), whose fourth property now names the `ENVIRONMENT` guard rather than the absence of the row; E0-18 can launch, and the reviewer's grep is replaced by two questions the suite asserts |
| [0069](0069-three-rules-held-by-a-docstring-are-swept-out-of-the-source.md) | Three rules held by a docstring are swept out of the source, not hooked into the session | Accepted — Todd's decision, 2026-08-18; one mechanism in the shape of the read-side sweep, pointed at [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md)'s marker, [0021](0021-a-sections-derived-calendar-has-one-writer.md)'s single writer and [0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md)'s chokepoint. The session-level hook is rejected on cost and failure mode, and its real advantage — the indirect writes — is stated. Three limits travel with it, and it does not close the seam: the proof-shaped instrument is the grant [0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md) defers to E1. Carries two open questions, each with a done-when: how a *sanctioned* writer satisfies "calls `guard_write`" is E1's by Todd's decision, 2026-08-19; and the floor protects only the tables it names, so a table added to `LMS_OWNED_TABLES` above it can be removed again in silence |
| [0070](0070-the-expensive-gates-short-circuit-rather-than-skip.md) | The expensive gates short-circuit on a documentation-only diff, rather than being skipped | Accepted — E0-38. Settled by [0002](0002-ci-gates-ship-tolerant.md)'s amended consequence and E0-36 item 1: the aggregate check now treats `skipped` as a failure, verified against the real pipeline, so a job-level `if:` would fail the required check on every documentation-only pull request. Records why the parsed-document exception is hand-listed rather than derived — a derived one would make its own guard unable to fail (entry 19) — and what coverage the inert set gives up |
| [0071](0071-the-reveal-answers-only-a-committed-record.md) | The reveal answers only a record that is already committed | Accepted — E0-26 item 1, closing the one live gap in a guarantee the spec states rather than a missing assertion: a `pulse_care` caller could `BEGIN; SELECT reveal(…); ROLLBACK;` and keep the name with no audit row. The door becomes two calls and the second raises until the first is committed; `dblink` and a loopback `postgres_fdw` are rejected because each puts a credential inside a `SECURITY DEFINER` function, which is what [0043](0043-the-reveal-function-has-an-owner-of-its-own.md) exists to prevent. "Committed" is asked of `pg_xact_status`, not of `xmin` against the current transaction, which a `SAVEPOINT` defeats. Amends [0001](0001-identity-separation-by-database-role.md), [0042](0042-the-care-pool-has-its-own-credential-and-opens-on-first-use.md) and [0043](0043-the-reveal-function-has-an-owner-of-its-own.md), whose grant budget moves from three to four for the first time. States its price in both directions: a row means an access was *authorised* rather than one that certainly happened, and a committed record can be spent more than once, so the log under-records too — that half carried to E10 |
| [0072](0072-one-institution-is-a-unique-index-on-a-constant.md) | The single-institution rule is a unique index on a constant expression | Accepted — E0-22, building the constraint SPEC §8 asks for and [0017](0017-prefix-codes-are-unique-across-the-deployment.md) rested on as an assumption. A check constraint cannot count its own table, so the rule is `UNIQUE ((true))`, whose collision names the institution rather than a prefix code three tables away. The `singleton boolean` column is rejected on the measurement that made it look safer: `alembic check` compares this index in both directions, expression included, so the extra column buys nothing. Reaches further than a schema edit looks — the demo seed can no longer run beside a real institution at all, and 41 tests that built an institution per containment chain now share one |
