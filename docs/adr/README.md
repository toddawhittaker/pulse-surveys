# Architecture decision records

A record of construction decisions that [`docs/SPEC.md`](../SPEC.md) does not
answer and that a reasonable engineer might have made differently.

## When to write one

Both halves have to be true: the spec is silent, **and** the choice is genuinely
contestable. Choosing a JSON library needs no record. Choosing how identity
separation is enforced in the database does.

Write it in the same pull request as the decision, not afterwards. An ADR
written later is a reconstruction, and reconstructions leave out the option that
seemed obvious at the time and turned out to be wrong.

## When not to write one

- **The spec already decides it.** Link to the spec section instead. An ADR that
  paraphrases a spec section is noise that makes the real ones harder to find.
- **The decision contradicts the spec.** An ADR is not sufficient and not the
  right instrument. Raise it, and update the spec — a record of having gone
  around the spec is not the same as the spec being right.

## Format

`NNNN-slug.md`, four sections, under a page:

1. **Context** — what forced a choice, and which spec section left it open.
2. **Decision** — what was chosen, stated plainly.
3. **Alternatives rejected** — each with the reason it lost. This is the section
   that earns the document; a list of alternatives with no reasoning is a list.
4. **Consequences** — what this costs, what it constrains later, and what has to
   be true for it to keep working.

Number sequentially, never reuse a number, never renumber. **Add a row to the
table below in the same commit** — an unindexed record is one nobody finds, and
three of them accumulated on one branch before anyone noticed, because the index
is the artifact nobody re-reads once it exists.

**0029, 0033 and 0034 do not exist, and no record is missing.** Three tickets
were built in parallel worktrees and each was given a range to number within —
0025–0029, 0030–0034, 0035–0039 — so that two branches could not both claim one
number. Each ticket used fewer than its range allowed and the remainder was left
unused rather than back-filled, because renumbering is the one thing this file
forbids. A gap in the sequence means a range ran out early, never that a record
was lost.

The same partition was made again on **2026-08-22** for Batch I's parallel
worktrees: 0077 to E0-39, 0078 and 0079 to E0-42. Each row below is added by the
ticket that owns the number, and E0-42 owns this file, so 0077's row was written
here before its record existed. Both branches have merged since (2026-08-23), and
0077's row carries its link like every other.

A superseded record stays where it is with a line at the top pointing at its
replacement. Where a later decision changes only *part* of an earlier one, say
amended rather than superseded, on the earlier record's status line and beside
the specific paragraph that moved — and in the table, so the qualifier is
visible without opening the file. Calling a record superseded when its decision
still stands sends a reader looking for a replacement that does not exist.

## Records

| # | Decision | Status |
|---|---|---|
| [0001](0001-identity-separation-by-database-role.md) | Identity separation enforced by database role and grant | Accepted — one consequence amended by [0009](0009-a-superuser-identity-is-sanctioned-for-migrations-and-bootstrap.md) and again by [0040](0040-pulse-migrate-is-the-bootstrap-identity-under-another-name.md), which also resolves `pulse_migrate`; [0043](0043-the-reveal-function-has-an-owner-of-its-own.md) adds the definer role it does not name |
| [0002](0002-ci-gates-ship-tolerant.md) | CI gates ship tolerant and name the ticket that enforces them | Accepted, recorded retroactively |
| [0003](0003-deferred-authz-seams-fail-closed.md) | Deferred authorization seams fail closed by raising | Accepted |
| [0004](0004-agent-roster-mechanism.md) | Agent roster mechanism: hooks, computed gating, session-scoped warmth | Accepted — roster split amended 2026-08-28: four per-PR reviewers moved to the epic boundary, fifteen agents and the mechanism unchanged |
| [0005](0005-dependency-locking.md) | Python dependencies are locked with pip-compile, hashes and all | Accepted |
| [0006](0006-settings-lifetime.md) | Settings are built inside `create_app()` and hung on `app.state` | Accepted |
| [0007](0007-container-images-pinned-by-tag-and-digest.md) | Container images are pinned by tag and by digest | Accepted |
| [0008](0008-env-has-two-readers-and-the-database-credential-is-split.md) | `.env` has two readers, and the database credential is split into parts | Accepted — the count amended to three by [0012](0012-the-migration-environment-builds-its-own-superuser-connection.md) and to four by E0-17's `scripts/seed.py`; everything else the record decides is unchanged both times |
| [0009](0009-a-superuser-identity-is-sanctioned-for-migrations-and-bootstrap.md) | A superuser identity is sanctioned for migrations and bootstrap | Accepted — one row of its provisioning table amended by [0040](0040-pulse-migrate-is-the-bootstrap-identity-under-another-name.md) |
| [0010](0010-the-celery-application-is-built-at-import-time.md) | The Celery application is built at import time, at module level | Accepted — answers for Celery the entry-point question [0006](0006-settings-lifetime.md) left open |
| [0011](0011-ci-validates-the-image-by-running-the-base-compose-file-alone.md) | CI validates the image by running the base Compose file alone | Accepted |
| [0012](0012-the-migration-environment-builds-its-own-superuser-connection.md) | The migration environment builds its own superuser connection | Accepted — settles the two open rows in [0009](0009-a-superuser-identity-is-sanctioned-for-migrations-and-bootstrap.md)'s provisioning table |
| [0013](0013-the-database-session-is-synchronous.md) | The database session is synchronous, and the engine is built at import | Accepted — answers for the database the entry-point question [0006](0006-settings-lifetime.md) left open. **Argument corrected in E0-37 item 8, decision unchanged**: it no longer cites [0010](0010-the-celery-application-is-built-at-import-time.md) as precedent, since Celery's import-time build is forced by `celery -A` resolving an attribute while FastAPI's is a choice, and no longer rejects a lazy engine partly on a test written in the same ticket. The alternative it never weighed — the engine on `app.state` inside `create_app()`, Celery keeping its own — is now named and not adopted |
| [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md) | LMS-owned columns are marked by an `lms_` name prefix | Accepted, **amended by E0-35, 2026-08-19** — E0-11 chose a table grain instead of the marker's ([0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md)), and E0-35's sweep now enforces the direction this record could not, at table grain. The marker is documentation, and "the chokepoint must be able to read it" — one of the two reasons for a name over an `info={}` dict — is void. The decision stands on the other reason; the closure reaches LMS-owned tables and nowhere else |
| [0015](0015-course-level-is-a-stored-generated-column.md) | Course level is a stored generated column, and the bands are its only authority | Accepted |
| [0016](0016-primary-keys-are-database-generated-uuids.md) | Primary keys are database-generated UUIDs | Accepted — UUIDv7 revisited when the Postgres image moves to 18 |
| [0017](0017-prefix-codes-are-unique-across-the-deployment.md) | Prefix codes are unique across the deployment, not per institution | Accepted — rests on one institution per deployment, which was stated in the record as a latent assumption and is a rule since 2026-08-20 ([0072](0072-one-institution-is-a-unique-index-on-a-constant.md)); the consequence about a confusing `uq_prefix_code` violation is retired with it |
| [0018](0018-cross-table-length-rules-are-enforced-by-a-composite-foreign-key.md) | Cross-table length rules are enforced by a composite foreign key carrying the term's length | Accepted |
| [0019](0019-a-naive-datetime-is-refused-by-the-column-type.md) | A naive datetime is refused by the column type, not by Postgres or a service | Accepted |
| [0020](0020-a-sections-end-date-is-its-last-day.md) | A section's end date is its last day, inclusive | Accepted |
| [0021](0021-a-sections-derived-calendar-has-one-writer.md) | A section's derived calendar is NOT NULL and has exactly one writer | Accepted, **amended by E0-35, 2026-08-19** — "exactly one writer" was convention when this was written and now has a sweep behind it, at module grain rather than the function grain the record names. The sweep is syntactic, so a second writer reached through a helper in another module or an ORM cascade is still invisible, and its own positive control is what the claim rests on |
| [0022](0022-identity-bearing-columns-are-marked-by-a-name-prefix.md) | Identity-bearing columns are marked by an `identity_` name prefix | Accepted — follows [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md), and takes the name where the two markers meet; the discovery rule beside it widened in E0-10, the marker itself unchanged |
| [0023](0023-overlapping-enrollments-are-refused-by-an-exclusion-constraint.md) | Overlapping enrollment windows are refused, by a GiST exclusion constraint | Accepted — settles the choice E0-08's criterion 5 leaves open |
| [0024](0024-the-person-to-user-link-is-carried-by-person.md) | The person-to-user link is carried by `person`, and is one to one | Accepted — rests on one registered platform per person, stated in the record |
| [0025](0025-an-assignments-scope-is-one-nullable-foreign-key-per-level.md) | An assignment's scope is one nullable foreign key per containment level | Accepted — settled what SPEC §8's singular `scope_node_id` referenced, and **§8 has since been rewritten to state the five columns itself** (`58064c4`, 2026-08-18). The record's context quotes the spec as it was; it is amended in place, 2026-08-22, rather than corrected, so the reason the choice was open is still readable |
| [0026](0026-entry-doors-are-derived-from-the-role-as-generated-columns.md) | Entry doors are derived from the role, as stored generated columns | Accepted — follows [0015](0015-course-level-is-a-stored-generated-column.md) |
| [0027](0027-supervision-edges-are-policed-by-one-row-level-trigger.md) | Supervision edges are policed by one row-level trigger, holding an advisory lock | Accepted — takes the trigger [0015](0015-course-level-is-a-stored-generated-column.md) rejected, for a rule that spans rows; every relation schema-qualified against a `pg_temp` hijack; holds under READ COMMITTED and SERIALIZABLE, and refuses REPEATABLE READ rather than being silently wrong under it; extended by [0044](0044-a-supervision-edge-must-climb-the-role-rank.md), which added two rules to the same function and made the cycle walk defence in depth |
| [0028](0028-a-student-holds-no-role-assignment.md) | A student holds no role assignment | Accepted — a student's access is resolved from `enrollment`. It contradicted SPEC §2.1, which listed Student among the roles a person holds an assignment for; **§2.1 has since been edited to agree** (2026-08-23, Todd's decision on the contradiction PR #68 raised), so the rule is spec now and what stays this record's own is the enum without a `STUDENT` label and the scope-grain constraint that makes such a row unwritable. Amended in place, 2026-08-23 |
| [0030](0030-a-verdict-is-an-enum-whose-value-is-the-stored-token.md) | A verdict is an `enum.Enum` whose value is the stored token | Accepted |
| [0031](0031-every-task-contract-carries-the-prompt-version-and-model-id.md) | Every task contract carries the prompt version and model ID, and the gateway supplies them | Accepted — reads E0-12's "every model" over §7.4's narrower "every classification" |
| [0032](0032-a-prompt-file-is-immutable-once-a-classification-cites-it.md) | Prompts are named `<task>.v<N>.md` and a committed prompt file is never edited | Accepted — half enforced by test, half convention, said so in the record |
| [0035](0035-the-mock-platform-signs-with-standard-library-rsa.md) | The mock platform signs with standard-library RSA | Accepted — bounded to `mock-lms/`, and the bound is part of the decision |
| [0036](0036-the-mock-platform-publishes-its-registration-as-a-document.md) | The mock platform publishes its registration as a document keyed by column | Accepted. **Closed in part 2026-08-25 by E1-06**: the consequence saying there is deliberately no `token_endpoint` "until E0-15 builds the Advantage services" describes a state that is over — the document states `auth_token_url` now, and the rule that nothing is advertised unless it is served is what made the four parts land together |
| [0037](0037-the-mock-platform-is-configured-by-compose-literals.md) | The mock platform is configured by Compose literals, and earns no `.env.example` entry | Accepted — applies the rule in [0008](0008-env-has-two-readers-and-the-database-credential-is-split.md); E0-16 reached the same answer for `mock-idp`, so it now covers both mocks. **Amended 2026-08-25 by E1-06**: the platform reads six values, not five — `MOCK_LMS_TOOL_JWKS_URL` joined them as a literal on the same terms, and the decision is unchanged |
| [0038](0038-the-mock-platform-ships-in-the-base-compose-file.md) | The mock platform ships in the base Compose file, and is kept out of a deployment by what it holds | Accepted |
| [0039](0039-the-two-app-packages-are-typechecked-in-two-runs.md) | The two `app` packages are typechecked in two mypy runs | Accepted — three runs since E0-16 added `mock-idp/app`; the record generalises rather than being superseded |
| [0040](0040-pulse-migrate-is-the-bootstrap-identity-under-another-name.md) | `pulse_migrate` is the bootstrap identity under another name, and is not created | Accepted — amends one row of [0009](0009-a-superuser-identity-is-sanctioned-for-migrations-and-bootstrap.md)'s provisioning table and resolves the third role named in [0001](0001-identity-separation-by-database-role.md), its own role count amended by [0043](0043-the-reveal-function-has-an-owner-of-its-own.md) |
| [0041](0041-a-read-view-ships-as-an-immutable-versioned-sql-file.md) | A read view ships as an immutable, versioned `.sql` file that a migration executes | Accepted — follows [0032](0032-a-prompt-file-is-immutable-once-a-classification-cites-it.md); the layout is a decision this record holds and the suite does not enforce, measured and said so |
| [0042](0042-the-care-pool-has-its-own-credential-and-opens-on-first-use.md) | The Care pool has a credential of its own, and opens on first use | Accepted — answers for the Care queue the entry-point question [0006](0006-settings-lifetime.md) left open; one rejected alternative reversed by E0-10, so `api` alone holds the credential and `CARE_DATABASE_URL` is optional |
| [0043](0043-the-reveal-function-has-an-owner-of-its-own.md) | The reveal function has an owner of its own, holding a readable list of grants | Accepted — amends the role count in [0040](0040-pulse-migrate-is-the-bootstrap-identity-under-another-name.md); the views deliberately keep the migration identity, and the record says what that leaves open. **The list was three and is four since E0-26** ([0071](0071-the-reveal-answers-only-a-committed-record.md)): the reveal reads its subject out of a committed `audit_log` row, so the definer holds `SELECT` there too. The rule that the owner holds exactly what the bodies do is what makes that a decision rather than a drift |
| [0044](0044-a-supervision-edge-must-climb-the-role-rank.md) | A supervision edge must climb the role rank, and the trigger is what refuses one that does not | Accepted — extends [0027](0027-supervision-edges-are-policed-by-one-row-level-trigger.md); settles the choice E0-09 left in neither place; bounds the graph at six deep and subsumes acyclicity; the same-role half was disputed and upheld ([E0-11-01](../disputes/E0-11-01.md)), the ruling finding that `LEAD_FACULTY → LEAD_FACULTY` is refused by §4.1 invariant 2 and every other same-role pair was spec-silent. **Spec-silent no longer**: `fd703bb` (2026-08-17) put the climbing rule, the same-role refusal, `ADMIN`'s and `CARE`'s absence and the six-deep bound into SPEC §2.1 in as many words, so what stays this record's own is where the rule is enforced — the trigger rather than the resolver. Amended in place, 2026-08-22 |
| [0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md) | The chokepoint refuses an LMS-owned write at table grain, plus one row | Accepted — chooses the grain E0-11 refuses to inherit from [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md)'s marker, and says what it does not catch. Two consequences moved on 2026-08-19: E0-35 closed [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md)'s open half on the guarded tables with a sweep beside this chokepoint rather than a change to it, and put a tripwire on "a caller can bypass it by not calling it" ([0069](0069-three-rules-held-by-a-docstring-are-swept-out-of-the-source.md)). How a *sanctioned* writer satisfies that tripwire was E1's by Todd's decision, and [0090](0090-a-sanctioned-writer-passes-the-chokepoint-by-being-in-a-catalog.md) answered it in E1-10 (2026-08-26), which also spent the narrowed form of the grant this record deferred |
| [0046](0046-a-purview-is-a-materialised-node-set-per-containment-level.md) | A purview is a materialised, downward-closed node set per containment level | Accepted — the value every read path is handed; Care is deliberately not one of its fields, and the deferred union it composes with is [0003](0003-deferred-authz-seams-fail-closed.md)'s |
| [0047](0047-the-posted-score-readback-is-a-mock-only-route.md) | The posted-score readback is a mock-only route, not a widened AGS `Result` | Accepted — Todd's decision, 2026-08-17; a conformant `Result` has no room for the fields E0-15's criterion 4 names, so the inspection surface sits outside the AGS namespace and nothing teaches E3 to expect them from a platform |
| [0048](0048-enrollment-windows-ride-on-a-namespaced-nrps-extension.md) | Enrollment windows ride on a namespaced NRPS member extension | Accepted — Todd's decision, 2026-08-17; NRPS 2.0 defines no date on a member while SPEC §3.4 and §9.2 both read one from NRPS, so the mock supplies it the way a real platform does. **What a platform supplying none should do was settled by Todd on 2026-08-21 in SPEC §3.4**, not here: such a student counts as enrolled from the section's start, unless they first appear in a roster sync later than the section's first sync, in which case the denominator starts at that sync's week. E0-28 item 1 seeds one member carrying no window, so the branch exists in a fixture. The record's own body was amended in the same pull request (#64) |
| [0049](0049-the-mock-gradebook-is-per-application-state-in-memory.md) | The mock platform's gradebook is per-application state, held in memory | Accepted — bounded to `mock-lms/`, like [0035](0035-the-mock-platform-signs-with-standard-library-rsa.md); the log grows unbounded and the service cannot be run at two replicas, both said in the record |
| [0050](0050-the-mock-roster-exposes-an-address-and-no-name.md) | The mock roster exposes an address and no name | Accepted — narrows E0-14's "no personal data at all" by exactly one field; every address is at an RFC 2606 reserved domain, and E1 cannot build a name display against this platform |
| [0051](0051-a-disagreeing-score-maximum-is-refused-rather-than-rescaled.md) | A disagreeing `scoreMaximum` is refused rather than rescaled | Accepted — a **deliberate narrowing of AGS 2.0**, which permits the mismatch and expects the platform to scale; it is what keeps E0-15's no-rescale ruling from producing a wrong grade in silence, and what E3 must learn is to post against the line item's own maximum |
| [0052](0052-an-equal-score-timestamp-is-accepted-as-a-retry.md) | An equal score timestamp is accepted as a retry | Accepted — fills a gap AGS leaves rather than narrowing it, and so runs opposite to [0051](0051-a-disagreeing-score-maximum-is-refused-rather-than-rescaled.md) in the same round; the deciding case is E3 re-sending an identical body after a network timeout, and what follows is that `409` means "stop retrying and re-read" |
| [0053](0053-the-gateway-speaks-openai-through-pydantic-ai.md) | The gateway reaches an OpenAI-compatible endpoint through `pydantic-ai-slim[openai]` | Accepted — Todd's decision, 2026-08-18, and **SPEC §7.4's sentence stands**: the spec's library, without the eight extras that put `anthropic`, `google-genai`, `mcp`, `logfire`, `keyring` and D-Bus in the image handling student comment text (30 packages against 100, resolved rather than estimated). Carries three decisions of its own — the model is asked for the contract *minus* the audit pair, output is native JSON rather than a tool call, and the library's feedback retry is off because it would append a message after the student's comment |
| [0054](0054-a-floored-classification-names-the-floor-in-its-audit-pair.md) | A fail-open classification names the floor in its prompt version and model ID | Accepted — `character-floor` / `no-model`, so §3.3's fail-open is visibly the submission being accepted rather than a classification silently skipped; the consequence is that a stored prompt version no longer always names a file under `app/ai/prompts/`; **amended 2026-08-18** — a provider reporting `"model": "no-model"` in its response envelope forged the floor's own marker, so the marker moved to the gateway, which refuses it from a provider, while the prompt half needs no guard because a provider never supplies one |
| [0055](0055-a-classification-row-names-its-task-and-no-comment.md) | A classification row names its task, carries its verdict as a token, and names no comment yet | Accepted — one table for verdicts whose vocabularies overlap (`nonsense` belongs to two tasks), closed per task by a check constraint `alembic check` cannot compare; SPEC §8's append-only becomes the grant `pulse_app` holds rather than a rule the next writer has to know · **amended 2026-09-01 (E2-08)**: the promised `answer_id` landed **nullable** rather than `NOT NULL`, because the rows written before E2-08 have nothing to backfill from; its `ON DELETE RESTRICT` is what [0115](0115-a-resubmission-revises-its-answers-in-place.md) is written about |
| [0056](0056-only-a-timeout-fails-open.md) | What fails open, what raises, and what the provider URL may carry | Accepted, and **rewritten in its own ticket after a second review pass measured two of its rows wrong**: `httpx.ConnectTimeout` is a subclass of `TimeoutException`, so a blackholed route floored with zero requests reaching a server, and an HTTP 503 — the ordinary shape of a hosted outage — raised. The record now carries the whole taxonomy as a table: reached-and-did-not-classify floors, never-arrived and answered-about-the-request raise. It also refuses a credential in `AI_PROVIDER_BASE_URL` itself, which had bypassed both the transport rule and `SecretStr` masking. **Amended by E0-37**: item 12 closed the keyless-cleartext allowance the record used to name as accepted — off this machine means `https` with or without a credential — and item 13 recorded why 429 and 500 stay outside the floor. **Amended 2026-08-31**: pydantic-ai moved its OpenAI client from `httpx` to `httpx2` at 2.32, and the fork keeps the class names while being different class objects, so the check matched nothing the new transport raised and a read timeout raised instead of flooring; the rule now names the classes of both packages. The taxonomy is unchanged by either amendment |
| [0058](0058-the-mock-provider-publishes-its-registration-and-its-seed.md) | The mock provider publishes its registration and its seed as one mock-only document | Accepted — answers the gap E0-16's test author named: an authorization request needs a `client_id` and a `redirect_uri` the ticket never spells, and the two-hat person cannot be found by signing in. Keyed by protocol name rather than by column, which is where it differs from [0036](0036-the-mock-platform-publishes-its-registration-as-a-document.md); names the seven members E1 and E0-18 may depend on — `roles_claim` among them, since discovery publishes the claim's name only as one unlabelled entry of `claims_supported` — so a test can hold exactly them |
| [0059](0059-each-mock-ships-its-own-copy-of-the-standard-library-signer.md) | Each mock ships its own copy of the standard-library RSA signer | Accepted — extends [0035](0035-the-mock-platform-signs-with-standard-library-rsa.md)'s bound to `mock-idp/` on the same terms; the two copies are identical below the docstring and `diff` is the whole guarantee, which is a cost the record states rather than hides |
| [0060](0060-the-mock-provider-authenticates-a-seeded-subject.md) | The mock provider holds no password: it signs in a seeded subject, and refuses by door | Accepted — SPEC §2's door rule computed from a person's assignments, one predicate read by the form and by the handler; the service is a signing oracle for its own fake identities, exactly as [0038](0038-the-mock-platform-ships-in-the-base-compose-file.md) says of the platform |
| [0061](0061-a-session-states-roles-in-a-namespaced-claim.md) | A session states roles in one namespaced claim, and states no scope | Accepted — the claim E1 reads; purview stays a function of the supervision graph (SPEC §2.1), so no org node reaches a token and a real IdP's claim mapping stays visible work rather than skippable |
| [0062](0062-a-request-is-parsed-once-at-the-edge.md) | A request is parsed once, at the edge, and nothing downstream re-derives it | Accepted — written after three review rounds found the same shape five times: a value transformed between the wire and the check meant to judge it. Bounded to `mock-idp/`; the mirrored question on the client side is E1's, and only the rule about echo semantics carries over. Rule 1 has a gate — an AST sweep for six normalising calls, permitting four shapes — whose three limits the record states |
| [0063](0063-the-demo-seed-runs-only-in-a-development-environment.md) | The demo seed runs only where `ENVIRONMENT` is `development` | Accepted — an equality rather than a deny-list, checked before a URL is built; surrounding whitespace stripped and case not folded, the first deliberate and the second inherited, both now pinned. The guard shipped with nothing in the suite executing it, said so, and ten tests now do. Whether `.env` may supply the name when the process environment does not was disputed ([E0-17-01](../disputes/E0-17-01.md)), found spec-silent, and settled by Todd in favour of resolved configuration; the residual gap — address from the process, permission from the file — is accepted by decision rather than closed |
| [0064](0064-the-demo-seed-is-idempotent-by-natural-key.md) | The demo seed is idempotent by matching natural keys, not by reloading | Accepted — a key must be scoped to a row the seed created or be a value it invented; `prefix.code` was neither and adopted a real institution's prefix, measured, so the seed now refuses rather than matching there. `role_assignment` has no `UNIQUE` constraint at all and its key is the seed's own property |
| [0065](0065-the-demo-institution-registers-a-fictional-platform.md) | The demo institution registers a fictional platform, and not the mock LMS | Accepted; second half superseded by [0068](0068-the-demo-seed-registers-the-mock-platform-behind-its-guard.md) — the fictional registration for the demo's own people stands, but the mock is registered too as of E0-31, so this no longer leaves [0038](0038-the-mock-platform-ships-in-the-base-compose-file.md) unamended |
| [0066](0066-seeded-people-are-named-for-what-they-do.md) | Seeded people are named for what they do, not for who they might be | Accepted — disagrees with `design/` deliberately, as the course numbers already do; nothing enforces it and the record says so |
| [0067](0067-schema-drift-outside-base-metadata-is-caught-by-a-probe-schema.md) | Drift in what `alembic check` cannot see is caught by rebuilding the model into a probe schema | Accepted — Postgres deparses both sides so no normaliser is hand-written; the grant expectation stays hand-derived per entry 19, and a grant added to a `views_sql` file is still a review control ([0041](0041-a-read-view-ships-as-an-immutable-versioned-sql-file.md)) |
| [0068](0068-the-demo-seed-registers-the-mock-platform-behind-its-guard.md) | The demo seed registers the mock platform, behind the guard it already had | Accepted — supersedes the second half of [0065](0065-the-demo-institution-registers-a-fictional-platform.md) and amends [0038](0038-the-mock-platform-ships-in-the-base-compose-file.md), whose fourth property now names the `ENVIRONMENT` guard rather than the absence of the row; E0-18 can launch, and the reviewer's grep is replaced by two questions the suite asserts |
| [0069](0069-three-rules-held-by-a-docstring-are-swept-out-of-the-source.md) | Three rules held by a docstring are swept out of the source, not hooked into the session | Accepted — Todd's decision, 2026-08-18; one mechanism in the shape of the read-side sweep, pointed at [0014](0014-lms-owned-columns-are-marked-by-a-name-prefix.md)'s marker, [0021](0021-a-sections-derived-calendar-has-one-writer.md)'s single writer and [0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md)'s chokepoint. The session-level hook is rejected on cost and failure mode, and its real advantage — the indirect writes — is stated. Three limits travel with it, and it does not close the seam: the proof-shaped instrument is the grant [0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md) defers to E1. Carried two open questions, each with a done-when. The first — how a *sanctioned* writer satisfies "calls `guard_write`" — is **closed by [0090](0090-a-sanctioned-writer-passes-the-chokepoint-by-being-in-a-catalog.md) in E1-10, 2026-08-26**: the writer calls the guard with a sanction a catalog backs, the sweep's rule is restated rather than softened, no exclusion list was added, and the live assertion stops being vacuous because there is finally a writer to find. Still open: the floor protects only the tables it names, so a table added to `LMS_OWNED_TABLES` above it can be removed again in silence |
| [0070](0070-the-expensive-gates-short-circuit-rather-than-skip.md) | The expensive gates short-circuit on a documentation-only diff, rather than being skipped | Accepted — E0-38. Settled by [0002](0002-ci-gates-ship-tolerant.md)'s amended consequence and E0-36 item 1: the aggregate check now treats `skipped` as a failure, verified against the real pipeline, so a job-level `if:` would fail the required check on every documentation-only pull request. Records why the parsed-document exception is hand-listed rather than derived — a derived one would make its own guard unable to fail (entry 19) — and what coverage the inert set gives up |
| [0071](0071-the-reveal-answers-only-a-committed-record.md) | The reveal answers only a record that is already committed | Accepted — E0-26 item 1, closing the one live gap in a guarantee the spec states rather than a missing assertion: a `pulse_care` caller could `BEGIN; SELECT reveal(…); ROLLBACK;` and keep the name with no audit row. The door becomes two calls and the second raises until the first is committed; `dblink` and a loopback `postgres_fdw` are rejected because each puts a credential inside a `SECURITY DEFINER` function, which is what [0043](0043-the-reveal-function-has-an-owner-of-its-own.md) exists to prevent. "Committed" is asked of `pg_xact_status`, not of `xmin` against the current transaction, which a `SAVEPOINT` defeats. Amends [0001](0001-identity-separation-by-database-role.md), [0042](0042-the-care-pool-has-its-own-credential-and-opens-on-first-use.md) and [0043](0043-the-reveal-function-has-an-owner-of-its-own.md), whose grant budget moves from three to four for the first time. States its price in both directions: a row means an access was *authorised* rather than one that certainly happened, and a committed record can be spent more than once, so the log under-records too — that half carried to E10 |
| [0072](0072-one-institution-is-a-unique-index-on-a-constant.md) | The single-institution rule is a unique index on a constant expression | Accepted — E0-22, building the constraint SPEC §8 asks for and [0017](0017-prefix-codes-are-unique-across-the-deployment.md) rested on as an assumption. A check constraint cannot count its own table, so the rule is `UNIQUE ((true))`, whose collision names the institution rather than a prefix code three tables away. The `singleton boolean` column is rejected on the measurement that made it look safer: `alembic check` compares this index in both directions, expression included, so the extra column buys nothing. Reaches further than a schema edit looks — the demo seed can no longer run beside a real institution at all, and 41 tests that built an institution per containment chain now share one |
| [0073](0073-the-tool-verifies-launches-with-pyjwt-rather-than-adopting-pylti1p3.md) | The tool verifies both doors' tokens with PyJWT, and `pylti1p3` waits for E1 | Accepted — E0-18, the first ticket where a real token reaches real tool code. Nothing in the locked closure verified RS256, and [0035](0035-the-mock-platform-signs-with-standard-library-rsa.md)'s bound forbids copying the mocks' arithmetic into the tool. `pylti1p3` covers one of the two doors, needs a session-shaped framework adapter E0 has no session for, and is deferred rather than rejected: SPEC §13 still names it. `cryptography` is declared directly, because a bare PyJWT locks a closure that fails at the first launch rather than at install. States what is given up — deep linking, the message-type vocabulary, service-token grants, the §7.3 platform adapters — and that the web door will still need this verifier after E1 adopts the library |
| [0074](0074-the-openapi-schema-is-served-only-in-development.md) | `/docs` and `/openapi.json` get routes only when `ENVIRONMENT` is `development` | Accepted — E0-18. The schema is a complete route list, and §6.2's Care surface and §5.5's roll-ups are where knowing a URL exists is itself information. Gates the *route*, never the generation: `app.openapi()` still produces the document, which §7.1's MCP server and §13's client generator need, and a test holds all three halves. Introduces `app.config.DEVELOPMENT_ENVIRONMENT` as the third reader of that string; E0-37 item 2 migrated the other two onto it, so the value has one definition. Gating on the authenticated actor is the consistent answer and is rejected as E1-shaped, not as wrong |
| [0075](0075-the-two-doors-addresses-are-settings-defaulted-to-the-development-stack.md) | The two doors' addresses are settings, and every default is the development stack | Accepted — E0-18, settling E0-30 item 3. Seven fields, horizon decided per value rather than per service: browser-facing addresses are `localhost`, server-facing ones are Compose service names, and `OIDC_ISSUER` is neither because it is compared against a claim. Registration columns stay E1's per E0-23, and reading endpoints from discovery is rejected because one document cannot serve two horizons — a tool built that way passes every in-process test and sends a real browser to a container name. Records the rule it declines: `app/config.py` refuses working defaults for deployment wiring. **Amended 2026-08-23**: that declining is what no longer holds for the five `OIDC_*` fields, which [0077](0077-the-web-doors-identity-provider-is-named-explicitly.md) supersedes. `docker-compose.yml` starts `mock-idp` in every deployment, so `http://mock-idp:8000` does resolve off this stack and the argument for defaulting those five was false; they are required now, with no default, and their development values moved into the Compose file. The two launch-door fields keep their defaults on the narrower argument that neither names a service the base file starts, and the per-value horizon rule, the `OIDC_ISSUER` note and the public client with no secret all still stand. The rejected alternatives are left exactly as written, because that reasoning is the price 0077 pays rather than an argument that turned out wrong. **Superseded again 2026-08-25** by E1-05, for the LTI platform's authorization endpoint: this record's own last consequence predicted that a second registered platform would have its launches sent to the first one's address, and E1-05 makes it a registration column and deletes the setting rather than leaving it as a fallback. One of the two launch-door fields remains, and the per-value horizon rule now governs a column |
| [0076](0076-what-a-compose-file-may-say-is-a-closed-set.md) | What a Compose file may say is a closed set, over the merged configuration | Accepted, **amended before merge on 2026-08-21 by two of E0-19's own security passes**, each measuring the central claim wrong one level further in. It records the three rules E0-03 imposed (the top-level key set, the outright refusal of `extends:`, the root holding no Compose-named file this suite does not read) and six of E0-19's. The first pass: the *service* level was not a closed set at all, so `volumes_from: - db` handed `worker` the whole Postgres data directory with the suite green. The second: even the closed sets read each file on its own, and Docker merges the top-level `volumes:` across files, so a volume the base file mounts could be redefined into a host bind by the override with nothing looking — the correction that holds is merge first, then apply every rule. Five rules became nine, and two reverse decisions this record made (the privilege rule is absolute rather than a comparison against `api`; volume bodies are read merged rather than per file). One alternative for all nine — read whatever exists and run every rule over it — rejected because a section the module cannot interpret reports clean rather than red, and because modelling every Compose feature is an open-ended obligation the closed set turns into a per-feature decision. Guards the [0009](0009-a-superuser-identity-is-sanctioned-for-migrations-and-bootstrap.md) bound; the reasoning and the measurements stay in `tests/unit/test_compose_stack.py` |
| [0077](0077-the-web-doors-identity-provider-is-named-explicitly.md) | The web door's identity provider is named explicitly, and a mock address is refused outside development | Accepted — E0-39, Batch I; supersedes [0075](0075-the-two-doors-addresses-are-settings-defaulted-to-the-development-stack.md) in part. **Amended 2026-08-25 by E1-05**: where it says 0075's "two launch-door fields" still stand, one of them is gone — the LTI platform's authorization endpoint is a registration column now — and its four host helpers gained a second caller in `app.models.lti`, which borrows the vocabulary and none of the conclusions ([0081](0081-a-registrations-addresses-are-refused-at-one-write-time-chokepoint.md)) |
| [0078](0078-the-login-state-cookie-is-signed-per-process-and-short-lived.md) | The login state cookie is signed with a per-process key and lives five minutes | Accepted — E0-18's decision (PR #57), recorded by E0-42 from `app/api/deps.py`'s docstring. The consequence to know before scaling anything: the signing key is per process, so two `api` replicas cannot serve one login, and a shared or configured key has to land first |
| [0079](0079-the-developer-console-is-gated-in-the-handler.md) | The developer console is a real route in every environment, refused in the handler | Accepted — PR #62, no ticket; recorded by E0-42. `GET /dev` lists the mock provider's web-login people and offers each as a one-click sign-in link through the ordinary web door, so its gate is deliberately **not** [0074](0074-the-openapi-schema-is-served-only-in-development.md)'s route removal: the environment check sits in the handler, in the same function as the behaviour it refuses. The record names what that costs — the route is registered for `GET` in every environment, so a method mismatch answers `405` where an unregistered path answers `404` — and a later refactor that gates routers by environment must change this one deliberately rather than as cleanup. **Amended 2026-08-25 by E1-05** in its context only: the page's single launcher link became one per registered platform, read off `lti_platform.authorization_endpoint`, with an honest line where nothing is registered; the gate is untouched and the surface is slightly larger. **Amended 2026-08-26 by E1-14**: the `405` this record measured and left unasserted is now pinned as accepted disclosure, settling the open note in the decision and consequences sections; see [0087](0087-healthz-and-dev-keep-disclosing-the-environment.md) |
| [0080](0080-the-view-sweep-closes-by-lineage-and-enumeration.md) | The §4.1 view sweep closes by table lineage and a hand-written column enumeration | Accepted — E1-01, closing the carried entry's two measured blind spots. A view may read no column of `user`, `user_identity` or `person` beyond the structural join keys, whatever the column is called — applied file-side by resolving table aliases and catalog-side over `pg_depend`, with view-on-view chains folded to a fixed point, coarsely and fail-closed. What `pulse_app` may read from a view is an exact two-direction equality between the catalog and a sanctioned list written by hand from the record, views only; base tables keep their table-grain equality. Rejected: a longer fragment list (the label is the author's choice), column-level grants in the database (moves the expectation into the files it polices, and makes every future column a migration), and a SQL parser (precision the coarse fold does not need, since the base view of any chain is flagged directly) |
| [0081](0081-a-registrations-addresses-are-refused-at-one-write-time-chokepoint.md) | A registration's addresses are judged at one write-time chokepoint, by four rules | Accepted — E1-05, closing E0-24 item 1 (`jwks_url` was credential-equivalent and unconstrained) and constraining the two columns that ticket adds beside it. A validator and not a `CHECK`, because every rule reads `ENVIRONMENT` and the database does not hold it. Four rules, all off in development: https with the on-this-machine exemption; the Compose service `mock-lms` refused on all three columns as a parsed host rather than as the seeded URL; loopback refused on the browser-facing column only, as a class; link-local refused on the two this container fetches, both families, because the cloud metadata service lives there. **Superseded in part by [0101](0101-a-fetched-address-is-judged-by-what-it-resolves-to.md)**: this record accepted private ranges on every column and rejected `not ip.is_global` by name, and rule 5 reverses both — the price it states for the other direction, a deployment whose platform lives on a private address being unregistrable, is paid deliberately there. Borrows [0077](0077-the-web-doors-identity-provider-is-named-explicitly.md)'s four host helpers and none of its conclusions, and says which is which. States the residue [0068](0068-the-demo-seed-registers-the-mock-platform-behind-its-guard.md) states: a writer going round SQLAlchemy is not judged |
| [0082](0082-the-tools-signing-key-lives-in-the-database.md) | The tool's own signing key lives in a one-row database table | Accepted — E1-05. The deciding fact is that the `api` container and the celery worker are two processes and one tool, and a platform holds the public half of exactly one key: per-process generation and per-container files each give the two processes different keys, and the half that fails fails at the platform with an error about a signature. One row by the constant-expression unique index [0072](0072-one-institution-is-a-unique-index-on-a-constant.md) chose, emphatically not over the key material, which would permit any number of rows holding different keys. Only the private PEM is stored; the public half and the RFC 7638 `kid` are derived on read (entry 19). Seeded behind [0063](0063-the-demo-seed-runs-only-in-a-development-environment.md)'s guard, checked at the write; an existing key is kept, never rotated, because rotation is invisible at the moment it happens. Rejected: a PEM in a settings variable, a key file at a configured path, and generation per process. States two gaps by decision — no `pulse_app` grant until E1-06 spends the key, and no supply route outside development, which is `docs/tickets/e1/deferred.md`'s with a done-when |
| [0083](0083-node-lives-in-a-root-npm-workspace-with-frontend-as-a-member.md) | Node lives in a root npm workspace, with `frontend/` as a member | Accepted — E1-02, settling where Node lives before E1-04's scaffold moves the gates a second time. The root package declares `workspaces: ["frontend"]`, the member lands empty, and the end-to-end tooling stays where E0-40 pointed the gates. One `package-lock.json`, at the root, because two lockfiles resolving one package to two versions is `docs/MISTAKES.md` entry 25 and `typescript` and `@types/node` are exactly the overlap that would drift. The cost it names and pays: `frontend/package.json` is committed from now on, so the `frontend` detect probe stops reading its existence and reads the `build` script the job actually runs — narrower, not weaker, and emphatically not the gate flip, which stays E1-04's. Rejected: moving all Node under `frontend/` (every path E0-40 fixed three tickets ago moves again, for nothing this epic needs), two independent package trees, declaring the workspace without the member (defers the one thing this layout costs), and a no-op `build` script in the stub (turns two gates on over an empty tree, and `check_bundle_size.py` reports a missing `dist` and exits 0). **Amended 2026-08-25 by E1-04**: the narrowed `frontend` probe is gone, withdrawn in the same change as its last two readers when that ticket made all four frontend gates enforcing; the narrowing paragraph and its consequence stay in place, struck through, because that reasoning is the price E1-04 paid rather than an argument that turned out wrong |
| [0086](0086-the-spa-is-served-by-the-app-factory-at-app.md) | The single-page application is served by the app factory, at `/app` | Accepted — E1-04, reconciling the two halves of SPEC §13: the section gives `main.py` the SPA static serve and also draws a `frontend/Dockerfile`, and the first wins. One process serves the API and the application, the api image gains a Node build stage pinned to the same major `NODE_VERSION` names, and `FRONTEND_DIST` points at the copied bundle. The mount is decided when the factory is called and a missing build is a supported state — `/app` is a 404 and nothing else changes, which is every checkout the backend suite runs on. Every unmatched path under the mount answers `index.html` so the client can route, and a matched one answers the file; nothing on the server knows the five route names. Carries the wiring contract with E1-08/E1-09/E1-13: the backend decides the landing role and redirects, the frontend renders what it is handed and never re-derives role. Rejected: a frontend container per §13's drawn Dockerfile (a fourth service plus a reverse proxy, and `docker compose up` split into two things that can be at different versions), serving at `/` (moves `/healthz`, both doors and `/openapi.json` to buy a shorter URL for five empty pages), `StaticFiles(html=True)` with no fallback (correct at `/app/`, 404 at every route a person actually arrives at), answering `index.html` to everything (a page whose every script returns HTML, 200 throughout), and a §6.3 setting for the dist path (two answers, both already known by the process). States the cost: three files spell `/app` and only the end-to-end spec can catch them disagreeing. **Amended 2026-08-31**: the record's own claim that the image's Node agreement with `NODE_VERSION` was a convention, and a drift between the two "a difference nothing compares," stopped being true — Dependabot proposed exactly that drift in #132 — and `tests/unit/test_types_node_tracks_runtime.py` now compares the image's node major against `NODE_VERSION` directly, alongside `@types/node`, so the three majors move as one or the gate is red |
| [0084](0084-the-mock-platforms-token-endpoint-bounds-an-assertion-at-five-minutes.md) | The mock platform's token endpoint bounds an assertion at five minutes, and refuses in RFC 6749's vocabulary | Accepted — E1-06. Five decisions the specifications leave to a platform and a client is then built against, taken together because a client meets them together: `aud` is the token endpoint's own URL and not the issuer, which is the value most likely to be compared against by accident; the lifetime bound is 300 seconds measured `exp - iat`, so the boundary does not move with how long the request took; every refusal is 400 with the RFC 6749 §5.2 code that says why, because [E1-11's client is only conformant if nonconformance is distinguishable](../tickets/e1/E1-11-roster-sync-service-client.md); the assertion is verified against every key in the set the tool publishes rather than the one the header's `kid` names, which is one line from the defect it resembles; and the issued token is a JWS the platform can check later without remembering anything. Rejected: accepting the issuer as `aud`, measuring the lifetime against the platform's clock, 401 for `invalid_client`, caching the fetched key set, an opaque token with a store, and deriving the tool's JWKS address from its launch URL |
| [0085](0085-the-tools-key-set-is-public-in-every-environment.md) | The tool's key set is public in every environment, and its `kid` is the key's thumbprint | Accepted — E1-06, the ⚠ half: this tool's first cryptographic production endpoint. [0082](0082-the-tools-signing-key-lives-in-the-database.md) decided custody and left the route open. `GET /lti/jwks` is registered unconditionally, because gating on `ENVIRONMENT` is what [0074](0074-the-openapi-schema-is-served-only-in-development.md), [0079](0079-the-developer-console-is-gated-in-the-handler.md) and [0063](0063-the-demo-seed-runs-only-in-a-development-environment.md) all do and a route written beside them inherits it — and a tool whose key set answers 404 in production cannot be registered anywhere that matters. The JWK is assembled from the public numbers rather than filtered out of a serialised pair, so a private member cannot appear through an omission; a deployment with no row answers 503 rather than an empty set, which a platform would accept and store. States what it costs: an unauthenticated route that reads the database per request, and `pulse_app` holding `SELECT` on the tool's private key |
| [0087](0087-healthz-and-dev-keep-disclosing-the-environment.md) | `/healthz` and `/dev` keep disclosing the environment, on one verdict | Accepted — E1-14, closing the carried entry's `/healthz` decision and the method-mismatch note it links to [0079](0079-the-developer-console-is-gated-in-the-handler.md). Keeps the `/healthz` `environment` field and leaves `/dev`'s `POST → 405 Allow: GET` exactly as measured, because the name is not a secret: the only consumer is an orchestrator health check, the value is `production` in production, and every guard the name points at ([0074](0074-the-openapi-schema-is-served-only-in-development.md), 0079, [0078](0078-the-login-state-cookie-is-signed-per-process-and-short-lived.md), [0063](0063-the-demo-seed-runs-only-in-a-development-environment.md)) still has to be reached and passed on its own. Rejected: dropping the field (breaks the orchestrator consumer and the README line for no guard gained) and gating it behind authentication (no auth model exists yet for these scaffold-era routes). No code changes; the disclosure is pinned in both directions by `tests/unit/test_healthz.py` and `tests/unit/test_dev_console_exposure.py` |
| [0088](0088-a-query-parameter-selects-one-wrong-launch-per-mint.md) | A query parameter selects one wrong launch per mint | Accepted — E1-07, the mock's own reason to exist per `docs/MISTAKES.md` entry 28: a driver that can only speak correctly leaves E1-08's refusal tests with nothing invalid to drive. `?defect=<name>` on the existing authorization endpoint, read off the URL regardless of method, so an omitted parameter runs the exact happy-path lines it always ran; every selector — the fifteen wrong launches and the three near-miss/edge fixtures together — is one tuple, and a mint carries exactly one. `reused_nonce` remembers a mock-process-local cache keyed by `nonce`, on one `WrongLaunchMinter` per running platform beside [0049](0049-the-mock-gradebook-is-per-application-state-in-memory.md)'s `GradeBook`; `foreign_signature`'s key is generated lazily on first use rather than at startup, so a platform that never asks for it does not pay [0035](0035-the-mock-platform-signs-with-standard-library-rsa.md)'s measured key-generation cost for nothing. Rejected: a dedicated endpoint (duplicates or skips the checks a correct launch already passes), overloading `state` or `login_hint` as the selector (couples it to values a real tool sends), a header (unreachable from a browser-driven click), and persisting the replay cache (nothing else here survives a restart either). States its own cost: the selector vocabulary is copied rather than imported everywhere it is consumed, because both mocks are packages named `app` ([0039](0039-the-two-app-packages-are-typechecked-in-two-runs.md)'s collision) |
| [0089](0089-the-session-both-doors-issue-and-the-launch-nonce-ledger.md) | The session both doors issue, and the launch-nonce ledger in Postgres | Accepted — E1-08, the ⚠ ticket. Defines the product's first session — a signed JWT carrying only `door`/`role`/`sub`/`iss`/`jti`/`iat`/`exp`, HS256 under one configured `SESSION_SECRET` (not per-process, so a restart does not log a sitting person out), 60 minutes, `HttpOnly`/`SameSite=None`/`Secure`-off-development cookie, a `jti`-bound double-submit CSRF primitive, and a cookieless delivery that hands the token to the SPA in the landing URL's fragment so the Bearer path carries it with no cookie required. The replay ledger is a Postgres table spent by a plain `INSERT` whose unique-constraint violation is the replay, purged daily; and the launch *handshake* is server-side too (`lti_launch_state`), keyed by `state`, so a launch validates inside a fully cookie-blocked cross-site iframe — which is what makes criterion 2 / §7.3 ("no third-party cookie is ever required") genuinely true, since a browser blocks an in-flight cookie whatever its attributes say (Todd's ruling on `docs/disputes/E1-08-01.md`; the launch door sets no cookie but the session one). Supersedes [0078](0078-the-login-state-cookie-is-signed-per-process-and-short-lived.md) in part (the launch door only; the web door keeps `login_secret` until E1-09). Rejected: a per-process session key, a one-row DB key table, `SameSite=Lax`, Redis for the ledger, and a cookie (or Redis) for the handshake. Costs: `jwcrypto` (LGPLv3+) enters the runtime closure as `pylti1p3`'s JWS verifier, the first weak-copyleft library the license gate reviews; and `pulse_app` gains `INSERT`/`DELETE` on the nonce ledger and `SELECT`/`INSERT`/`DELETE` on the handshake store, whose `RUNTIME_BASE_TABLE_PRIVILEGES` entries live in a test file (`docs/disputes/E1-08-02.md`, `docs/disputes/E1-08-05.md`) |
| [0090](0090-a-sanctioned-writer-passes-the-chokepoint-by-being-in-a-catalog.md) | A sanctioned writer passes the chokepoint by being in a catalog | Accepted — E1-10, closing the open half [0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md) and [0069](0069-three-rules-held-by-a-docstring-are-swept-out-of-the-source.md) both left with a "done when": `guard_write(table="course")` refused unconditionally and nothing recorded how the launch path and E1's roster sync were supposed to get through it. `guard_write` gains an optional `sanction`; with none its answer is exactly what it was, and with one the **catalog** decides — `SANCTIONED_WRITERS` maps a writer's name to its tables and the guard reads that rather than the `WriteSanction` it was handed, so a hand-built sanction naming an uncatalogued writer, or naming more tables than the catalog grants, is refused exactly as none would be. One entry today (`launch_provisioning` → course, section, user), pinned as an equality in a test file the guarded module cannot shrink (`docs/MISTAKES.md` entry 35); the `INSTRUCTOR` `role_assignment` row is outside the mechanism because no writer is granted that table. Rejected: an exclusion list in E0-35's sweep (moves an authorization decision into a test and leaves a write path passing no chokepoint), a `sanctioned=True` flag, trusting `sanction.tables`, a call-stack check, and doing it only in the database — which is [0045](0045-the-chokepoint-refuses-an-lms-owned-write-at-table-grain-plus-one-row.md)'s own preferred instrument and is unavailable, since the launch path and the sync are one connection. E1-10 spends the narrowest grant as well; the two instruments do not replace each other. States its limit: a sanction says which *code* is writing, not which request |
| [0091](0091-what-a-launch-provisions-and-what-it-writes-down-instead.md) | What a launch provisions, and what it writes down instead | Accepted — E1-10, with Todd's three rulings of 2026-08-26. A `user` row on **every** verified launch, before the landing is resolved and inserted with no prior read, so a person the door has no view for is still somebody E1-12 can link; a staff launch is the exact context-instructor URN and nothing else until E1-12 activates §7.3's leadership limb; a new section belongs to the one term whose dates contain the day of the launch, and a pre-term launch refusing is a named limit. The titleless case splits in two: `id` alone identifies no course and is refused, while a context that kept its `label` gets `"BIOL 215"` from the label's prefix and number plus a new `course.title_is_fallback` marker — which is what makes a real title replace a guess, a guess never replace a real title, and a renamed real title follow the LMS. Every refusal is a `launch_defect` row and never a failed launch: a closed enum, five fields, and never a `sub`, a name, an email or a claims payload (§10). Rejected: refusing the launch, logging instead of recording, leaning on `course.level`'s NOT NULL (a 500, not a refusal), a nullable or context-id title, an unmarked fallback, and recording the subject. **Round 3 — the security review — added two kinds and closed the rename residue from its other side.** `section` is bound to `(lti_deployment_id, lms_context_id)`, unique in the database, and a launch resolves by that pair *before* the parsed label: a Canvas course copy reproduces prefix, number and section code exactly, so a launch from the copy used to repoint the original section's stored roster address — which E1-11 fetches with the tool's own credentials — and rewrite its course's title. The two identities disagreeing is `context_collision`, and nothing at all is written, the title included. The claim-carried roster address is judged by `refuse_invalid_fetched_address`, the same rules `jwks_url` and `auth_token_url` pass — four then, five since [0101](0101-a-fetched-address-is-judged-by-what-it-resolves-to.md) — reached through one function rather than a copy, and a refusal leaves the address NULL, records `roster_address_refused` and still provisions the section (§7.3's never-synced state is a state, not a fault). States four limits: the launch day is UTC's, overlapping terms are decided by the most recently started, a section stored before the binding carries a synthetic context id, and a duplicate-key insert on the three tables is tolerated rather than reported |
| [0093](0093-the-web-door-keeps-the-cookie-carried-login-handshake.md) | The web door keeps the cookie-carried login handshake | Accepted — E1-09, settling what [0089](0089-the-session-both-doors-issue-and-the-launch-nonce-ledger.md) left as a deferral ("the web door keeps `login_secret` until E1-09"). [0078](0078-the-login-state-cookie-is-signed-per-process-and-short-lived.md)'s `pulse_oidc_login` cookie stays exactly as it is, and 0089's supersession of it is launch-door-only: the reason that handshake had to move server-side is the LMS's cross-site iframe, where a browser blocks a cookie whatever its attributes say, and `/auth/oidc/callback` is a top-level navigation to this tool's own address, which is what `SameSite=Lax` is written to allow. A server-side store for this door is rejected as a table, a migration, two grants and a purge beat bought for a problem the door does not have — a mechanism copied for symmetry is one nobody can later tell the reason for. What E1-09 widens is burn-after-use: the cookie is cleared on all three exits from the callback, the cancel branch included. Also records why `cancelled_page()` takes no argument (`error_description`/`error_uri` are attacker-chosen text) and why the log line repeats only an exact member of a four-code set. Costs: two `api` replicas still cannot serve one web login, two handshake mechanisms now exist on purpose, and a false sentence in `app/services/session.py` about a `None` `iss` — a value PyJWT will not encode — is named with a done-when rather than fixed, that module being read-only for this ticket |
| [0094](0094-subjects-resolve-to-ids-through-definer-functions.md) | A subject resolves to row ids through SECURITY DEFINER functions | Accepted — E1-11 and E1-12, which ship the file and this record byte-identical from both branches under their own revisions; `CREATE OR REPLACE` and a guarded role creation make whichever merges second a replay. Two point resolvers, `resolve_platform_user` and `resolve_person_for_user`, turn a subject the caller already holds into a row id and can return nothing but a uuid. They exist because the column that answers the launch half, `user.lms_user_id`, is the one [0091](0091-what-a-launch-provisions-and-what-it-writes-down-instead.md)'s round-3 review revoked from `pulse_app` — readable at table grain it lets the application connection enumerate every subject that ever launched and join a response back to a person — and because a lookup view cannot help, a view being filterable only on a column it exposes. The owner is `pulse_resolve_definer`, a NOLOGIN role holding SELECT on exactly five columns with no identity-bearing column among them ([0043](0043-the-reveal-function-has-an-owner-of-its-own.md)'s pattern). Rejected: re-granting the column, a lookup view, and a resolver per ticket. Consequences: the identity-grant inventory gains EXECUTE entries and `pulse_app` now holds a definer door beside `pulse_care`, and a future bulk resolution needs its own reviewed mechanism rather than a loop over these |
| [0095](0095-the-roster-syncs-enrollment-windows-and-what-it-refuses.md) | The roster sync records two windows, no status, and refuses a member it cannot date | Accepted — E1-11. Row added 2026-08-27 by E1-11's fix round: the record landed in E1-11 without one, which is the omission the paragraph above this table describes. What SPEC §3.4's three rules need from a row, decided where §8 is silent — the two `enrollment` columns are the tool's record of the window it was told about, and a member whose extension cannot be dated is refused rather than dated by guess. Rests on [ADR 0048](0048-enrollment-windows-ride-on-a-namespaced-nrps-extension.md)'s extension and reopens neither [ADR 0090](0090-a-sanctioned-writer-passes-the-chokepoint-by-being-in-a-catalog.md)'s chokepoint nor [ADR 0094](0094-subjects-resolve-to-ids-through-definer-functions.md)'s resolvers |
| [0096](0096-every-fetched-roster-url-is-judged-and-the-instructor-write-is-bounded-by-a-definer.md) | Every roster URL the walk fetches is judged, and the teaching-instructor write is bounded by a definer | Accepted — E1-11's security round, row added 2026-08-27 by its fix round. F1 (HIGH): a walk fetches the stored address and then whatever `Link: rel="next"` names, so every fetched URL goes through `refuse_invalid_fetched_address` rather than the stored one alone. F2 (MEDIUM): the table-wide `INSERT` on `role_assignment` is withdrawn and the teaching-instructor row is written through a definer, so the connection every screen runs on can no longer write a `CARE` assignment. Extends [ADR 0081](0081-a-registrations-addresses-are-refused-at-one-write-time-chokepoint.md) and [ADR 0090](0090-a-sanctioned-writer-passes-the-chokepoint-by-being-in-a-catalog.md); one residual carried in `docs/tickets/e1/deferred.md` |
| [0097](0097-the-identity-a-verified-subject-resolves-to.md) | The identity a verified subject resolves to, at both doors | Accepted — E1-12. The stored identity is the `person` row and the session carries it as `person_id`: a launch resolves `sub` → `user` → `person`, a web login resolves `(iss, sub)` → `person` through a new `web_login_subject` table, and NULL on the launch side is a state the session carries rather than a refusal ([0028](0028-a-student-holds-no-role-assignment.md)). The linkage is a table with two unique constraints — one subject is one person, and one web account per person, the second conservative and droppable in one migration — and `pulse_app` holds no grant on it at all, so a web login provably writes nothing. A merge is never inferred from a mutable claim; email equality is rejected by name, as [0024](0024-the-person-to-user-link-is-carried-by-person.md) rejected it for the person-to-user link. An unlinked web login gets a calm 200 page with its own testid, and identity is resolved before the role at both doors. §7.3's leadership limb, left dormant by [0091](0091-what-a-launch-provisions-and-what-it-writes-down-instead.md), activates by asking whether the resolved person holds a leadership role — through a new `holds_leadership` in `authz.py` beside `holds_care`, because that module is where every read of the unfiltered `assignment_scope` view belongs and a §4.1 invariant caught the first attempt to query it from the launch path. Amends [0068](0068-the-demo-seed-registers-the-mock-platform-behind-its-guard.md): the mock platform's reachable set goes from empty to exactly two named subjects, and the ADR states plainly what a launch as either of them reaches on a development box, the dean's purview included |
| [0098](0098-the-landing-comes-from-the-assignment-model.md) | The landing view comes from the assignment model, and which hat wins | Accepted — E1-13, closing the second entry of `docs/tickets/e1/carried-from-e0.md`. `app/services/landing.py` is deleted and which view a person opens on is resolved from their own live assignments, filtered in SQL by ADR 0026's `permits_launch`/`permits_web_login` and reduced by a recorded precedence — leadership, instructor, care, admin — with enrollment as the student fallback at the launch door alone ([0028](0028-a-student-holds-no-role-assignment.md)). The ordering is what the spec leaves open: §2 names the multi-hat problem and defers the switcher to E9, and the carried entry measured that reversing E0-18's unwritten ordering left 424 tests green. `STUDENT` is deliberately outside the ordering, so an assignment always beats an enrollment and a teaching assistant opens on the section she grades. The door rule keeps exactly one authority: `chosen_landing` is pure and takes the filtered role set as given, so "a Care assignment cannot open a launch" is a property of the row rather than a Python branch beside a column that agrees with it today. Rejected: per-door orderings (instructor-first-on-launch, which §2 contradicts and which doubles what E9 must supersede), keeping the roles claim as a fallback or intersecting it with the rows (both hand an LMS administrator a veto or a grant), deferring wholly to E9, re-deriving the door rule in Python, a 4xx for a person with no view, and one function doing the reads and the decision. Consequences: `authz.py` reads one base table (`public.enrollment`, over E1-11's own grant) and its "five views and no base table at all" sentence is corrected; the Care sweep's `EXCEPTIONS` is empty, so its canary is the whole of what stands between that sweep and vacuity; the LIS vocabulary moves to `app/lti/launch.py`, which §7.3's provisioning and E1-11's sync go on reading lawfully; and E9's switcher supersedes the precedence rather than deleting it |
| [0099](0099-the-mock-enforces-a-token-on-nrps-and-not-on-ags.md) | The mock platform requires a token on NRPS, and still does not on AGS | Accepted — E1-11's fix round, closing the half [ADR 0084](0084-the-mock-platforms-token-endpoint-bounds-an-assertion-at-five-minutes.md) left open and the half SPEC §14.3's E1 exit line needs: a roster read is an authenticated service call or it is nothing. The roster refuses a call presenting no token this platform issued for NRPS 2.0's membership scope, in RFC 6750's vocabulary and in its `WWW-Authenticate` challenge; AGS keeps E1-06's argument, because the first grade-passback client is E3's (SPEC §14.3) and a service refusing before its first client exists would be refusing this repository's own tests (`docs/MISTAKES.md` entry 22). The check lives beside the mint in `app.tokens`, reading `issued_token`'s rules backwards, and is reached before every other judgement the route makes — which is why `page` carries no bound in the route signature, a constraint there being enforced before the handler runs at all (the review's LOW 1: `?page=0` answered 422 to a caller presenting nothing). Rejected: enforcing on AGS for symmetry, a store of issued tokens, the error code in a JSON body, an `error_description` in the challenge, and a flag for whether either service enforces |
| [0100](0100-the-exit-proof-reads-derived-dates-off-the-developer-console.md) | The exit proof reads derived dates off the developer console | Accepted — E1-15. SPEC §14.3's E1 exit line requires that a synced section shows correct derived dates and §9.2 makes the proof a browser proof, and E1 ships no product screen that renders a calendar — all five landings are empty by design, and E1-15's own scope forbids building one inside a proof ticket. So `GET /dev` grows a sections table behind the handler-body gate [0079](0079-the-developer-console-is-gated-in-the-handler.md) already applies, on the same route so that [0087](0087-healthz-and-dev-keep-disclosing-the-environment.md)'s method-mismatch verdict is not reopened: one row per section with its derived dates, length and modality, an enrolled count from `public.section_enrollment_count` (§4.1 invariant 4's view, whose `SELECT` is spelled in `dev.py` rather than reached through `app.views_sql.queries`, which a unit sweep keeps to one importer), and a yes/no on whether a roster address is stored — never the address, and **no identity column of any kind**, asserted by `tests/integration/test_the_dev_console_names_nobody.py` over a section a real sync has filled. Rejected: a JSON dev endpoint (a second gated route with its own disclosure analysis), asserting from the database outside a browser (the test that already exists, and not what §9.2 asks for), building a product surface, and carrying the clause past the epic boundary — the failure E0's README recorded. Costs: the console becomes a read path over roster-derived rows, a row key of prefix + number + code would collide across two terms, and the exit proof now rests on a development-only surface |
| [0101](0101-a-fetched-address-is-judged-by-what-it-resolves-to.md) | A fetched address is judged by what it resolves to, and the connection is pinned to it | Accepted — the E1 cleanup Batch C, closing the last open finding with a severity: a registered platform pointing a tokened roster fetch at an internal service that holds a valid certificate on a private address. Rule 5 at both chokepoints resolves the URL's host and refuses every returned address that is not `ip.is_global`, keeping [0096](0096-every-fetched-roster-url-is-judged-and-the-instructor-write-is-bounded-by-a-definer.md)'s loopback split for the operator-registered sidecar and refusing an unresolvable host outright; it runs after the four spelling rules, so a refused spelling is never looked up. The roster walk pins each host to the first address it resolved to and connects there under the platform's own hostname, with TLS verified against the name — a check of one address and a request to another is the redirect bypass one layer down. The write-time chokepoint stops being a call convention: `before_insert` and `before_update` events on `LtiPlatform` judge every ORM write, reading the environment the session states and judging a session that states none **as a deployment**. **Supersedes [0081](0081-a-registrations-addresses-are-refused-at-one-write-time-chokepoint.md) in part** — its private-ranges-accepted decision and its rejected `not ip.is_global` alternative — and pays that record's stated cost deliberately. Rejected: a literal-spelling denylist (resolution subsumes it and it says nothing about a name), a flat `is_global` with no loopback carve-out, judging without pinning, and `os.environ` as the event's environment. Amended by its own security review: both ends of the pin key on one canonical-host helper (`app.config.canonical_host` — case, one trailing dot, the IDNA form through urllib3's own encoder), because a pin written under one folding and looked up under another misses silently and the request is re-resolved; and a host the resolver will not *encode* raises `UnicodeError`, which is not an `OSError` and escaped the rules entirely. Residue, measured on SQLAlchemy 2.0.52: `bulk_save_objects`, an ORM-enabled `session.execute(update(...))`, a Core `insert()` and raw SQL fire no mapper event, while `session.add`, a dirty attribute and `merge` do — and the token POST and key-set fetch keep registration-time judgment only |
| [0102](0102-the-framing-policy-is-read-from-the-registration-table-per-document.md) | The framing policy is read from the registration table, per document | Accepted — E1-04 item 2 (Batch D). The app factory attaches a security header set to every response through a middleware registered last in `create_app()`: `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin` (keeps the full URL off a cross-origin request, so `unsafe-url` and the browser default are rejected), and a CSP of `default-src 'self'; script-src 'self'` — no `'unsafe-inline'` anywhere, because a real `npm run build` emits an external module script and an external stylesheet and injects neither inline. `frame-ancestors 'self'` plus the origin of every registered platform's `authorization_endpoint` is added only to `text/html` documents, read per request through `launcher_origins` so the policy tracks a platform registered after boot rather than a set frozen at startup — which keeps `/healthz` and every JSON body off the database. `launcher_origins` moves from the dev-console router to `app.lti.registration`, the platform-config module §13 names, so the console and the middleware read one derivation of one column (`docs/MISTAKES.md` entry 13). Serving a document does not depend on a reachable database: a read that cannot reach `lti_platform` degrades to `'self'` alone, never wider — fail-closed, and what lets the factory's own DB-less unit tests serve the SPA mount and `/docs`. Rejected: a static env-configured origin list (drifts from the registrations it mirrors — the setting E1-05 deleted, back under another name), `frame-ancestors` on every response including JSON (a database read on every liveness probe), a set computed once in `create_app()` (wrong the moment a registration is added), and failing the response when the read fails (makes the static shell depend on the database). Costs: the SPA document and `/docs` carry a DB-derived directive when the database is reachable and `'self'` when it is not; `tests/e2e/cookieless-launch.spec.ts` is the browser-side proof the iframe loads and the check for any runtime inline style the built stylesheet does not cover |
| [0103](0103-the-refusing-guard-is-named-in-a-data-reason-marker.md) | The refusing guard's class name is a `data-reason` marker on the refusal page | Accepted — E1 cleanup Batch B item 2. Both doors share `refusal_page`, and E1-15's browser proof read its prose to say which guard refused; the ten `LaunchRefusedError` subclasses and the web door's `SessionRefusedError` are already the machine vocabulary the door logs. That class name reaches the page as `data-reason="<guard>"` on the container carrying `data-testid="pulse-entry-refused"` — `refusal_page`/`refused` take a keyword `guard`, the launch door passes `refusal.guard` (a property that is the class name, except the nonce-ledger replay wrapper names the wrapped `NonceReplayedError`), the web door passes `type(refusal).__name__`. When no guard is named the attribute is omitted entirely, never `data-reason=""`, which the suites' exactly-one-marker assertion depends on; the value is escaped and the three calm pages render no marker. Rejected: prose only (couples every refusal spec to error copy), an opaque numeric code (a second vocabulary against the class names that already exist), and rendering `data-reason=""` (defeats the exactly-one-marker check). No new disclosure — a guard name is not a person, section or count — and the §4.1 launch-view invariant is untouched because a refusal is not a landing. **Amended by the E1 boundary fix (M7) and ADR 0106**: the marker decision stands, but the guard name is now the *only* parameter `refusal_page`/`refused` take — the body copy is derived from it through a `REFUSAL_COPY` constant with a constant default, so no caller's words can reach the page at all — which leaves no attribute-less case (the web door's provider-error branch names its own `SessionRefusedError`), and the launch vocabulary is eleven subclasses rather than ten |
| [0104](0104-the-unit-and-integration-pass-runs-under-xdist.md) | The unit and integration pass runs under xdist, with a Postgres per worker | Accepted — the CI wall-clock change. `Test · pytest + invariants` was 20.6 minutes on the last green run and every other job finished well before it, so the unit and integration pass runs under `pytest-xdist` with `-n 4`, in the workflow and in the Makefile `test` target that mirrors it. The suite already provisions its own testcontainers Postgres per session, so there is no shared database to collide in — and no shared database is also why four workers mean four containers and four alembic runs. That duplicated setup, about a minute in parallel, is accepted rather than engineered away. The §4.1 invariant pass stays serial: 90 tests, no minutes in it, and its exit code is masked so the verdict comes from a checker reading JUnit XML that a crashed worker could truncate. Rejected: a template database cloned per worker (a fixture layer between every integration test and its database, to buy back under a minute — the thing to build if the containers ever cost more than measured), sharding across matrix jobs (more runners, a coverage-combining step, and a `needs` entry per shard for a split `-n 4` already gets inside one job), and per-pull-request test selection (an import graph does not see this suite's fixture, migration and view dependencies, and a pass that silently stops running a test is the failure CI exists to prevent) |
| [0105](0105-hourly-roster-beat-and-the-fire-and-forget-trigger.md) | Hourly roster beat, and the trigger that must not hold a launch | Accepted — written at the E1 boundary, recording decisions E1-11 built and reasoned only in code comments. SPEC §7.3 orders the roster pulled "on schedule and on launch (debounced)" and says nothing about cadence, schedule type, or a down queue. Decided: hourly as `crontab(minute="0")` rather than a `timedelta` (an interval drifts with every beat restart, so which minute an institution syncs in would depend on when beat last came up); the walk is scheduled rather than the per-section task (the walk *is* the scheduled half's discovery); the launch trigger is fire-and-forget — `retry=False`, `ignore_result=True`, `try`-wrapped, failure logged at error level — because a person must never fail to enter the product over an unavailable queue, and a lost trigger costs at most the hour the beat already bounds. Rejected: a `timedelta` schedule, per-section scheduling (moves discovery into beat), and a retrying or blocking trigger (inverts §7.3's priority — entry first, freshness second). Consequences: staleness and trigger loss share the one-hour bound; the walk has no term filter (carried, `docs/tickets/e1/boundary-review.md`); the cadence is code, not a §6.3 knob |
| [0106](0106-a-launch-that-names-nobody-is-refused.md) | A launch that names nobody is refused, against LTI 1.3's MUST | Accepted — E1 boundary fix batch B (M4). LTI 1.3 Core §5.3.6.1 says a tool MUST read a missing `sub` as an anonymous-user launch, so such a token is conformant; Pulse has no anonymous user — every §4 view is somebody's and the door resolves the subject to a `user` row — and until now such a launch passed every guard, spent its nonce, and then raised inside `app.services.provisioning`, answering an unauthenticated caller with a 500 and refusing the platform's retry as a replay (boundary review M4). Decided: `AnonymousLaunchRefused` joins the `LaunchRefusedError` family and fires as the last check in `_validate`, after the version check and before the nonce is spent — so the calm shared page, HTTP 400, `data-reason="AnonymousLaunchRefused"`, the one WARNING carrying the guard name, the consumed handshake and the absent session all follow from the family rather than from a special case. Rejected: supporting anonymous launches (a second identity-free shape for every view, and a second answer to "whose data is this"), leaving the crash (a conformant message answered 500 after spending its nonce), refusing inside the writer (the nonce is already spent and admission is the door's job), and substituting a synthetic subject (unbounded unlinkable `user` rows — the `pre-binding-section-` idea that H2 is about). Consequences: Pulse is knowingly non-conformant with that MUST and this record is the answer to give; an anonymous-launch platform gets a named refusal rather than an error; the launch guard vocabulary grows from ten names to eleven |
| [0107](0107-the-org-view-sweep-polices-a-catalog-closure-with-pinned-exemptions.md) | The org-view sweep polices a catalog closure, with pinned exemptions | Accepted — E1 boundary fix batch C, closing boundary finding M8. The raw-SQL sweep's policed inventory is parsed at runtime from `views_sql/*.sql` — every `CREATE VIEW` name plus every relation the view bodies read FROM — so `enrollment` is policed because the roster views are built on it and no hand-written list exists for a future grant to outgrow; as written the closure holds fourteen relations including `course` and `section`, and that breadth is deliberate: a legitimate raw read that reds the sweep either moves to a sanctioned location or lands an exemption in its own reviewed commit with a statement pin, and the inventory is never narrowed. Exemptions are four locations — `authz.py` and the `views_sql/` package unpinned (reading these relations is their whole job; the one-importer sweep guards the package), `api/dev.py` pinned to exactly `section_enrollment_count` (ADR 0100), `services/safety.py` pinned to exactly `role_assignment` (the four-copies holds-Care design on the `pulse_care` credential, caught by the closed sweep's first real run and ruled rather than silently excused) — with each pin asserted as an equality, so the excused read must exist and nothing may join it unseen. Rejected: the hand list (the finding itself), a GRANT-derived inventory (polices ordinary product tables under a rule nobody agreed to; attempted and reversed in this batch), routing the Care check through `authz.py` (wrong credential), and unpinned whole-file exemptions (both pinnable files' reasons covered one statement each) |
| [0108](0108-the-leadership-limb-is-scoped-by-the-launchers-own-grant.md) | The leadership limb binds only what the launcher's own grant reaches | Accepted — E2-02, closing the E1 boundary review's M9. §7.3's leadership limb referred to the launch's context nowhere, so any holder of a live leadership assignment could bind any section and store its roster address permanently; the limb now unions `_own_grant_of` over the person's `LEADERSHIP_ROLES` assignments (`authz.leadership_grant_covers`, no new SQL) and binds only where the launch's section, its course, **or the prefix above it** is in that union. Prefix-or-below is the design answer the ticket owed: a dean's first launch into a course Pulse has never seen is in no course set by construction, and the same prefix test is what refuses a dean launching into another college. The claim limb is exempt, because the LTI roles claim is context-scoped and an Instructor URN already states staffness of the launched context. A refused launch records `context_outside_purview` and still lands the person — the binding is refused, never the launch. Rejected: course-or-section only (breaks the first launch), transitive purview (raises until E9, ADR 0003), trusting the roles claim for leadership (E0-09: the platform's administrator writes the claims), gating both limbs (stops every real instructor discovering their own section), refusing the launch itself. Consequences: an assistant dean holding nothing else fails closed until E9 (ADR 0046), a VP passes everywhere as §2.1 says, and the mock world's dean is re-scoped to the college he actually launches into |
| [0109](0109-the-dev-clock-is-a-database-offset-not-a-freeze.md) | The development clock is a database offset, not a freeze | Accepted — E2-04. SPEC §3.1 makes every survey window wall-clock behaviour and E2 has to be drivable by hand, so `app.services.clock` becomes the one place scheduling and visibility code asks the time — `now` (the instant, UTC) and `today` (the date, institution timezone) — and the three direct `datetime.now(ZoneInfo(...)).date()` copies in provisioning, the roster sync and `authz` read it. The development override is a **single-row table** rather than a process setting, because the tool and the worker are two processes and E2-06 schedules in the worker; and an **offset** (`real + (pretend_now - anchored_at)`) rather than a freeze, because a frozen Friday 18:00 never reaches Sunday 23:59:59 and no window a developer opens would ever close. It applies only where `is_development(settings)`, checked before the table is read, so a deployment holding a stray row issues no statement about it; the `/dev` control carries the console's own in-handler gate (ADR 0079) and answers 404 — never 405 — to every method it does not serve, which is stricter than the page beside it (ADR 0087) because a control is a thing to attack. **"Every method" is enforced by matching any method at registration and refusing in the handler**, after the security round of 2026-09-01 measured the first version — which enumerated the verbs it answered — handing `TRACE` and any unlisted token the router's own `405 Allow: POST` before the environment check ran (`docs/MISTAKES.md` entry 35); the price is the two paths' OpenAPI entries and their `Depends`, so they open their own session. Accepted residue, named rather than left to be found: the controls take a plain form with no CSRF token, so a page a developer visits can post to their own stack's clock — accepted beside the console's larger become-any-user exposure, narrowed by the loopback-only port publish, and gated for both by the exact-equality environment check; an `Origin`/`Sec-Fetch-Site` check is the hardening considered and not taken, because it belongs to `/dev` as a whole rather than to the clock. The clocks it deliberately does **not** touch are listed by name: launch validation (nonce, state, `exp`, skew), session expiry, audit timestamps, `func.now()` defaults, the NRPS debounce and call log, and beat's own firing schedule. One column is named so two tickets cannot each leave it to the other: `response`'s submission timestamp is application-written through this service (E2-08), because a row stamped by a server default could sit outside the window that accepted it and E3 counts exactly those rows. Rejected: a freeze, an environment variable or module global (moves one process), freezegun/time-machine (patches a process, answers neither the worker nor the browser), browser-side faking (the backend decides what is open), a `singleton boolean` column for the one-row rule (ADR 0072 measured it), `models/term.py` as the home (that is the institution's configured calendar, not a developer's scaffold), and a third service function for the console's "is an override standing" readout. Consequences: `pulse_app` gains `SELECT, INSERT, DELETE` on `clock_override` with `UPDATE` withheld, recorded deliberately; the gate is behavioural in two places and structural in none; a stack left overridden stays overridden across restarts, which is why the console shows the effective clock beside the sections table; and the sweep question is stated as a review rule — a new direct `datetime.now` in scheduling code is the thing to ask about — rather than built as a test whose exemptions would outnumber its catches |
| [0110](0110-answer-values-are-validated-by-the-write-path.md) | An answer's value is validated by the write path, not by a constraint on `answer` | Accepted — E2-05. SPEC §3.2's ranges are stored as data on `question` — a minimum, a maximum and a step per row — because the set is versioned so a later feature can append questions without a schema migration. A `CHECK` cannot read another table ([0018](0018-cross-table-length-rules-are-enforced-by-a-composite-foreign-key.md)'s opening problem), so `answer` carries no range check at all: its only rule over the three value columns is `num_nonnulls(rating, comment_text, workload_hours) = 1`, which is about the shape of the row rather than the value in it, and E2-08's write path checks a submitted value against the question it answers. Rejected: a literal `rating BETWEEN 1 AND 5` (a second statement of a rule already on `question`, and wrong at the first v2 set with a 1-to-7 scale); [0018](0018-cross-table-length-rules-are-enforced-by-a-composite-foreign-key.md)'s composite-foreign-key mechanism carrying the bounds onto `answer` (it works — rejected on cost: three carried numerics on the largest table in the schema, silently unchecked under `MATCH SIMPLE` for exactly the comment rows, a step rule needing a modulo in a `CHECK`, and an `ON UPDATE CASCADE` that rewrites every answer row when an admin edits a range); a trigger joining to `question` (commits a violating row under a concurrent edit, the reason 0018 rejected one); and a range wide enough to refuse nothing. Costs: anything that is not E2-08 can store a rating of 9 — contained by E2-05 granting `pulse_app` nothing on these tables at all — and E2-08 owes the validation and its two edge tests, the out-of-range value and the in-range value that is not a multiple of the step |
| [0111](0111-survey-windows-are-materialized-up-front-and-answered-at-read-time.md) | Survey windows are materialized up front and answered at read time | Accepted — E2-06. SPEC §3.1 gives the weekly rhythm and E2-05 gave `survey_window` its columns; the spec settles neither when a row exists nor who writes it. Every window a section's calendar implies is written up front in one idempotent pass, and open-or-closed is a comparison against `app.services.clock` made when the question is asked — because Celery beat fires on real time (ADR 0109 leaves beat's own schedule outside the override), so a design that wrote a row when the window opened would never answer a developer pretending it is Friday evening, which is the one thing E2-04 was built for. `app.services.survey_windows` is the only writer (ADR 0021's shape, swept by `tests/unit/test_survey_windows_have_one_assignment_site.py`), with two call sites that call the same function: an hourly reconciler on `crontab(minute="30")` — minute 30 because minute 0 is E1-11's roster sync and both walk every section — and `scripts/seed.py` after `seed_sections()`, so a seeded stack has windows immediately. Both ends of a window are inclusive (`opens_at <= instant <= closes_at`), which an offset clock cannot stand on, so `open_window_for_section` takes an `at` a test supplies and every production caller leaves `None`. A course week whose term has no `week` row yields no window, one warning and no exception — ADR 0018's lengthening gap, whose repair is E11's. Rejected: materializing a window when it opens (dead under the development clock); writing windows from the flows that create sections (E2-02's ingestion surface — the price of not taking it is up to an hour of staleness, accepted); deriving on read and storing nothing (leaves E2-05's table empty forever while E2-08 and §3.4 are keyed to those rows); an upsert rather than a skip (takes E11's re-derivation decision hourly with nobody watching); and re-reading the section's code against the start-letter map rather than the columns `apply_section_code` already derived. Consequences: a section can be an hour old before it has windows; `pulse_app` needs `SELECT` and `INSERT` on `survey_window` with `UPDATE` and `DELETE` withheld, recorded in `RUNTIME_BASE_TABLE_PRIVILEGES`; the rhythm ships as four cited constants until §6.3's surface is built in E11; the zone conversion is per instant, not per window, which is what the daylight-saving week of 1 November 2026 turns on |
| [0113](0113-the-mock-model-provider-is-development-only-and-selects-in-band.md) | The mock model provider is development-only, and selects its wrong answers in band | Accepted — E2-07. `mock-ai` joins the base Compose file (ADR 0038), so `AI_PROVIDER_BASE_URL` gains the pair the identity provider's four URLs already carry (ADR 0077): the E0-37 item 12 transport rule becomes environment-conditioned, because `http://mock-ai:8000/v1` is cleartext to another container and an unconditional rule refuses the configuration `.env.example` ships and CI copies to `.env`; and a second validator refuses a base URL whose host is `mock-ai` outside development, compared as a normalised component so an institution's own `mock-ai.example.edu` is not. The exemption is the environment and not the host, and the on-this-machine exemption does not move — a sidecar is `localhost` in production too. `environment` moves to the head of its block because pydantic validates in declaration order and a rule reading `info.data` from above it sees `None` and permits everything. The mock's wrong answers are selected **in band**, by `mock-ai:503` / `:500` / `:malformed` / `:stall` and three forced verdicts in the comment itself, because a query parameter or a header would mean the gateway putting something there — the backend change E2-07's scope forbids — and an arming route would make the service stateful, which makes ADR 0053's one bounded re-ask succeed and puts §7.4's "then the error" out of reach. Rejected: TLS in front of the mock, pointing the stack at the published host port (unreachable from inside the network), naming `mock-ai` as the one permitted cleartext address, "in a deployment require https" with no loopback carve-out (refuses the §7.4 sidecar), equality against the development URL, substring matching, a client-id catalog entry (there is nothing to hold — it authenticates nobody), extracting on `[[STUDENT_COMMENT]]` (`render_prompt` replaces it, so it never reaches a provider) and an unreachable selector (E2-08 drives that row at a closed port). Costs, stated: a student's comment decides what the provider does wherever `ENVIRONMENT` is `development`; the prompt's marker line is a second copy in `mock-ai/app/rules.py`, held against the prompt by a unit test; the transport rule is now weaker on a laptop than it was; and field declaration order is load-bearing in two places with nothing enforcing it |
| [0114](0114-an-unclassifiable-comment-refuses-rather-than-floors.md) | A comment the provider cannot be asked about refuses; it does not floor | Accepted — E2-08. [0056](0056-what-fails-open-what-raises-and-what-the-provider-url-may-carry.md) left this decision to E2 twice in as many words, and this is it: the three classes it keeps outside SPEC §3.3's floor — unreachable, refused, invalid — answer HTTP 503 with `Retry-After: 60` and the `submit.classifier_down` sentence, with the response and its answers rolled back so the student's work is still in the form and no row exists they were not told about. The three are enumerated rather than caught by their base class, which would widen the one sanctioned fail-open to every failure there is. The same route's other statuses are recorded here because they are one answer table: a bounce is 422 carrying the verdict and its coaching copy, an unreachable section and an unknown one are both 404 with the same body (§4.1 item 1), and a closed window and a duplicate are 409. Rejected: flooring on everything (hands "is this comment classified" to anyone who can drop packets, and hides a permanent misconfiguration one comment at a time), a raw 500 (no `Retry-After`, and it loses the one thing the refusal has to say), storing the submission and marking it invalid (§3.3's exact opposite), and accepting with no classification at all (ADR 0054's audit pair is how the async sweep finds work, so such a row is one nothing comes back to). Costs: a provider down in a way 0056 does not floor blocks submission entirely, loudly and on purpose |
| [0115](0115-a-resubmission-revises-its-answers-in-place.md) | A resubmission revises its answer rows in place, because a judged comment cannot be deleted | Accepted — E2-08, deviating from that ticket's work order, which settled both halves of an incompatible pair. `classification.answer_id` names the comment a verdict judged under `ON DELETE RESTRICT` ([0055](0055-a-classification-row-names-its-task-and-no-comment.md)'s promised reference), so the delete-and-reinsert the work order names would have made the first resubmission of any classified comment a 500. Instead each question's row is revised where it exists, inserted where it does not, and deleted where the resubmission no longer answers it — unless a verdict names it, which is refused in words (`submit.comment_already_judged`, 409) rather than left to surface as a constraint error under a student. Rejected: `ON DELETE SET NULL` (an `UPDATE` on an append-only table through a path the grants cannot see, leaving a verdict about nothing), `CASCADE` (an ordinary edit erasing §7.4's audit record), keeping the withdrawn comment silently (the words reach §5.1's report after the student removed them), and refusing resubmission outright once anything is classified. Costs: a classified comment can be revised and not withdrawn, which is a product rule this record creates and the copy states; and answer-row identity now survives a resubmission, which the re-classification sweep depends on |
| [0116](0116-the-three-webfonts-are-self-hosted-in-the-bundle.md) | The three webfonts are self-hosted in the bundle | Accepted — E2-10, closing E1-04's deferred item 1 against the epic's first real screen, which is the circumstance that entry's "done when" named. `@fontsource/literata`, `@fontsource/schibsted-grotesk` and `@fontsource/spline-sans-mono` are pinned exactly at 5.3.0 and imported from `frontend/src/main.tsx` — six imports, one per weight `docs/DESIGN_BRIEF.md` gives a job (Literata 600/700, Schibsted Grotesk 400/500, Spline Sans Mono 400/500), latin subset, normal style — so the bundler emits the faces beside the bundle and the browser fetches them from the tool's own origin. `design/tokens.css` is untouched and stays where the families and their fallbacks are named. Rejected: a `<link>` to a font host (a third-party request made on a student's behalf from inside their institution's iframe, and first paint made to depend on a host outside the deployment — E0-18's call for the server-rendered landings, transferred), shipping the fallbacks (the brief spends a section on why the three faces are load-bearing), variable fonts (this design uses two weights per family, so the whole axis is the larger file), a custom glyph subset (a build step this repository would own and have to keep correct as copy changes), and a `<link rel="preload">` nothing has measured a need for. Costs, measured on the branch: **+219 bytes gzipped** in the payload the budget counts (93,475 of 163,840 after), **119,100 bytes of woff2** the budget does not count, and a further **147,028 bytes of woff** that ships because each `@fontsource` stylesheet lists both and no supported browser fetches it — accepted rather than hand-writing six `@font-face` rules that restate the package. The three packages are OFL-1.1, which `scripts/ci/check_licenses.py` has no rule for, so they report as unrecognized rather than failing; that and the licence's own attribution requirement are `docs/tickets/e2/deferred.md`'s, because `scripts/ci/` is a heavy-lane path |
| [0117](0117-the-survey-screen-calls-two-endpoints-by-hand.md) | The survey screen calls its two endpoints by hand | Accepted — E2-10. SPEC §13's tree draws `frontend/src/api/` as "generated client from backend OpenAPI + TanStack Query hooks" and E1-04 left the choice to "E2's decision to make against a real screen" in `main.tsx`; this is it. Two `fetch` calls in `frontend/src/api/student.ts`, the wire types transcribed in the wire's own spelling (snake case, decimals as strings), and this route's status table turned into four named outcomes — stored, bounced, closed, refused — because the interesting part is which of `app.copy`'s sentences a status carries. Rejected: generating a client from `/openapi.json` (a generator, its output, a build step and a currency check, for two calls — and the generated client would still need the outcome mapping; the argument reverses when a later epic makes it twenty), TanStack Query (a cache, retries and invalidation for a screen that reads once and posts once, inside a budget with a product reason behind it), fetching in the router's loader (a loader that throws turns a sessionless read into an error page, where the honest answer is the calm screen), and a schema library at the seam (a third dependency restating a shape the server publishes). Costs: the wire types are a transcription nothing checks, so a renamed member goes stale silently and the end-to-end spec is what catches it; and there is no cache, so returning to the screen refetches — which is right for a window that may have closed |
